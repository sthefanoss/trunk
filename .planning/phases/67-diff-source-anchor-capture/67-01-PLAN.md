---
phase: 67-diff-source-anchor-capture
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/diff-anchor.ts
  - src/lib/diff-anchor.test.ts
autonomous: true
requirements: [ANCH-01]

must_haves:
  truths:
    - "L-02: a pure TS adapter translates selected diff indices -> (side, start_line, end_line) via each line's old_lineno/new_lineno/origin"
    - "L-01: the produced Anchor carries ONLY commit_oid, file_path, source, side, start_line, end_line — never hunk_index/line_index/context_lines/ignore_whitespace"
    - "L-04: file status forces the side — Added->New, Deleted->Old, Renamed->New(+new path); Copied->New(+new path); Untracked/Unknown->treated as Modified"
    - "L-03: a mixed Add+Delete selection resolves to side=New, drops pure-Delete lines from the persisted range, but the cachedExcerpt still includes those `-` lines"
    - "D-03: non-contiguous selections collapse to min..max of the chosen side's line numbers with no rejection"
    - "L-06: cachedExcerpt is assembled at capture-time in diff format over the contiguous min..max index span (origin-prefixed lines incl. dropped `-` and in-between context)"
  artifacts:
    - path: "src/lib/diff-anchor.ts"
      provides: "Pure capture-time adapter: indices -> { anchor, cachedExcerpt }"
      exports: ["buildDiffAnchor"]
      min_lines: 40
    - path: "src/lib/diff-anchor.test.ts"
      provides: "No-mock Vitest unit tests for all selection shapes (SC-1, SC-3)"
      contains: "describe"
  key_links:
    - from: "src/lib/diff-anchor.ts"
      to: "src/lib/types.ts"
      via: "import type { Anchor, DiffLine, DiffStatus, FileDiff, Side, Source }"
      pattern: "import type.*types"
---

<objective>
Build the pure capture-time adapter that converts an in-memory diff line selection into a stable source-coordinate `Anchor` plus a diff-format `cachedExcerpt`. This is the heart of L-02/L-03/L-04/L-06 and the highest-value TDD target in the phase: defined I/O, no side effects, no IPC.

Purpose: keep the `add_comment` Rust command a dumb writer (RESEARCH §Summary) — all coordinate translation and excerpt assembly happen client-side where every input already lives in memory.
Output: `src/lib/diff-anchor.ts` (exported `buildDiffAnchor`) + `src/lib/diff-anchor.test.ts` covering every selection shape from 67-VALIDATION.md SC-1/SC-3.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-diff-source-anchor-capture/67-PATTERNS.md
@.planning/phases/67-diff-source-anchor-capture/67-RESEARCH.md

<interfaces>
<!-- Verified shapes the adapter reads (src/lib/types.ts) — use directly, no codebase exploration needed. -->

From src/lib/types.ts (existing — DO NOT redeclare):
  type DiffOrigin = "Context" | "Add" | "Delete"
  interface DiffLine { origin: DiffOrigin; content: string; old_lineno: number | null; new_lineno: number | null; spans: ... }
  type DiffStatus = "Added" | "Deleted" | "Modified" | "Renamed" | "Copied" | "Untracked" | "Unknown"
  interface FileDiff { path: string; status: DiffStatus; is_binary: boolean; hunks: DiffHunk[] }
  interface DiffHunk { ...; lines: DiffLine[] }
  type Source = "Diff" | "FullFile"
  type Side = "Old" | "New"
  interface Anchor { commit_oid: string; file_path: string; source: Source; side: Side; start_line: number; end_line: number }

git2 maps libgit2's -1 "absent on this side" sentinel to null:
  Add line:     { origin: "Add",     old_lineno: null, new_lineno: 42 }
  Delete line:  { origin: "Delete",  old_lineno: 17,   new_lineno: null }
  Context line: { origin: "Context", old_lineno: 16,   new_lineno: 41 }
</interfaces>
</context>

<feature>
  <name>buildDiffAnchor — pure index->source adapter</name>
  <files>src/lib/diff-anchor.ts, src/lib/diff-anchor.test.ts</files>
  <read_first>
    - src/lib/merge-parser.ts — the exact shape for a pure, fully-tested transform lib (file-level doc comment asserting purity, exported return `interface`, Set-based immutable helpers). MIRROR this structure.
    - src/lib/merge-parser.test.ts — no-mock Vitest structure (`import { describe, expect, it } from "vitest"`, `.js` import extension). MIRROR this for the test file.
    - src/lib/types.ts (lines ~18, 145-176, 285-316) — DiffOrigin, DiffLine, DiffStatus, FileDiff, Anchor/Source/Side. REUSE these types; do NOT redeclare.
    - .planning/phases/67-diff-source-anchor-capture/67-PATTERNS.md §"src/lib/diff-anchor.ts" — resolveSide core, range-from-side-filtered-linenos, and the excerpt-vs-range divergence note.
    - .planning/phases/67-diff-source-anchor-capture/67-RESEARCH.md §"Pitfall 3" — range excludes dropped Delete lines; excerpt includes them.
  </read_first>
  <behavior>
    Exported signature: `buildDiffAnchor(commitOid: string, file: FileDiff, hunkIdx: number, selectedLineIndices: Set<number>): { anchor: Anchor; cachedExcerpt: string }`. Returned `anchor.source` is always `"Diff"` in this phase; `anchor.file_path` is `file.path` (the new path for Renamed/Copied per L-04).

    Test 1 (SC-1, pure-Add): selection of only Add lines on a Modified file -> `anchor.side === "New"`, `start_line === min(new_lineno over selection)`, `end_line === max(new_lineno)`.
    Test 2 (SC-3, pure-Delete): selection of only Delete lines -> `anchor.side === "Old"`, range = min..max of old_lineno.
    Test 3 (SC-3, mixed Add+Delete on Modified): -> `anchor.side === "New"`; range = min..max of new_lineno over the Add lines ONLY (Delete lines excluded from range); `cachedExcerpt` STILL contains the `-`-prefixed Delete lines.
    Test 4 (D-03, non-contiguous): a Set with gaps within one hunk -> range still collapses to overall min..max of the chosen side; excerpt spans the contiguous min..max index range so in-between unselected/context lines appear.
    Test 5 (L-04, Added file): status "Added" with an all-Add selection -> side forced "New" (assert it ignores any contrary signal).
    Test 6 (L-04, Deleted file): status "Deleted" -> side forced "Old".
    Test 7 (L-04, Renamed file): status "Renamed" -> side "New" AND `anchor.file_path === file.path` (the new path).
    Test 8 (A1/Q1, Copied): status "Copied" -> treated like Renamed: side "New", file_path = file.path.
    Test 9 (A1/Q1, Untracked/Unknown): treated like Modified — side derived from selected origins.
    Test 10 (L-06 excerpt format): cachedExcerpt lines are origin-prefixed (`+ `/`- `/`  ` matching the diff-format convention), built over the contiguous min..max index span of the selection.
    Test 11 (L-01): the returned anchor has exactly the 6 schema keys — assert `Object.keys(anchor).sort()` equals the frozen set; no hunk_index/line_index/context_lines/ignore_whitespace.
  </behavior>
  <action>
    RED first: create `src/lib/diff-anchor.test.ts` mirroring `merge-parser.test.ts` (no mocks — adapter is pure). Build plain `DiffLine[]` / `FileDiff` fixtures inline; enumerate Tests 1-11 above. Run `just vitest src/lib/diff-anchor.test.ts` and confirm it fails (module not found / function missing).

    GREEN: create `src/lib/diff-anchor.ts` mirroring `merge-parser.ts` purity discipline. Export `buildDiffAnchor`. Internal `resolveSide(status, selected)`: file status wins — "Added"->"New", "Deleted"->"Old", "Renamed"/"Copied"->"New", else (Modified/Untracked/Unknown) use origin-based rule: any Add present -> "New", else any Delete -> "Old", else "New" defensive default. Compute `start_line`/`end_line` as `Math.min`/`Math.max` over the chosen side's lineno field (`new_lineno` for New, `old_lineno` for Old), filtering nulls — this naturally drops Delete lines from a New-side range (L-03). Assemble `cachedExcerpt` over the CONTIGUOUS `min..max` index span of `selectedLineIndices` within `file.hunks[hunkIdx].lines`, prefixing each line by origin (`+`/`-`/space) so dropped `-` lines and in-between context appear (L-06). Construct the `Anchor` with `source: "Diff"`, `file_path: file.path`. Reuse the existing `Anchor`/`Side`/`Source` types from types.ts — do NOT redeclare. Do NOT place any `hunk_index`/`line_index` on the output.

    REFACTOR (if needed): extract excerpt-line prefixing into a small private helper; keep all functions pure. Re-run vitest green.

    Each phase is its own atomic commit per @/Users/joaofnds/code/trunk/.claude/get-shit-done/references/tdd.md (test(67-01)/feat(67-01)/refactor(67-01)).
  </action>
  <verify>
    <automated>just vitest src/lib/diff-anchor.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `just vitest src/lib/diff-anchor.test.ts` passes with all 11 behavior cases (maps to 67-VALIDATION.md SC-1 TS-adapter row + both SC-3 TS-adapter rows).
    - Source assertion: `grep -E "hunk_index|line_index|context_lines|ignore_whitespace" src/lib/diff-anchor.ts | grep -v '^[[:space:]]*//' | wc -l` returns 0 (L-01 — no array-index/options field).
    - Source assertion: `grep -c "interface Anchor\|type Anchor\|type Side =\|type Source =" src/lib/diff-anchor.ts` returns 0 (types reused from types.ts, not redeclared — PATTERNS correction).
    - Behavior assertion (Test 3): mixed selection range excludes Delete-line numbers AND cachedExcerpt string contains a `-`-prefixed line.
    - Behavior assertion (Test 7): Renamed file -> `anchor.file_path === file.path`.
    - `just check` (frontend portion) green before commit.
  </acceptance_criteria>
  <done>
    `buildDiffAnchor` exists as a pure function, all 11 tests pass, the output anchor has exactly the 6 frozen schema fields, and no diff-array-position data leaks into the anchor.
  </done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-memory transform; no I/O, no IPC, no external input crosses a process boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Tampering | `buildDiffAnchor` output | accept | Pure function over already-in-memory diff data; no new sink. Persistence/path-traversal surface is owned by Plan 02 (the Rust writer). No control here. |
</threat_model>

<verification>
- `just vitest src/lib/diff-anchor.test.ts` green (SC-1, SC-3 adapter rows).
- L-01 source assertion: no array-index/options identifiers in the adapter.
- Anchor schema-key assertion (Test 11) confirms the 6-field frozen shape.
</verification>

<success_criteria>
- SC-1 (adapter layer): pure-Add selection -> `{ side:"New", start:min, end:max }`.
- SC-3 (adapter layer): mixed Add+Delete -> `{ side:"New", ... }` with deletes dropped from range but kept in excerpt; pure-Delete -> Old; non-contiguous -> min..max; Added forces New; Renamed stores new path.
</success_criteria>

<output>
Create `.planning/phases/67-diff-source-anchor-capture/67-01-SUMMARY.md` when done.
</output>
