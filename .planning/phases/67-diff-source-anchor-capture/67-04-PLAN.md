---
phase: 67-diff-source-anchor-capture
plan: 04
type: execute
wave: 2
depends_on: [67-01, 67-02, 67-03]
files_modified:
  - src/components/diff/CommentComposer.svelte
  - src/components/diff/CommentComposer.test.ts
  - src/components/DiffPanel.svelte
  - src/components/diff/HunkView.svelte
  - src/components/diff/SplitView.svelte
autonomous: false
requirements: [ANCH-01]

must_haves:
  truths:
    - "D-01: a Comment affordance appears on an active commit-diff selection (mirroring the on-selection staging action row) and opens an inline expanding textarea below the selected lines"
    - "D-03: the composer shows a 'Comments on lines N-M' preview reflecting the collapsed min..max range from buildDiffAnchor"
    - "D-04: on a merge commit (commitDetail.parent_oids.length > 1) the Comment affordance renders DISABLED with tooltip 'Diff comments aren't available on merge commits' — taught at the point of action"
    - "D-04: file-status side constraints (L-04) do NOT disable the affordance — Added/Deleted/Renamed simply force the side; the affordance stays enabled"
    - "D-02: selecting a NEW range while the current draft text is non-empty prompts 'Discard your unsaved comment?' via plugin-dialog ask; an EMPTY draft switches silently"
    - "L-05: every keystroke persists the draft via save_draft_comment (debounced ~300ms on input idle); submit persists via add_comment with the buildDiffAnchor output"
    - "empty-text submit is disabled (button disabled when text.trim() === '')"
    - "split-view composer is new-side only by construction (RESEARCH Pitfall 2) — it never produces a side=Old anchor"
    - "on submit the composer + selection clear; confirmation relies on the existing session-changed -> panel reload (Claude's-discretion attach-success feedback)"
  artifacts:
    - path: "src/components/diff/CommentComposer.svelte"
      provides: "Inline textarea + N-M preview + submit/draft IPC + D-02 confirm + empty-text disable"
      min_lines: 50
    - path: "src/components/diff/CommentComposer.test.ts"
      provides: "Component tests: guard-lift selection, merge-disable+tooltip, confirm-on-discard, draft-on-keystroke invoke, empty-text disabled, split never Old"
      contains: "describe"
  key_links:
    - from: "src/components/diff/CommentComposer.svelte"
      to: "src/lib/diff-anchor.ts"
      via: "buildDiffAnchor(commitOid, file, hunkIdx, selectedLineIndices)"
      pattern: "buildDiffAnchor"
    - from: "src/components/diff/CommentComposer.svelte"
      to: "add_comment / save_draft_comment"
      via: "safeInvoke"
      pattern: "safeInvoke\\(\"add_comment\"|safeInvoke\\(\"save_draft_comment\""
---

<objective>
Build the inline comment composer and the Comment affordance (D-01), wire them to the Plan 01 adapter and Plan 02 commands, and enforce the capture-time UX rules: range preview (D-03), merge-commit disable + tooltip (D-04), confirm-before-discard-draft (D-02), debounced draft persistence (L-05), and empty-text submit disable. This is the user-facing surface that turns a commit-diff selection into a persisted anchored comment.

Purpose: deliver ANCH-01's user-visible capability — select a line range in the diff and attach a comment.
Output: CommentComposer.svelte + its component tests; Comment-affordance branches in HunkView/SplitView; composer host state + D-02 confirm in DiffPanel.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-diff-source-anchor-capture/67-PATTERNS.md
@.planning/phases/67-diff-source-anchor-capture/67-RESEARCH.md
@.planning/phases/67-diff-source-anchor-capture/67-01-PLAN.md
@.planning/phases/67-diff-source-anchor-capture/67-02-PLAN.md

<interfaces>
<!-- From Plan 01 (src/lib/diff-anchor.ts): -->
  buildDiffAnchor(commitOid: string, file: FileDiff, hunkIdx: number, selectedLineIndices: Set<number>): { anchor: Anchor; cachedExcerpt: string }

<!-- From Plan 02 (Tauri commands), called via safeInvoke<void>: -->
  add_comment        { path, text, anchor, cachedExcerpt }   // submit; emits session-changed; clears draft slot
  save_draft_comment { path, text, anchor }                  // keystroke; does NOT emit

<!-- From Plan 03: commitDetail is threaded to the commit DiffPanel; selection works in commit diffs. -->
  commitDetail.oid -> commitOid (anchor);  commitDetail.parent_oids.length > 1 -> isMerge (D-04 disable)

<!-- Patterns to mirror (verified): -->
  HunkView.svelte:172-213  on-selection action-button row (hasSelection-gated, disabled+title pattern = D-04 analog)
  SplitView.svelte:230-289 split-hunk-header action row (.staging-btn success-btn classes); right-col/new-side context
  DiffPanel.svelte:386-394 confirm-before-discard: dynamic import("@tauri-apps/plugin-dialog") ask(msg, { title, kind: "warning" })
  DiffPanel.svelte:341-360 handleStageLines: in-flight guard + safeInvoke + try/catch showToast + clearSelection in finally
  src/lib/invoke.ts:10     safeInvoke<T> (parses TrunkError) — all IPC goes through this
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CommentComposer.svelte + component tests (adapter wiring, preview, D-02, draft, empty-disable)</name>
  <files>src/components/diff/CommentComposer.svelte, src/components/diff/CommentComposer.test.ts</files>
  <read_first>
    - src/components/diff/HunkView.svelte (lines 172-298) — the on-selection action-row pattern + the `disabled={stagingDisabled} title={stagingDisabledTitle}` disabled+tooltip shape (the D-04 analog).
    - src/components/DiffPanel.svelte (lines 341-360 handleStageLines try/catch+showToast+clearSelection; 383-414 handleDiscardLines incl. the 386-394 plugin-dialog ask). MIRROR both.
    - src/lib/diff-anchor.ts (Plan 01) — buildDiffAnchor signature + return shape.
    - src/lib/invoke.ts (line 10) — safeInvoke<T> wrapper.
    - src/components/DiffPanel.test.ts (lines 1-75) — tauri-mock + invoke/store/toast mock setup; src/components/StagingPanel.test.ts (lines 28-32) — plugin-dialog ask mock. MIRROR for the component test.
    - .planning/phases/67-diff-source-anchor-capture/67-PATTERNS.md §"CommentComposer.svelte" and §"Vitest component test" — exact mock excerpts + coverage list.
  </read_first>
  <behavior>
    Tests live in src/components/diff/CommentComposer.test.ts (component-level; mock invoke/toast/plugin-dialog per PATTERNS). Write these BEFORE the composer passes them:
    - (a) Preview: a sample selection renders "Comments on lines N-M" where N..M match buildDiffAnchor(...).anchor.start_line/end_line (D-03).
    - (b) Empty-disable: Submit is disabled when the textarea is empty/whitespace, enabled once non-empty text is entered.
    - (c) Draft persist: typing into the textarea triggers a save_draft_comment invoke (flush the ~300ms debounce with fake timers + tick) with { path, text, anchor } (L-05).
    - (d) Submit: clicking Submit invokes add_comment with the buildDiffAnchor { anchor, cachedExcerpt } and the typed text; on success the composer clears.
    - (e) Confirm-on-discard: with non-empty text, confirmDiscardIfDirty calls plugin-dialog ask; a false return blocks the switch; with empty text ask is NOT called (D-02).
    - (f) Split never Old: a split/new-side selection fixture yields anchor.side === "New" (RESEARCH Pitfall 2).
  </behavior>
  <action>
    Create CommentComposer.svelte: props include the selected `file: FileDiff`, `hunkIdx`, `selectedLineIndices: Set<number>`, `commitOid`, and `repoPath`. Bind a `$state` textarea. Show a "Comments on lines N-M" preview where N..M come from `buildDiffAnchor(...).anchor.start_line/end_line` (D-03) — derive reactively from the selection. On every input, persist via `safeInvoke("save_draft_comment", { path: repoPath, text, anchor })` debounced ~300ms on idle (L-05; not per-keystroke fsync — RESEARCH Q2 decision). The Submit button is `disabled` when `text.trim() === ""` (empty-text submit disabled — Claude's-discretion validation point). On submit: call `buildDiffAnchor` for `{ anchor, cachedExcerpt }`, `safeInvoke("add_comment", { path: repoPath, text, anchor, cachedExcerpt })` with an in-flight guard + try/catch + `showToast(err.message, "error")` (mirror handleStageLines), then clear text + emit a close/clear event so the host clears the selection (attach-success feedback relies on the existing session-changed -> panel reload; no extra toast). All colors via `--color-*` vars (never inline a literal). Do NOT innerHTML user text (render via textarea / text node only — T-67-04). Expose a `confirmDiscardIfDirty(): Promise<boolean>` (or equivalent) the host calls before switching selection — it runs the plugin-dialog `ask("Discard your unsaved comment?", { title: "Discard Comment", kind: "warning" })` ONLY when current text is non-empty (D-02); empty switches silently returning true.

    Create CommentComposer.test.ts mirroring DiffPanel.test.ts mocks (tauri-mock, `vi.mock("../../lib/invoke.js", ...)`, toast mock) + StagingPanel.test.ts plugin-dialog ask mock. Cover: (a) the N-M preview text matches the collapsed range for a sample selection; (b) empty-text -> Submit disabled, non-empty -> enabled; (c) typing triggers a save_draft_comment invoke (debounce can be flushed with fake timers + tick); (d) submit invokes add_comment with the buildDiffAnchor anchor+cachedExcerpt; (e) confirm-on-discard: with non-empty text, `ask` is called and a false return blocks the switch; empty text does not call `ask`; (f) split-view context never produces side=Old (construct a split/new-side selection fixture and assert `anchor.side === "New"`).
  </action>
  <verify>
    <automated>just vitest src/components/diff/CommentComposer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `just vitest src/components/diff/CommentComposer.test.ts` passes cases (a)-(f) [maps to 67-VALIDATION.md SC-1 component row + the confirm-on-discard / draft-on-keystroke / empty-disable / split-never-Old Tier-Map rows].
    - Source assertion: `grep -c "buildDiffAnchor" src/components/diff/CommentComposer.svelte` >= 1 (adapter wired).
    - Source assertion: `grep -c "safeInvoke(\"add_comment\"\|safeInvoke(\"save_draft_comment\"" src/components/diff/CommentComposer.svelte` returns 2 (both commands wired).
    - Source assertion: `grep -c "ask(" src/components/diff/CommentComposer.svelte` >= 1 (D-02 confirm) and the ask is gated on non-empty text.
    - Source assertion: no inline hex/rgb color literal — `grep -nE "#[0-9a-fA-F]{3,6}\b|rgb\(" src/components/diff/CommentComposer.svelte | grep -v 'var(--' | wc -l` returns 0 (theme rule).
    - Source assertion: `grep -c "innerHTML" src/components/diff/CommentComposer.svelte` returns 0 (T-67-04).
    - Test (f) asserts a split/new-side fixture yields `anchor.side === "New"` (split never Old).
  </acceptance_criteria>
  <done>
    The composer persists drafts on keystroke (debounced), submits via add_comment with the adapter output, shows the N-M preview, disables empty submit, confirms before discarding a dirty draft, never renders user text as HTML, and split-view captures are always new-side. Component tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount the Comment affordance in HunkView + SplitView; host composer state + D-02 confirm in DiffPanel</name>
  <files>src/components/diff/HunkView.svelte, src/components/diff/SplitView.svelte, src/components/DiffPanel.svelte</files>
  <read_first>
    - src/components/diff/HunkView.svelte (lines 172-298) — add a `{:else if diffKind === 'commit'}` Comment-affordance branch AFTER the staged branch (254-298); do NOT touch the unstaged/staged staging branches (172/254).
    - src/components/diff/SplitView.svelte (lines 230-289) — same: a `commit` Comment branch in split-hunk-header after the staged branch (271-289), reusing `.staging-btn` classes; mounts in the right (new) column context.
    - src/components/DiffPanel.svelte (lines 33/51 commitDetail prop; 71-74 selection $state; 298-339 handleLineClick / clearSelection). Host the composer open/draft state alongside selection $state; derive `commitOid = commitDetail?.oid` and `isMerge = (commitDetail?.parent_oids.length ?? 0) > 1`.
    - .planning/phases/67-diff-source-anchor-capture/67-PATTERNS.md §"HunkView.svelte", §"SplitView.svelte", §"DiffPanel.svelte" — the disabled+title D-04 substitution and the host-state changes.
    - .planning/phases/67-diff-source-anchor-capture/67-RESEARCH.md §"Pitfall 1" (commitDetail derivation) and §"D-04" (file-status forces side, does NOT disable).
  </read_first>
  <action>
    In HunkView.svelte add a `{:else if diffKind === 'commit'}` branch inside the hunk-header toolbar (after the staged branch, ~254-298): a `hasSelection`-gated "Comment" button using the existing disabled+title pattern — `disabled={isMerge}` and `title={isMerge ? "Diff comments aren't available on merge commits" : ""}` (D-04). `isMerge` is passed down from DiffPanel (derived from commitDetail.parent_oids). File-status side constraints (L-04) must NOT set disabled — only the merge gate disables (D-04). In SplitView.svelte add the equivalent commit branch in split-hunk-header (after the staged branch ~271-289) reusing `.staging-btn` classes, mounted in the right/new column context. In DiffPanel.svelte: add composer host state (which hunk's composer is open, the draft text), pass `commitOid`/`isMerge`/selection into the affordance + CommentComposer, and gate selection-switch on the composer's `confirmDiscardIfDirty()` (D-02) — when a new range is selected while the open composer has dirty text, run the confirm before replacing the selection; on cancel, keep the current selection/composer. Wire the composer's submit-clear to `clearSelection()`. Leave staging branches untouched so staging buttons stay absent in commit diffs.
  </action>
  <verify>
    <automated>just vitest src/components/DiffPanel.test.ts src/components/diff/CommentComposer.test.ts && just svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "diffKind === 'commit'" src/components/diff/HunkView.svelte` returns 1 (the Comment branch) and the staging branches at 172/254 are unchanged (no commit case added there).
    - Source assertion: `grep -c "Diff comments aren't available on merge commits" src/components/diff/HunkView.svelte src/components/DiffPanel.svelte` >= 1 (D-04 tooltip).
    - Component test: merge-commit fixture (commitDetail.parent_oids.length === 2) renders the Comment button disabled with the tooltip; non-merge fixture renders it enabled [maps to 67-VALIDATION.md merge-commit-disabled manual row, asserted at component level here].
    - Component test: an Added-file fixture (forces side) renders the affordance ENABLED (L-04 is not a blocked case — D-04).
    - Component test: selecting a new range with a dirty composer calls `confirmDiscardIfDirty`/`ask`; cancel keeps the selection (D-02).
    - `just svelte-check` clean; `just check` green before commit.
  </acceptance_criteria>
  <done>
    The Comment affordance renders on commit-diff selections in both hunk and split layouts, is disabled-with-tooltip only on merge commits (file-status constraints keep it enabled), the composer is hosted in DiffPanel with D-02 confirm on selection switch, and staging buttons remain absent. svelte-check + just check green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of the attach flow (UAT)</name>
  <files>(none — manual verification only)</files>
  <action>
    PAUSE for human verification. This task performs no automated change — Tasks 1-2 already built and unit/component-tested the feature. The developer manually exercises the attach flow per the how-to-verify steps and confirms the manual-only rows in 67-VALIDATION.md (attach UX feel + N-M preview correctness, anchor metadata persists after whitespace toggle + app restart, merge-commit disabled affordance + tooltip). Do not advance the phase until the human responds with the resume-signal.
  </action>
  <what-built>
    Inline comment composer on commit-diff line selections: select a range in a commit's diff (hunk or split), click Comment, type, submit — the comment persists as a source-line anchor via add_comment. Draft persists on keystroke. Merge commits show a disabled Comment affordance with tooltip. Mixed Add/Delete selections attach on the new side.
  </what-built>
  <how-to-verify>
    1. `just dev`, open a repo, select a non-merge commit, open its diff.
    2. Select a contiguous line range in a hunk -> a "Comment" affordance appears -> click it -> type text -> confirm the "Comments on lines N-M" preview matches your selection (D-03) -> Submit. Composer + selection clear.
    3. Select a NEW range, type, then (without submitting) click a different range -> confirm the "Discard your unsaved comment?" prompt appears (D-02); cancel keeps your text; with an empty draft, switching is silent.
    4. Confirm the Submit button is disabled while the textarea is empty.
    5. In split view, select on the right panel and attach a comment (always new-side).
    6. Select a range spanning added AND deleted lines (hunk view) -> attach without error (records new side; SC-3).
    7. Toggle ignore-whitespace, then restart the app -> the comment metadata persists (full re-resolution display is Phase 70; here just confirm it didn't vanish; SC-2).
    8. Open a MERGE commit's diff -> the Comment affordance is disabled with tooltip "Diff comments aren't available on merge commits" (D-04 / L-07).
  </how-to-verify>
  <verify>
    <human-check>Developer confirms steps 1-8 pass and types the resume-signal.</human-check>
  </verify>
  <done>Human typed "approved" (or issues were filed and resolved).</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user textarea -> Tauri command | User-typed comment text + the adapter-produced anchor cross from the composer into add_comment/save_draft_comment (persistence surface owned by Plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-04 | Tampering / XSS | CommentComposer rendering user text | mitigate | Composer renders text only via the textarea / text nodes — never innerHTML. Markdown rendering (and its escaping) is Phase 70 (out of scope here). Locked by a source assertion that innerHTML count is 0. |
| T-67-05 | Tampering | comment text/anchor sent to add_comment | accept (delegated) | Persistence-side mitigation is owned by Plan 02 (atomic store, FNV-1a path keying, schema_version refusal). The composer adds no new persistence path; it calls the Plan 02 commands via safeInvoke. |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH §Package Legitimacy Audit — none installed). |
</threat_model>

<verification>
- `just vitest src/components/diff/CommentComposer.test.ts src/components/DiffPanel.test.ts` green (SC-1 component, D-02/D-03/D-04, empty-disable, split-never-Old).
- `just svelte-check` clean.
- `just check` green before the phase gate.
- Human UAT (Task 3) covers the manual-only rows in 67-VALIDATION.md (attach UX feel, anchor-persists-after-restart, merge-commit disabled affordance).
</verification>

<success_criteria>
- SC-1: user selects a line range in a commit diff and attaches a comment (end-to-end).
- SC-2: comment metadata persists across whitespace toggle + restart (anchor source-coords unchanged; render re-resolution is Phase 70).
- SC-3: a selection spanning added + deleted lines attaches without error and records side=New.
</success_criteria>

<output>
Create `.planning/phases/67-diff-source-anchor-capture/67-04-SUMMARY.md` when done.
</output>
