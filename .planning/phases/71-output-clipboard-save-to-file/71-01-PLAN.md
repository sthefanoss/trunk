---
phase: 71-output-clipboard-save-to-file
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/ReviewDocPreview.svelte
  - src/components/ReviewDocPreview.test.ts
autonomous: true
requirements: [OUT-01]
tags: [tauri, clipboard, svelte, ui-affordance]

must_haves:
  truths:
    - "Clicking Copy invokes writeText with the markdown prop verbatim"
    - "Successful copy swaps the button to '✓ Copied' for ~1.5s, then reverts"
    - "Re-clicking Copy during the affordance window extends the window (timer cleared, not duplicated)"
    - "Failed clipboard write surfaces an error toast 'Failed to copy: <reason>' (kind=error) and does NOT flip the success affordance"
    - "Non-Error rejection values are coerced via String(e) — never an empty/'[object Object]' tail"
    - "← Back to comments button continues to invoke onBack (no regression from the new button placement)"
  artifacts:
    - path: "src/components/ReviewDocPreview.svelte"
      provides: "Copy button + onCopy handler + copied state + .copy-button style block"
      contains: "class=\"copy-button\""
    - path: "src/components/ReviewDocPreview.test.ts"
      provides: "Eight unit tests covering the onCopy handler — per 71-VALIDATION.md Per-Task Verification Map"
      exports: []
  key_links:
    - from: "src/components/ReviewDocPreview.svelte"
      to: "@tauri-apps/plugin-clipboard-manager"
      via: "static import + await writeText(markdown) in onCopy handler"
      pattern: "writeText\\(markdown\\)"
    - from: "src/components/ReviewDocPreview.svelte"
      to: "src/lib/toast.svelte.ts"
      via: "showToast call in onCopy catch block"
      pattern: "showToast\\([^)]*\"error\"\\)"
    - from: "src/components/ReviewDocPreview.svelte (header)"
      to: ".copy-button (rendered after .preview-spacer)"
      via: "flex docking — no positioning hacks"
      pattern: "preview-spacer.*copy-button"
---

<objective>
Add an explicit-feedback Copy button to the existing `ReviewDocPreview.svelte` preview header that writes the rendered review markdown to the system clipboard via the already-installed `@tauri-apps/plugin-clipboard-manager`. Success flips the button label/icon to `✓ Copied` for ~1.5s; failure surfaces an error toast with the underlying reason. Implements OUT-01 (D-01..D-08) without adding any new Tauri capability, IPC command, dependency, or backend code.

Purpose: The rendered review markdown IS the user-facing artifact of v0.13. The three existing `writeText(...).catch(() => {})` fire-and-forget callsites elsewhere in the app intentionally tolerate silent failure (they copy short SHAs); this phase intentionally diverges — silent clipboard failure on the artifact is unacceptable per CONTEXT.md.
Output: Modified `ReviewDocPreview.svelte` (handler + state + template + style block) + new `ReviewDocPreview.test.ts` (8 unit tests).
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/71-output-clipboard-save-to-file/71-CONTEXT.md
@.planning/phases/71-output-clipboard-save-to-file/71-RESEARCH.md
@.planning/phases/71-output-clipboard-save-to-file/71-PATTERNS.md
@.planning/phases/71-output-clipboard-save-to-file/71-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from on-disk reads. -->

From src/components/ReviewDocPreview.svelte (current, lines 10-15) — existing Props contract; do NOT change:
  interface Props { markdown: string; onBack: () => void; }
  let { markdown, onBack }: Props = $props();

From src/lib/toast.svelte.ts (lines 24-32) — showToast signature; failure path consumer:
  export function showToast(message: string, kind: ToastKind = "success", ms = 3000): void;
  // ToastKind = "success" | "error"

From @tauri-apps/plugin-clipboard-manager (installed v2.3.2, dist-js/index.d.ts):
  export function writeText(text: string, opts?: { label?: string }): Promise<void>;

From @lucide/svelte (installed ^0.577.0) — named-export Svelte icon components:
  // Default: Clipboard (icon-only). Acceptable alternative: ClipboardCopy.
  // [ASSUMED — A1 in 71-RESEARCH.md] Verify with: node -e 'const m=require("@lucide/svelte");console.log(["Clipboard","ClipboardCopy"].filter(n=>n in m))'
  // Fallback if neither resolves: import Files instead (also Lucide).

From src/components/ReviewDocPreview.svelte (current, lines 19-29) — header markup and dock cell:
  // <header class="preview-header"> already display:flex (lines 40-48)
  //   <button class="back-button" onclick={onBack}>← Back to comments</button>
  //   <span class="preview-spacer"></span>  ← dock point; the new <button class="copy-button"> goes immediately after this span
  // </header>

From src/components/ReviewDocPreview.svelte (current, lines 50-64) — .back-button rule; .copy-button must mirror these colors/border/hover and ADD `display: inline-flex; align-items: center; gap: 4px;` for the icon-row layout.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 (W0 — RED scaffold): Create ReviewDocPreview.test.ts with boundary mocks and lifecycle hooks</name>
  <files>src/components/ReviewDocPreview.test.ts</files>
  <read_first>
    - src/components/ReviewDocPreview.svelte (whole file, ~81 lines — confirm current Props shape and that no Copy button exists yet)
    - src/components/ReviewPanel.test.ts (lines 1-30 for imports + vi.mock pattern; lines 115-118 for the existing flush helper that we are NOT copying verbatim)
    - src/components/CommitGraph.test.ts (lines 1-80 — exact `vi.mock("@tauri-apps/plugin-clipboard-manager", () => ({ writeText: vi.fn().mockResolvedValue(undefined) }))` pattern)
    - .planning/phases/71-output-clipboard-save-to-file/71-VALIDATION.md (the Per-Task Verification Map — 8 -t selectors are the test names this file will eventually contain)
    - .planning/phases/71-output-clipboard-save-to-file/71-RESEARCH.md (§ Validation Architecture > Recommended mock skeleton — verbatim source for this scaffold; Pitfall 5 — fake timers; Pitfall 4 — static import)
  </read_first>
  <action>
    Create `src/components/ReviewDocPreview.test.ts` with ONLY the mock skeleton — no `it` blocks yet. Wave 1 (RED→GREEN cycles) adds the eight `it` blocks one at a time. This task is the RED-step vehicle.

    Required contents (no fenced code in this action — see the analog files for the exact code shape; copy verbatim, then adapt the component import and the renderPreview helper to ReviewDocPreview's Props { markdown, onBack }):

    1. Top-of-file imports (mirror 71-PATTERNS.md "Combined skeleton"): `{ fireEvent, render, screen }` from `@testing-library/svelte`; `{ tick }` from `svelte`; `{ afterEach, beforeEach, describe, expect, it, vi }` from `vitest`; `{ writeText }` from `@tauri-apps/plugin-clipboard-manager`; `{ showToast }` from `../lib/toast.svelte.js`; `ReviewDocPreview` from `./ReviewDocPreview.svelte`.

    2. Two `vi.mock` calls at module top level (must be hoisted by vitest):
       - `vi.mock("@tauri-apps/plugin-clipboard-manager", ...)` — exposes `writeText: vi.fn().mockResolvedValue(undefined)`. Source: `CommitGraph.test.ts:56-58`.
       - `vi.mock("../lib/toast.svelte.js", ...)` — exposes `showToast: vi.fn()`. Source: `ReviewPanel.test.ts:24-26`.

    3. `beforeEach`: `vi.clearAllMocks(); vi.useFakeTimers();`
    4. `afterEach`: `vi.useRealTimers();`

    5. A single `describe("ReviewDocPreview", () => { /* it blocks added in Wave 1 */ })` shell — empty body except for an inline comment explaining the W1 RED→GREEN plan.

    6. A local `renderPreview(markdown, onBack = vi.fn())` helper near the top of the describe that calls `render(ReviewDocPreview, { props: { markdown, onBack } })` and returns the render result. Wave 1 tests will reach for this helper.

    CRITICAL — do NOT copy `ReviewPanel.test.ts`'s `flush()` helper verbatim. That helper uses `setTimeout(r, 0)` which deadlocks under `vi.useFakeTimers()`. Wave 1 tests will flush microtasks with `await tick()` or `await Promise.resolve()` directly — no helper needed.

    CRITICAL — `@lucide/svelte` is imported by the *component* file, not this test file. This file does not mock Lucide; the real Lucide SVG renders fine in jsdom.

    Do NOT add any `it` blocks in this task. The acceptance criterion is "file exists, mocks declared, vitest can run the file without TypeError" — Wave 1 fills in the behavior tests one-at-a-time as part of the TDD task.
  </action>
  <verify>
    <automated>test -f src/components/ReviewDocPreview.test.ts && npx vitest run src/components/ReviewDocPreview.test.ts 2>&1 | grep -E "(no tests|0 passed|PASS|FAIL)" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/ReviewDocPreview.test.ts` exists.
    - File contains the string `vi.mock("@tauri-apps/plugin-clipboard-manager"` and the string `vi.mock("../lib/toast.svelte.js"`.
    - File contains the string `vi.useFakeTimers()` inside a `beforeEach` and `vi.useRealTimers()` inside an `afterEach`.
    - File contains `describe("ReviewDocPreview"`.
    - `npx vitest run src/components/ReviewDocPreview.test.ts` exits without TypeError / SyntaxError (vitest may report "no tests found" — that's the expected RED-baseline state for W0).
    - File contains a `renderPreview(...)` helper that calls `render(ReviewDocPreview, { props: { markdown, onBack } })`.
    - File does NOT yet contain any `it(` blocks (those are added one-by-one in Wave 1).
    - File does NOT contain the `setTimeout(r, 0)` flush helper pattern copied from ReviewPanel.test.ts.
  </acceptance_criteria>
  <done>Scaffold file committed; vitest can load it; zero behavior tests yet (intentional — Wave 1 adds them as RED→GREEN cycles).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (W1 — TDD GREEN+REFACTOR): Implement Copy button (handler + state + template + style) against 8 RED tests</name>
  <files>src/components/ReviewDocPreview.svelte, src/components/ReviewDocPreview.test.ts</files>
  <read_first>
    - src/components/ReviewDocPreview.svelte (current state — particularly lines 1-16 script block, lines 19-29 header markup, lines 50-64 .back-button style rule which is the visual template)
    - src/components/ReviewDocPreview.test.ts (the W0 scaffold from Task 1 — add `it` blocks here)
    - src/components/CommitDetail.svelte (line 3 — the static `import { writeText } from "@tauri-apps/plugin-clipboard-manager";` form to copy; this is the form to use, NOT App.svelte:133's dynamic `await import(...)`)
    - src/components/ReviewPanel.svelte (lines 278-287 — `onGenerateClick` is the analog `try/catch + showToast("error")` pattern; NOTE divergence: this phase narrows `e instanceof Error` rather than casting `as TrunkError`. Lines 360-368 — `.generate-button` markup for the icon+label layout. Lines 742-760 — `.generate-button` CSS for the `display: inline-flex; align-items: center; gap: 4px;` icon-row helpers.)
    - .planning/phases/71-output-clipboard-save-to-file/71-PATTERNS.md (all of it — the verbatim shapes to mirror)
    - .planning/phases/71-output-clipboard-save-to-file/71-RESEARCH.md (Pitfalls 1, 2, 3, 4, 5 — every one of them is load-bearing for at least one of the 8 tests)
    - .planning/phases/71-output-clipboard-save-to-file/71-VALIDATION.md (Per-Task Verification Map rows 71-01-01..71-01-08 — the eight test names below match these exactly)
    - .planning/phases/71-output-clipboard-save-to-file/71-CONTEXT.md (D-01..D-08 — locked decisions; D-04 keeps the button always clickable; D-08 means no empty-markdown defensive branch; D-07 means no app-level keyboard binding)
    - CLAUDE.md (project rules: never inline colors, never fight layout with positioning hacks)
    - ~/.agents/rules/coding_style_typescript.md (§1 — no `as` casts; narrow with `instanceof`)
  </read_first>
  <behavior>
    The eight `it` blocks below are added to `ReviewDocPreview.test.ts` (Task 1 scaffold) in this order. Each one starts RED, then the implementation step in `ReviewDocPreview.svelte` makes it GREEN. After all eight are GREEN, perform one REFACTOR pass for clarity (no behavior change). Test names MUST match exactly so the `-t` selectors in 71-VALIDATION.md keep working.

    1. **"writes markdown prop"** (test ID 71-01-01) — render with `markdown="hello world"`; click the Copy button; flush microtasks (`await Promise.resolve()` and/or `await tick()`); assert `vi.mocked(writeText)` was called exactly once with the single argument `"hello world"` (verbatim — not a transformed string, not the rendered `<pre>` text content).

    2. **"shows Copied affordance on success"** (71-01-02) — render with any markdown; click Copy; await microtasks; assert the button's rendered text now contains "Copied" (and does NOT contain "Copy" alone — disambiguate via `getByRole("button", { name: /Copied/ })` or a class-based query). Before the click, asserts the button text contains "Copy".

    3. **"reverts after timeout"** (71-01-03) — render; click Copy; await microtasks (assert "Copied" visible); `vi.advanceTimersByTime(1500)`; await `tick()`; assert the button text reverts to "Copy" (no "Copied").

    4. **"remains clickable during window"** (71-01-04, D-04) — render; click Copy at virtual-t=0; await microtasks; `vi.advanceTimersByTime(500)` (now at t=500, mid-window); click Copy again; await microtasks; `vi.advanceTimersByTime(1499)` (now at t=1999 — if the FIRST timer had survived, it would have fired at t=1500); assert button still shows "Copied"; `vi.advanceTimersByTime(1)` (now t=2000 — second timer fires); await `tick()`; assert button shows "Copy". This test forces `clearTimeout` of the prior timer before scheduling a new one (RESEARCH Pitfall 2).

    5. **"shows error toast on failure"** (71-01-05) — `vi.mocked(writeText).mockRejectedValueOnce(new Error("plugin disabled"))`; render; click Copy; await microtasks; assert `vi.mocked(showToast)` was called with `("Failed to copy: plugin disabled", "error")`.

    6. **"does not flip copied on failure"** (71-01-06) — same setup as 5; click; await; assert the button text is still "Copy" (NOT "Copied"). The success affordance must never appear on a failed write.

    7. **"coerces non-Error rejection"** (71-01-07) — `vi.mocked(writeText).mockRejectedValueOnce("raw string")` (non-Error value); render; click; await; assert `vi.mocked(showToast)` was called with `("Failed to copy: raw string", "error")`. This locks in the `String(e)` fallback (RESEARCH Pitfall 1 + 3).

    8. **"back button still invokes onBack"** (71-01-08) — pass an `onBack = vi.fn()`; render; click the `← Back to comments` button (query by accessible name `/Back to comments/`); assert `onBack` called exactly once. This is the regression guard that the new button hasn't displaced the existing one.
  </behavior>
  <action>
    Execute as TDD: for each of the 8 `it` blocks above, write the test first (RED), then make the minimal change in `ReviewDocPreview.svelte` to turn it GREEN. Commit per RED→GREEN cycle if practical, or a single commit per test pair — whichever flows. After all 8 are GREEN, do a single REFACTOR pass (no behavior change).

    Implementation in `ReviewDocPreview.svelte` (additive — do not modify any existing lines except adding inside the `<script>`, inside the header markup after `.preview-spacer`, and at the end of the `<style>` block):

    A. **Script-block additions** (after the existing `let { markdown, onBack }: Props = $props();` line):
       - Static import (top of `<script>`): `import { writeText } from "@tauri-apps/plugin-clipboard-manager";` (form from CommitDetail.svelte:3 — NOT dynamic per RESEARCH Pitfall 4).
       - Static import: `import { Clipboard } from "@lucide/svelte";` (Discretion: `ClipboardCopy` acceptable; if neither named export exists per the A1 verification check, fall back to `Files` and note the substitution in the SUMMARY).
       - Static import: `import { showToast } from "../lib/toast.svelte.js";`.
       - `import { tick } from "svelte";` is NOT needed in the component — only the test file uses it.
       - State: `let copied = $state(false);`
       - Timer handle (NOT `$state` — it's not reactive, only a handle): `let copyTimer: ReturnType<typeof setTimeout> | null = null;`
       - Handler `async function onCopy()` with this exact behavior:
         (a) `try` block: `await writeText(markdown)`; THEN `if (copyTimer !== null) clearTimeout(copyTimer);` (Pitfall 2 — required by test 71-01-04); THEN `copied = true`; THEN `copyTimer = setTimeout(() => { copied = false; copyTimer = null; }, 1500);`.
         (b) `catch (e)`: `const msg = e instanceof Error ? e.message : String(e);` (NEVER `as Error`, NEVER `(e as TrunkError).message` — coding_style_typescript.md §1 + Pitfall 3); `showToast(\`Failed to copy: ${msg}\`, "error");`. Do NOT flip `copied` in the catch path (test 71-01-06).

    B. **Template addition** (inside `<header class="preview-header">`, immediately AFTER `<span class="preview-spacer"></span>` on line 26 — flex docks it to the right per CLAUDE.md "Never fight layout with positioning hacks"):
       - `<button type="button" class="copy-button" onclick={onCopy}>` with an `{#if copied}` / `{:else}` split: success branch shows a literal `<span aria-hidden="true">✓</span><span>Copied</span>`; default branch shows `<Clipboard size={14} /><span>Copy</span>`. Pattern source: 71-PATTERNS.md §"Button markup pattern". Do NOT add a `disabled` attribute (D-04). Do NOT add a `title` (D-08 means there's no empty-doc gating).

    C. **Style-block addition** (at the end of the existing `<style>` block, after the `.preview-body` rule):
       - `.copy-button { display: inline-flex; align-items: center; gap: 4px; background: transparent; color: var(--color-text-muted); border: 1px solid var(--color-border); border-radius: 4px; cursor: pointer; padding: 2px 8px; font-size: 12px; font-family: inherit; }`
       - `.copy-button:hover, .copy-button:focus-visible { color: var(--color-text); background: var(--color-hover); }`
       - All colors via theme tokens — `var(--color-*)`, CLAUDE.md rule "Never inline colors". The three flex helpers (inline-flex/align-items/gap) are the only divergence from the `.back-button` analog at lines 50-64; everything else is identical aside from the class name.

    Important test-side rules (Wave 1 author MUST follow):
    - Under `vi.useFakeTimers()` from `beforeEach`, do NOT call `await new Promise(r => setTimeout(r, 0))` — that pending setTimeout will never resolve. Flush microtasks with `await Promise.resolve()` and/or `await tick()` only. (See RESEARCH Pitfall 5 + the explicit note in 71-PATTERNS.md line ~230.)
    - For DOM assertions after a click, the sequence is: `await fireEvent.click(button); await Promise.resolve(); await tick();` — that flushes both the awaited `writeText` mock's resolved microtask and Svelte's reactive update.
    - For the icon-rendered button text, use `getByRole("button", { name: /Copy/ })` then narrow via accessible name — Lucide SVGs render inline and don't add text content, so `screen.getByText("Copy")` may match the body `<pre>` if the markdown happens to contain "Copy"; use `name` regex on the button instead.

    Lucide export verification (Discretion A1 mitigation, run BEFORE adding the import — one-liner, output only — do not edit anything based on it without explicit fallback noted in SUMMARY):
    `node -e 'const m = require("@lucide/svelte"); console.log(["Clipboard", "ClipboardCopy"].filter(n => n in m));'`
    Expected: at least one of `Clipboard`/`ClipboardCopy` printed. If empty array, import `Files` instead and record the substitution in SUMMARY.md.

    Out of scope for this task (do NOT add): defensive empty-markdown branch (D-08); success toast (D-03 forbids it); app-level keybinding for Cmd/Ctrl+C (D-07); modifying any file other than these two; touching `src-tauri/`; adding any dependency.
  </action>
  <verify>
    <automated>npx vitest run src/components/ReviewDocPreview.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 8 named tests in `ReviewDocPreview.test.ts` exist and pass: `npx vitest run src/components/ReviewDocPreview.test.ts` exits 0 with `8 passed` (or higher if executor adds extra cases).
    - Each of these 8 -t selectors locates exactly one passing test (matches 71-VALIDATION.md):
      - `npx vitest run src/components/ReviewDocPreview.test.ts -t "writes markdown prop"` exits 0
      - `npx vitest run src/components/ReviewDocPreview.test.ts -t "shows Copied affordance on success"` exits 0
      - `npx vitest run src/components/ReviewDocPreview.test.ts -t "reverts after timeout"` exits 0
      - `npx vitest run src/components/ReviewDocPreview.test.ts -t "remains clickable during window"` exits 0
      - `npx vitest run src/components/ReviewDocPreview.test.ts -t "shows error toast on failure"` exits 0
      - `npx vitest run src/components/ReviewDocPreview.test.ts -t "does not flip copied on failure"` exits 0
      - `npx vitest run src/components/ReviewDocPreview.test.ts -t "coerces non-Error rejection"` exits 0
      - `npx vitest run src/components/ReviewDocPreview.test.ts -t "back button still invokes onBack"` exits 0
    - `src/components/ReviewDocPreview.svelte` contains the string `class="copy-button"` (exactly that, in the markup).
    - `src/components/ReviewDocPreview.svelte` contains a static `import { writeText } from "@tauri-apps/plugin-clipboard-manager";` line (NOT `await import(...)`). `grep -n 'await import("@tauri-apps/plugin-clipboard-manager")' src/components/ReviewDocPreview.svelte` returns no matches.
    - `src/components/ReviewDocPreview.svelte` contains `e instanceof Error ? e.message : String(e)` (or equivalent narrowing) and contains NEITHER `as Error` NOR `as TrunkError` casts: `grep -E 'as (Error|TrunkError)' src/components/ReviewDocPreview.svelte` returns no matches.
    - `src/components/ReviewDocPreview.svelte` contains a `clearTimeout(copyTimer)` (or equivalent guarded clear) call BEFORE `setTimeout(...)` in `onCopy` (test 71-01-04 enforces this empirically; this is a static cross-check).
    - The `.copy-button` style rule uses only `var(--color-*)` tokens — `grep -nE '#[0-9a-fA-F]{3,8}|rgb\(' src/components/ReviewDocPreview.svelte | grep -v '^--' | wc -l` returns 0 (no inline hex/rgb colors in the file). The pre-existing `.back-button` analog already conforms, so any non-zero count is a regression in the new rule.
    - The new `.copy-button` is rendered AFTER `<span class="preview-spacer"></span>` in the header (flex docking): `grep -n 'preview-spacer\|copy-button' src/components/ReviewDocPreview.svelte` shows `preview-spacer` line < `copy-button` line.
    - `npx svelte-check --tsconfig ./tsconfig.json src/components/ReviewDocPreview.svelte` 2>&1 reports 0 errors / 0 warnings for this file.
  </acceptance_criteria>
  <done>All 8 tests pass; component additions land; svelte-check is clean for the modified file; ready for the phase gate.</done>
</task>

<task type="auto">
  <name>Task 3 (Phase Gate): Run `just check` and confirm full pipeline green</name>
  <files></files>
  <read_first>
    - CLAUDE.md ("Run `just check` before every commit and push")
    - .planning/phases/71-output-clipboard-save-to-file/71-VALIDATION.md (§ Sampling Rate — `just check` is the phase gate)
  </read_first>
  <action>
    Run `just check` from the repo root. This runs fmt, biome, svelte-check, clippy, cargo-test, and vitest (full suites — not just the new test file).

    If anything that is NOT in `src/components/ReviewDocPreview.{svelte,test.ts}` is red, follow ownership.md: do not declare done. Fix the issue (or document it explicitly as a pre-existing defer with a tracked TODO) before signing off. Specifically:
    - Pre-existing svelte-check warnings in other files: do NOT silence; either fix in a separate commit or surface in the SUMMARY with an explicit defer note.
    - Biome / formatter complaints introduced by Task 2's additions: auto-fix with `just fmt` (or the project's configured formatter) and re-run.
    - Cargo / clippy red: surprising for a frontend-only phase; if it appears, it's pre-existing and ownership.md applies — investigate before declaring done.

    Do NOT commit anything new in this task — the commits land alongside Tasks 1 and 2. This task is the verification gate only.
  </action>
  <verify>
    <automated>just check 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `just check` exits 0 (all of fmt, biome, svelte-check, clippy, cargo-test, vitest pass).
    - The new file `src/components/ReviewDocPreview.test.ts` is included in the vitest run (file listed in vitest's report; 8 tests reported as passed).
    - No new svelte-check warnings or biome diagnostics are introduced by the additions in `ReviewDocPreview.svelte` (diff against `main`: zero net new diagnostics on the modified file).
  </acceptance_criteria>
  <done>`just check` is green; phase 71 is ready for `/gsd:verify-work`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component prop → clipboard plugin | The `markdown: string` prop (already in-memory, already rendered to a `<pre>` in this same component) is passed verbatim to `writeText`. |
| Clipboard plugin → OS clipboard | The already-permitted `clipboard-manager:allow-write-text` capability (granted in Phase 65; verified in `src-tauri/capabilities/default.json:14`) mediates this hop. No change in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-S | Spoofing | — | N/A | No new identity, no new auth, no new principal. Zero attack surface added. |
| T-71-T | Tampering | — | N/A | No new IPC command, no new persisted state, no new file write. The `markdown` prop is the same string already rendered to the `<pre>` in this component; copying anything else is structurally impossible because both the `<pre>` and `writeText` read the same prop. |
| T-71-R | Repudiation | — | N/A | No new logging, no new audit trail. Existing toast infra is the only failure surface. |
| T-71-I | Information Disclosure | error toast text | accept | The error toast surfaces the underlying plugin error message via `e.message` / `String(e)`. Tauri clipboard-manager error messages are non-sensitive by design (per Assumption A2 in 71-RESEARCH.md). Empirically the message originates inside `@tauri-apps/plugin-clipboard-manager` and contains permission/plugin-state strings, not user PII or filesystem paths. Risk: LOW — the user types the markdown themselves; nothing in this path touches an external secret store, env var, or filesystem read. |
| T-71-D | Denial of Service | — | N/A | Rapid re-clicks are explicitly designed-for (D-04 + Pitfall 2). The `setTimeout` is bounded to 1500ms with `clearTimeout` on each new click, so timer state cannot grow without bound. No external resource consumed beyond a single clipboard write per click. |
| T-71-E | Elevation of Privilege | — | N/A | No new Tauri capability requested. The `clipboard-manager:allow-write-text` permission already exists from Phase 65 and is unchanged. No new IPC command. No new shell-out. No `dangerousRemoteUrlIpcAccess` change. |
| T-71-SC | Supply Chain | npm/pip/cargo installs | N/A | Zero new packages. All required libraries (`@tauri-apps/plugin-clipboard-manager@2.3.2`, `@lucide/svelte@^0.577.0`) are already in `package.json` and have been used by prior phases (Phase 65 onward for the Tauri plugin; Phase 69 for `@lucide/svelte` in `ReviewPanel.svelte`). slopcheck protocol does not apply (71-RESEARCH.md § Package Legitimacy Audit). |

**Honest summary:** This phase adds zero new attack surface — no new IPC, no new capability, no new network call, no new file write, no new dependency, no eval / innerHTML / dangerouslySetInnerHTML. The only data flow is `in-memory string prop → already-permitted plugin → OS clipboard`. The error toast information-disclosure category (T-71-I) is the only one with any substantive content, and even there the risk is LOW because the plugin error messages are bounded by Tauri conventions and contain no user secrets. No HIGH-severity threats; ASVS L1 gate does not block.
</threat_model>

<verification>
- All 8 named tests in `ReviewDocPreview.test.ts` pass under `npx vitest run src/components/ReviewDocPreview.test.ts`.
- `just check` is green end-to-end (fmt + biome + svelte-check + clippy + cargo-test + vitest).
- `grep -E 'as (Error|TrunkError)' src/components/ReviewDocPreview.svelte` returns no matches (no banned casts).
- `grep -nE '#[0-9a-fA-F]{3,8}|rgb\(' src/components/ReviewDocPreview.svelte` returns no matches in the new `.copy-button` block (no inlined colors per CLAUDE.md).
- The threat model accurately reflects zero-new-attack-surface (T-71-S/T/R/D/E are N/A; T-71-I is accept-LOW; T-71-SC is N/A).

**Manual-only verification** (out of scope for this plan's automated gate — handled by `/gsd:verify-work` per 71-VALIDATION.md § Manual-Only Verifications):
1. Real OS clipboard receives the markdown after a click in `just dev`.
2. Visual styling parity with `← Back to comments` (border, padding, hover, font-size).
3. "Copied" affordance duration feels right (~1.5s; Discretion to tune).
4. Failure path surfaces a readable toast (force by temporarily revoking `clipboard-manager:allow-write-text`; restore before commit).
5. Rapid double-click test: no flicker, no console errors.
</verification>

<success_criteria>
- **OUT-01 (Pending → Complete):** User can copy the generated markdown to the clipboard with explicit success/failure feedback. Verified by 8 unit tests + manual smoke in `just dev`.
- All 8 tests in 71-VALIDATION.md § Per-Task Verification Map pass with the exact `-t` selectors.
- `just check` green.
- No new svelte-check / biome diagnostics introduced.
- No new Tauri capability, IPC command, dependency, or backend code added (verified by `git diff --stat src-tauri/ package.json package-lock.json` showing no changes).
- The descope (OUT-02 dropped) is reflected: this plan does NOT touch save-to-file, file dialog, or `dialog:allow-save` capability.
</success_criteria>

<output>
Create `.planning/phases/71-output-clipboard-save-to-file/71-01-SUMMARY.md` when done. Record:
- Lucide icon used (`Clipboard` vs `ClipboardCopy` vs fallback `Files` — per A1 verification result).
- Final "Copied" duration if tuned away from 1500ms.
- Any pre-existing diagnostics surfaced by `just check` that were deferred (per ownership.md, with concrete TODO).
- Confirmation that no `src-tauri/` files were touched.
</output>
