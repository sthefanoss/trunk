---
phase: 65-data-model-persistence-session-lifecycle
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/git/types.rs
  - src/lib/types.ts
  - src-tauri/tests/test_integ_serde.rs
autonomous: true
requirements: [SESS-01, SESS-02, SESS-03]

must_haves:
  truths:
    - "D-05: The full review-session data model (session + comment + anchor + draft + enums) exists in Rust and serializes/deserializes round-trip — the keystone schema is defined now even though capture lands in 67/68."
    - "Source and Side enums serialize as PascalCase strings (\"Diff\"/\"FullFile\", \"Old\"/\"New\"), matching the codebase enum convention."
    - "D-02: source discriminates diff-source (Phase 67) from full-file-source (Phase 68) anchors, and side distinguishes old/new for diff-source anchors."
    - "D-01: The Anchor type never carries hunk_index, line_index, context_lines, or ignore_whitespace fields (source coordinates only — never diff-array positions or diff options)."
    - "D-03: The persisted document carries schema_version: 1 from the first commit. D-06: one active session per repo; schema_version leaves the door open for SESS-F1 multi-session but the model does not design for it now."
    - "D-04: Comment text and cached_excerpt are stored independently of anchor resolvability so render (Phase 70) can always surface a comment. D-07: no timestamp/author/severity/status/ordering fields — comment order is implicit capture-order, re-ordered by line at render time."
    - "The TypeScript mirror in types.ts is string-for-string identical to the Rust on-wire shape."
  artifacts:
    - path: "src-tauri/src/git/types.rs"
      provides: "ReviewSession, Comment, Anchor, DraftComment structs + Source, Side enums (Serialize + Deserialize)"
      contains: "pub struct ReviewSession"
    - path: "src/lib/types.ts"
      provides: "TS mirrors: ReviewSession, Comment, Anchor, DraftComment interfaces + Source, Side unions"
      contains: "export interface ReviewSession"
    - path: "src-tauri/tests/test_integ_serde.rs"
      provides: "session_serde_shape test asserting PascalCase enums, snake_case fields, absence of forbidden fields"
      contains: "session_serde_shape"
  key_links:
    - from: "src-tauri/src/git/types.rs"
      to: "src/lib/types.ts"
      via: "string-for-string serde mirror"
      pattern: "schema_version"
---

<objective>
Define the keystone review-session data model — the FULL schema (session + comment + anchor + draft + enums) — in Rust `git/types.rs` and its TypeScript mirror in `src/lib/types.ts`, locked now so later phases (66/67/68/70) write into it without reshaping it.

Purpose: D-05 makes the schema the keystone — getting it wrong is a high-cost migration that gates every later phase. This plan freezes the on-wire and on-disk shape with a serde-shape test that fails if a forbidden field (hunk_index, line_index, diff options) ever appears.
Output: Rust DTOs (Serialize + Deserialize), TS mirrors, and a serde-shape integration test.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-data-model-persistence-session-lifecycle/65-CONTEXT.md
@.planning/phases/65-data-model-persistence-session-lifecycle/65-RESEARCH.md
@.planning/phases/65-data-model-persistence-session-lifecycle/65-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Serde-shape test for the review-session schema (RED)</name>
  <files>src-tauri/tests/test_integ_serde.rs</files>
  <read_first>
    - src-tauri/tests/test_integ_serde.rs (existing serde-shape test idiom — `graph_result_serializes_with_expected_fields`; mirror its `serde_json::to_value` + per-field `assert!` structure)
    - src-tauri/src/git/types.rs (existing enum convention: `RefType`/`DiffStatus` PascalCase variants, no `rename_all`; existing struct field snake_case)
    - .planning/phases/65-data-model-persistence-session-lifecycle/65-RESEARCH.md (Code Examples §"Recommended Rust DTO shapes" lines 377-419 — the exact locked shapes)
  </read_first>
  <behavior>
    - Test `session_serde_shape`: build a `ReviewSession` literal with one anchored `Comment` and assert via `serde_json::to_value`:
      - `json["schema_version"]` equals 1
      - `Source::Diff` serializes to the string `"Diff"` and `Source::FullFile` to `"FullFile"`
      - `Side::Old` serializes to `"Old"` and `Side::New` to `"New"`
      - the anchor object's `commit_oid`, `file_path`, `source`, `side`, `start_line`, `end_line` keys are all present and snake_case
      - forbidden fields are absent: `json[...]["hunk_index"].is_null()`, `["line_index"].is_null()`, `["context_lines"].is_null()`, `["ignore_whitespace"].is_null()`
      - `draft_comment` serializes as `null` when `None`
    - Test `session_round_trips`: `serde_json::to_string` a `ReviewSession`, `serde_json::from_str` it back, assert the deserialized value equals the original (Clone + PartialEq on the comparison path, or field-by-field assert).
  </behavior>
  <action>
    Add two `#[test]` functions to test_integ_serde.rs following the existing `graph_result_serializes_with_expected_fields` idiom. Reference the new types via `trunk_lib::git::types::{ReviewSession, Comment, Anchor, DraftComment, Source, Side}`. Write the tests FIRST against the schema as locked in RESEARCH lines 377-419 — they will not compile until Task 2 defines the types (this is the RED state). Assert PascalCase enum serialization, snake_case struct fields, the schema_version=1 invariant, absence of all four forbidden fields, and a full round-trip. Do NOT weaken the forbidden-field assertions — they are the migration guardrail per D-01.
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml session_serde_shape 2>&1 | grep -qE "cannot find|unresolved import|error\[" && echo "RED: types not yet defined (expected)"</automated>
  </verify>
  <acceptance_criteria>
    - test_integ_serde.rs contains `fn session_serde_shape` and `fn session_round_trips`
    - session_serde_shape asserts `json["..."]["hunk_index"].is_null()` AND `["line_index"].is_null()` AND `["context_lines"].is_null()` AND `["ignore_whitespace"].is_null()`
    - session_serde_shape asserts `Source::Diff` serializes to `"Diff"` (string assertion, not snake_case)
    - Before Task 2: `cargo test --manifest-path src-tauri/Cargo.toml session_serde_shape` FAILS to compile (types undefined) — confirms RED
  </acceptance_criteria>
  <done>Both tests written; compilation fails because the session types do not exist yet (RED confirmed).</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Rust DTOs + TS mirror making the schema tests pass (GREEN)</name>
  <files>src-tauri/src/git/types.rs, src/lib/types.ts</files>
  <read_first>
    - src-tauri/src/git/types.rs (add to this file; mirror `RefType` enum style at lines 25-31 and `DiffStatus` Serialize+Deserialize derive at lines 206-217; struct field snake_case)
    - src/lib/types.ts (mirror `RefType` PascalCase string union at line 10; snake_case interface fields)
    - .planning/phases/65-data-model-persistence-session-lifecycle/65-RESEARCH.md (Code Examples lines 377-434 — Rust shapes AND the TS mirror block, copy field-for-field)
    - src-tauri/tests/test_integ_serde.rs (the tests from Task 1 that must now pass)
  </read_first>
  <behavior>
    - After this task, `session_serde_shape` and `session_round_trips` from Task 1 PASS.
    - `ReviewSession { schema_version: u32, commits: Vec<String>, comments: Vec<Comment>, draft_comment: Option<DraftComment> }` round-trips.
    - Enums serialize PascalCase; structs serialize snake_case fields; all derive Deserialize (read back from disk).
  </behavior>
  <action>
    In `src-tauri/src/git/types.rs` add exactly these types per RESEARCH lines 377-419 (locks D-01..D-07 and DP-02):
    - `Source` enum (variants `Diff`, `FullFile`) and `Side` enum (variants `Old`, `New`) — PascalCase, NO `#[serde(rename_all)]`, derive `Debug, Serialize, Deserialize, Clone, PartialEq` (DP: enums serialize as `"Diff"`/`"FullFile"`/`"Old"`/`"New"` per Pitfall 4 — NEVER apply snake_case rename to enums).
    - `Anchor` struct with snake_case fields `commit_oid: String, file_path: String, source: Source, side: Side, start_line: u32, end_line: u32`. NEVER add hunk_index / line_index / context_lines / ignore_whitespace (D-01).
    - `Comment` struct: `text: String, anchor: Option<Anchor>, cached_excerpt: Option<String>` (D-04: text + cached_excerpt stored independent of anchor resolvability; `anchor: None` = commit-level comment for ANCH-03).
    - `DraftComment` struct: `text: String, anchor: Option<Anchor>` (DP-02 — the in-progress draft lives on the session, NOT component-level, because ROADMAP Phase 67 requires "persist the draft comment on change, not only on submit").
    - `ReviewSession` struct: `schema_version: u32` (D-03: always 1 now), `commits: Vec<String>` (OIDs, Phase 66 fills), `comments: Vec<Comment>` (capture-order per D-07), `draft_comment: Option<DraftComment>` (DP-02, serializes null until Phase 67 wires capture).
    All structs derive `Debug, Serialize, Deserialize, Clone` (Deserialize because sessions are read back from disk — unlike most write-only DTOs in this file). Then add the string-for-string TS mirror to `src/lib/types.ts` per RESEARCH lines 420-434: `Source`/`Side` PascalCase string unions and `Anchor`/`Comment`/`DraftComment`/`ReviewSession` interfaces with snake_case fields and nullable optionals (`Anchor | null`, `string | null`). Define NO `status`/`timestamp`/`author`/`severity` fields anywhere (D-07 — single-user AI-recipient tool; deferred/out-of-scope).
  </action>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml session_serde_shape session_round_trips 2>&1 | tail -5 && bun run check 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --manifest-path src-tauri/Cargo.toml session_serde_shape session_round_trips` passes (GREEN)
    - types.rs contains `pub struct ReviewSession`, `pub struct Comment`, `pub struct Anchor`, `pub struct DraftComment`, `pub enum Source`, `pub enum Side`
    - `grep -v '^//' src-tauri/src/git/types.rs` shows NO `rename_all` attribute on `Source` or `Side`
    - types.ts contains `export interface ReviewSession`, `export type Source = "Diff" | "FullFile"`, `export type Side = "Old" | "New"`
    - `bun run check` (svelte-check / tsc) passes with the new TS interfaces
    - No `hunk_index`, `line_index`, `context_lines`, `ignore_whitespace`, `status`, `timestamp`, `author`, or `severity` field anywhere in the new Rust or TS types
  </acceptance_criteria>
  <done>All schema types defined in Rust and TS; serde-shape and round-trip tests pass; the schema is frozen for downstream phases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-disk JSON → Rust deserialize | A persisted session file (written this phase onward) is read back via `Deserialize`; the shape must be strict so malformed/foreign fields cannot smuggle state. (Recovery handling itself lives in Plan 02.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01 | Tampering | Anchor schema (forbidden fields) | mitigate | Schema deliberately omits hunk_index/line_index/diff-options (D-01); serde-shape test asserts their absence so a future regression that re-introduces a position-based anchor fails CI before it can be persisted. |
| T-65-02 | Information disclosure | Session DTO fields | accept | Session carries only commit OIDs, file paths, and user-authored comment text — no secrets, no PII beyond what the user types (D-07: no author/timestamp). Acceptable for a local single-user tool. |
| T-65-SC | Tampering | npm/cargo installs | mitigate | No new packages added (RESEARCH Package Legitimacy Audit: N/A). No install task in this plan; nothing to gate. |
</threat_model>

<verification>
- `cargo test --manifest-path src-tauri/Cargo.toml session_serde_shape session_round_trips` passes.
- `bun run check` passes (TS mirror compiles).
- Forbidden-field assertions present and passing.
</verification>

<success_criteria>
The full review-session schema (session + comment + anchor + draft + Source/Side enums) exists in Rust and TS, serializes PascalCase enums / snake_case fields, round-trips through serde, and provably excludes hunk_index/line_index/diff-option fields. Downstream phases can write into this schema without reshaping it.
</success_criteria>

<output>
Create `.planning/phases/65-data-model-persistence-session-lifecycle/65-01-SUMMARY.md` when done.
</output>
</output>
