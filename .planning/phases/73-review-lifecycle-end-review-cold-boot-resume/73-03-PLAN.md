---
phase: 73-review-lifecycle-end-review-cold-boot-resume
plan: 03
type: execute
wave: 3
depends_on:
  - 73-01
  - 73-02
files_modified:
  - src/components/ReviewPanel.svelte
  - src/components/ReviewPanel.test.ts
autonomous: true
requirements:
  - REQ-73-EMPTY
  - REQ-73-SUMMARY
  - REQ-73-MULTITAB
  - REQ-73-NYQUIST
  - REQ-73-CHECK
tags:
  - svelte
  - review-session
  - empty-states
  - multi-tab

must_haves:
  truths:
    - "Cold state (sessionState === 'none') renders a NEW empty-state block with copy 'No active review' / 'Toggle review mode in the toolbar to start.' — distinct from the existing 'No commits in this review yet.' branch (D-06)."
    - "Warm-with-commits-zero-comments state renders 'Review started.' / 'Select diff lines or add a commit note to comment.' — replaces the prior 'No comments yet.' copy."
    - "Warm-no-commits state ('groups.length === 0' but sessionState !== 'none') preserves the existing 'No commits in this review yet.' copy verbatim."
    - "A session summary caption {N} comments · {M} commits (U+00B7 middle dot, no pluralization) appears above the comments list whenever sessionState !== 'none'; hidden in the cold branch (D-04)."
    - "When tab A ends a review, the captured session-changed listener callback fires in tab B; tab B's reload sees sessionState === 'none' and renders the cold empty state (D-09, REQ-73-MULTITAB)."
    - "A session-changed event for a different canonical path is filtered out by the existing listener — no reload, no IPC call."
  artifacts:
    - path: "src/components/ReviewPanel.svelte"
      provides: "Three-way empty-state branching gated on sessionState; session-summary caption above the list"
      contains: "No active review"
    - path: "src/components/ReviewPanel.test.ts"
      provides: "describe(\"empty states\") + describe(\"summary line\") + describe(\"multi-tab coordination\") blocks"
      contains: "describe(\"empty states\""
  key_links:
    - from: "src/components/ReviewPanel.svelte:scroll-body region"
      to: "session summary caption span"
      via: "renders directly above {#if groups.length === 0} block, gated on sessionState !== 'none'"
      pattern: "comments\\.length.*·.*commits\\.length"
    - from: "src/components/ReviewPanel.svelte:empty-state branches"
      to: "three mutually-exclusive {#if} branches"
      via: "sessionState === 'none' (cold) | groups.length === 0 (warm-no-commits) | !hasAnyComment (warm-with-commits)"
      pattern: "sessionState === \"none\""
    - from: "Plan 01's captured sessionChangedHandler"
      to: "ReviewPanel.reload() under cold-boot-after-end simulation"
      via: "fireSessionChanged('/repo') → reload sees status.state='none' → cold empty state renders"
      pattern: "sessionChangedHandler"
---

<objective>
Close the remaining three Phase 73 success criteria:

1. **REQ-73-EMPTY** — distinct empty-state copy for cold (`sessionState === "none"`) vs warm-empty (session active, zero comments). Replaces the current single `!hasAnyComment` branch with a three-way gate that uses Plan 01's `sessionState` rune.
2. **REQ-73-SUMMARY** — small muted caption `{N} comments · {M} commits` above the comments list whenever `sessionState !== "none"` (D-04).
3. **REQ-73-MULTITAB** — automated test asserting tab A's `end_review_session` emission (simulated via Plan 01's captured `sessionChangedHandler`) drives tab B into the cold empty state.

No new state shapes; no new IPC; pure template + test changes consuming the runes Plan 01 introduced and the affordance Plan 02 added.

Purpose: completes REQ-73-EMPTY, REQ-73-SUMMARY, REQ-73-MULTITAB, REQ-73-NYQUIST, REQ-73-CHECK.

Output:
- `src/components/ReviewPanel.svelte` template: three-branch empty-state block + session-summary span above the list.
- `src/components/ReviewPanel.test.ts`: three new describe blocks covering empty-state branching (3 tests), summary line (2 tests), and multi-tab coordination (2 tests).
- Updated existing tests that previously asserted on the old "No comments yet." copy (if any) — replace with the new warm-with-commits copy.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joaofnds/code/trunk/.planning/PROJECT.md
@/Users/joaofnds/code/trunk/.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-CONTEXT.md
@/Users/joaofnds/code/trunk/.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-RESEARCH.md
@/Users/joaofnds/code/trunk/.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-PATTERNS.md
@/Users/joaofnds/code/trunk/.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-UI-SPEC.md

@/Users/joaofnds/code/trunk/.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-01-SUMMARY.md
@/Users/joaofnds/code/trunk/.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-02-SUMMARY.md
@/Users/joaofnds/code/trunk/src/components/ReviewPanel.svelte
@/Users/joaofnds/code/trunk/src/components/ReviewPanel.test.ts

<interfaces>
<!-- Runes and helpers Plans 01/02 produced — consume; do NOT redeclare. -->
- `sessionState: $state<SessionState>` (Plan 01) — read for empty-state gating and summary-line visibility
- `groups: $derived(...)` (existing) — `groups.length === 0` means no commits hand-picked into review
- `hasAnyComment: $derived(comments.length > 0)` (existing, line 128) — drives the warm-with-commits-zero-comments branch
- `sessionChangedHandler` + `fireSessionChanged(payload)` (Plan 01 test scaffold) — drives multi-tab simulation

<!-- Branching contract from 73-UI-SPEC.md §"Copywriting Contract" (machine-readable form): -->

sessionState === 'none'                             → "No active review" / "Toggle review mode in the toolbar to start."
sessionState ∈ {'active','resume-available'}
  AND groups.length === 0                           → "No commits in this review yet." / "Add commits from the graph to start reviewing."  (EXISTING — preserved verbatim)
  AND groups.length > 0 AND hasAnyComment === false → "Review started." / "Select diff lines or add a commit note to comment."  (REPLACES the prior "No comments yet." copy)
  AND hasAnyComment === true                        → no empty state; list renders

<!-- Summary line contract from 73-UI-SPEC.md §"Session-summary caption": -->
- visible when sessionState !== 'none'
- single <span> inside the scroll body, ABOVE the {#if groups.length === 0} block
- text: `{comments.length} comments · {commits.length} commits` (middle dot U+00B7, no pluralization)
- style: font-size 11px, color var(--color-text-muted) — matches the existing muted-caption rule at ReviewPanel.svelte line ~537 or the inline rule at :420-422

<!-- Multi-tab test pattern from 73-PATTERNS.md §"Test mocks": fireSessionChanged('/repo') simulates the cross-tab emit. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three-way empty-state branching + summary caption (TDD: 5 tests)</name>
  <files>src/components/ReviewPanel.test.ts, src/components/ReviewPanel.svelte</files>
  <read_first>
    - src/components/ReviewPanel.svelte (lines 404-431 for current empty-state markup; lines 417-431 for the two existing branches; check Plan 02's End button placement in the header rows 376-403)
    - src/components/ReviewPanel.test.ts (post-Plans 01+02 — verify installReads supports status overrides; look for any existing tests that assert on "No comments yet." string and earmark them for update)
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-RESEARCH.md (§"Pattern 3: Empty-state gating — cold vs. warm" and §"Pattern 4: Session summary line")
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-PATTERNS.md (§"Empty-state copy" and §"Session-changed listener — UNTOUCHED")
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-UI-SPEC.md (§"Copywriting Contract" table — exact strings; §"Empty-state regions" — style triplet; §"Session-summary caption" — placement + style)
  </read_first>
  <behavior>
    Five tests across two new describe blocks (peer to existing ones; REAL timers — no fake-timer scoping needed). Use the file-global `flush()` helper.

    `describe("empty states", ...)`:
    - Test 1 ("renders cold empty state when no session"): `installReads({ status: { state: "none", file_exists: false, canonical_path: "/repo" }, commits: [], comments: [], resolutions: [] })` → render → flush → assert `screen.getByText(/No active review/)` and `screen.getByText(/Toggle review mode in the toolbar to start/)` are in the DOM, AND `screen.queryByText(/Review started/)` is null, AND `screen.queryByText(/No comments yet/)` is null.
    - Test 2 ("renders warm-with-commits empty state when session active and zero comments"): `installReads({ status: { state: "active", ... }, commits, comments: [], resolutions: [] })` (note: `commits` is the top-of-file fixture with 2 commits) → render → flush → assert `screen.getByText(/Review started\./)` and `screen.getByText(/Select diff lines or add a commit note/)` are visible, AND cold copy is NOT visible.
    - Test 3 ("renders existing warm-no-commits empty state when session active and zero commits"): `installReads({ status: { state: "active", ... }, commits: [], comments: [], resolutions: [] })` → render → flush → assert `screen.getByText(/No commits in this review yet/)` and `screen.getByText(/Add commits from the graph/)` are visible (existing copy preserved verbatim).

    `describe("summary line", ...)`:
    - Test 4 ("renders session summary line when session active"): `installReads({ status: { state: "active", ... }, commits, comments: [lineAnchoredComment("c1", COMMIT_A, "x"), lineAnchoredComment("c2", COMMIT_A, "y")], resolutions: [resolvable("c1"), resolvable("c2")] })` → render → flush → assert `screen.getByText(/2 comments · 2 commits/)` is in the DOM (exact middle-dot U+00B7 character — use the actual character in the regex, not `\\s*·\\s*`, since the contract is precise). The character is `·` (U+00B7).
    - Test 5 ("no summary line when cold"): `installReads({ status: { state: "none", ... }, commits: [], comments: [], resolutions: [] })` → render → flush → assert `screen.queryByText(/comments · /)` is null (the dot+spaces unique-substring won't appear in the cold copy).

    All tests use the file-global `flush()` (not `flushFake`) and DO NOT use `vi.useFakeTimers()`.

    Audit existing tests: if any prior test in the file asserts on the substring "No comments yet" (the pre-Phase-73 copy), update it to assert on "Review started." instead — but ONLY if the test's `installReads` setup gives commits and zero comments (the warm-with-commits-zero-comments branch). Tests that assert on the unchanged "No commits in this review yet." branch should stay verbatim.
  </behavior>
  <action>
    Apply RED → GREEN → REFACTOR.

    **RED step:** Add the five tests in two new describe blocks at the bottom of `ReviewPanel.test.ts` (peer to End-review/cold-boot-resume blocks). Run `pnpm vitest run src/components/ReviewPanel.test.ts -t "empty states\|summary line"` — all five MUST fail (the cold copy doesn't exist; the warm-with-commits copy still says "No comments yet"; the summary span doesn't exist).

    Also: audit existing tests for the substring "No comments yet". `grep -n '"No comments yet"' src/components/ReviewPanel.test.ts`. If any existing test asserts that copy in a context where commits exist + zero comments, update to "Review started." in the SAME commit as the RED step (these are failing-test prep, not behavior changes). If no such test exists today, skip the audit — proceed.

    **GREEN step:** Modify `src/components/ReviewPanel.svelte`:

    1. Replace the current empty-state block (lines 417-431) with three mutually-exclusive `{#if}` branches per 73-PATTERNS.md §"Empty-state copy" and 73-UI-SPEC §"Copywriting Contract":

       Branch order (specificity-first, matching the contract):
       ```
       {#if sessionState === "none"}
         <div class="flex flex-col" style="gap: 4px; padding: 12px;">
           <span>No active review</span>
           <span style="color: var(--color-text-muted); font-size: 11px;">
             Toggle review mode in the toolbar to start.
           </span>
         </div>
       {:else if groups.length === 0}
         <!-- EXISTING warm-no-commits branch — preserved verbatim from lines 418-422 -->
         <div class="flex flex-col" style="gap: 4px; padding: 12px;">
           <span>No commits in this review yet.</span>
           <span style="color: var(--color-text-muted); font-size: 11px;">
             Add commits from the graph to start reviewing.
           </span>
         </div>
       {:else if !hasAnyComment}
         <!-- UPDATED copy: "Review started." / "Select diff lines..." (was "No comments yet.") -->
         <div class="flex flex-col" style="gap: 4px; padding: 12px;">
           <span>Review started.</span>
           <span style="color: var(--color-text-muted); font-size: 11px;">
             Select diff lines or add a commit note to comment.
           </span>
         </div>
       {/if}
       ```

       Use the EXACT style triplet shown (gap: 4px; padding: 12px outer; color: var(--color-text-muted); font-size: 11px body). NO inline colors beyond CSS custom properties.

    2. Add the session-summary span ABOVE the `{#if groups.length === 0}` block — i.e., BEFORE the new three-way empty-state block in the template flow — INSIDE the existing scroll body container (parent of line 417). Per 73-UI-SPEC §"Session-summary caption":

       ```
       {#if sessionState !== "none"}
         <span style="color: var(--color-text-muted); font-size: 11px; padding: 2px 0;">
           {comments.length} comments · {commits.length} commits
         </span>
       {/if}
       ```

       The middle dot must be the literal U+00B7 character `·` (NOT a `*` or `-`). The span is a direct child of the scroll-body flex column; no extra wrapper.

    Run the five new tests; all MUST pass. Run the full file; existing tests MUST still pass (after the "No comments yet" → "Review started." audit-update in the RED step, if any).

    **REFACTOR step:** If the three branches share the same outer wrapper styling, consider extracting `<EmptyState heading body>` snippets — Svelte 5 has `{#snippet}` syntax. Only do this if the executor sees genuine duplication that obscures intent. Otherwise keep all three inline (the duplication is small and reading top-to-bottom is clearer than chasing snippets). Re-run tests after any refactor.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk &amp;&amp; pnpm vitest run src/components/ReviewPanel.test.ts -t "empty states|summary line"</automated>
  </verify>
  <acceptance_criteria>
    - All five new tests pass.
    - Full file passes: `pnpm vitest run src/components/ReviewPanel.test.ts` exits 0.
    - `grep -n '"No active review"' src/components/ReviewPanel.svelte` returns the cold heading line.
    - `grep -n '"Review started\\."' src/components/ReviewPanel.svelte` returns the warm-with-commits-zero-comments heading.
    - `grep -n '"No commits in this review yet\\."' src/components/ReviewPanel.svelte` STILL returns the existing line (preserved verbatim — regression guard).
    - `grep -n 'comments · ' src/components/ReviewPanel.svelte` returns the summary-caption literal with the U+00B7 middle dot.
    - `grep -n '"No comments yet"' src/components/ReviewPanel.svelte` returns NO hits (the prior copy is replaced).
    - svelte-check passes.
    - Commits: `test(73-03): add failing tests for empty states + summary line` (RED) + `feat(73-03): three-way empty-state branching + session summary caption` (GREEN).
  </acceptance_criteria>
  <done>Cold/warm-no-commits/warm-with-commits-zero-comments empty states are visually distinct; the session-summary caption sits above the list and only renders when a session exists; no test regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-tab coordination tests (TDD: 2 tests)</name>
  <files>src/components/ReviewPanel.test.ts</files>
  <read_first>
    - src/components/ReviewPanel.svelte (lines 353-367 — the session-changed listener; verify it is UNTOUCHED in Plans 01/02)
    - src/components/ReviewPanel.test.ts (post-Task-1 — verify `fireSessionChanged` / `sessionChangedHandler` is exported from the Plan 01 scaffold)
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-RESEARCH.md (§"MULTI-TAB COORDINATION (REQ-73-MULTITAB)" flow diagram and §"Pitfall 2: Stale canonicalPath after End")
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-PATTERNS.md (§"Test mocks" — captured-callback pattern)
  </read_first>
  <behavior>
    Two tests in a new `describe("multi-tab coordination", ...)` block. REAL timers.

    - Test 1 ("End in another tab clears panel"): Set up a swappable status that flips after the first `get_review_session_status` call. Implementation: use `vi.mocked(safeInvoke).mockImplementation(...)` with a closure variable `let currentStatus: SessionStatus = { state: "active", file_exists: true, canonical_path: "/repo" }`. The dispatcher routes `get_review_session_status` to a fresh `currentStatus` read each time. After the initial render+flush asserts the warm state, mutate `currentStatus = { state: "none", file_exists: false, canonical_path: "/repo" }`, also flip `commits` / `comments` mock returns to `[]`, then call `fireSessionChanged("/repo")` → `await flush()` → assert the cold empty state ("No active review") is now in the DOM AND the warm copy is gone. The End button (from Plan 02) should also disappear since `sessionState === "none"`.
    - Test 2 ("session-changed for different repo is filtered out"): Render with `status: { state: "active", canonical_path: "/repo" }`, two commits, one comment. Capture the safeInvoke call count after initial flush. Then call `fireSessionChanged("/different-repo")` → `await flush()` → assert the safeInvoke call count is unchanged (the listener's `canonicalPath && event.payload !== canonicalPath` filter at line 357 short-circuits; reload does NOT fire). Assert the comment is still in the DOM (no re-render churn that would clear it).

    Both tests use the existing `flush()` helper, not `flushFake`. The captured `sessionChangedHandler` from Plan 01's test scaffold is the simulator.
  </behavior>
  <action>
    Apply RED → GREEN → REFACTOR.

    **RED step:** Add `describe("multi-tab coordination", ...)` to `ReviewPanel.test.ts` (peer to the others). Implement the two tests. The swappable-status helper is local to this describe (or a small helper function `setStatus(s)` that mutates the closure variable).

    Run `pnpm vitest run src/components/ReviewPanel.test.ts -t "multi-tab coordination"`. Both tests SHOULD pass immediately — the multi-tab behavior is entirely emergent from Plan 01's `sessionState` rune + the existing untouched session-changed listener + Plan 03 Task 1's empty-state branching. There is no NEW production code to write.

    If a test fails, diagnose:
    - Test 1 failure most likely cause: the second reload after `fireSessionChanged` does not actually re-fetch status because `installReads` is mocked once and not re-routed. Fix the test: ensure the dispatcher reads `currentStatus` on EVERY call (closure reference, not a captured-by-value).
    - Test 2 failure most likely cause: the listener's `canonicalPath` is not yet set when `fireSessionChanged` is called (race condition). Fix: ensure `await flush()` before the simulator call so the initial reload has set `canonicalPath`.

    If either failure is genuinely caused by a production-code bug surfaced by the test, that is a found bug — per ownership.md, fix it in this plan or surface it explicitly with file:line.

    **GREEN step:** Likely no production change needed. If a real bug surfaces in the listener filter or `sessionState` propagation, fix minimally and re-run tests.

    **REFACTOR step:** Consolidate the closure-variable status pattern into a small helper if the executor finds it reused — but this is the only test that needs it. Likely no refactor.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk &amp;&amp; pnpm vitest run src/components/ReviewPanel.test.ts -t "multi-tab coordination"</automated>
  </verify>
  <acceptance_criteria>
    - Both new tests pass.
    - Full file passes: `pnpm vitest run src/components/ReviewPanel.test.ts` exits 0.
    - `grep -n 'describe("multi-tab coordination"' src/components/ReviewPanel.test.ts` returns one hit.
    - `grep -n 'fireSessionChanged\|sessionChangedHandler' src/components/ReviewPanel.test.ts` shows usage in both new tests.
    - The session-changed listener at `ReviewPanel.svelte:353-367` is BYTE-FOR-BYTE UNCHANGED from Plan 02's state (`git diff src/components/ReviewPanel.svelte` shows no edits in this range). D-09 contract preserved.
    - Commits: `test(73-03): multi-tab coordination tests` (RED+GREEN if no production change) OR split RED/GREEN if a bug was fixed.
  </acceptance_criteria>
  <done>Multi-tab End → cold-state collapse is automated. Cross-repo filter is automated. The existing listener is preserved untouched (D-09).</done>
</task>

<task type="auto">
  <name>Task 3: just check phase gate + Phase 73 closure</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-VALIDATION.md (§"Manual-Only Verifications" — three rows)
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-CONTEXT.md (the full Success Criteria from the planning_context preamble)
  </read_first>
  <action>
    Run `just check`. This is the final phase gate.

    On success, prepare the SUMMARY with explicit traceability:
    - REQ-73-RESUME → Plan 01 ✓
    - REQ-73-END → Plan 02 ✓
    - REQ-73-EMPTY → Plan 03 Task 1 ✓
    - REQ-73-SUMMARY → Plan 03 Task 1 ✓
    - REQ-73-MULTITAB → Plan 03 Task 2 ✓
    - REQ-73-NYQUIST → all three plans contribute ✓
    - REQ-73-CHECK → confirmed green here ✓

    Three manual smokes (recorded in SUMMARY for `/gsd:verify-work` to spot-check):
    1. Cold-boot resume across actual app restart: `pnpm tauri dev`, open a repo with an on-disk session, observe comments appear without mutation.
    2. Multi-tab End → empty state in other window: open two windows on same repo, end review in A, observe B re-render.
    3. Visual treatment of two-step End button: click End once, observe "Click again to confirm" + danger color + ~3s auto-revert.

    Per ownership.md: fix any failures that surface. Per continuous_improvement.md: end this plan with a post-task reflection block in the SUMMARY (5 questions: what was harder than expected, anything done twice, incorrect assumptions, follow-up improvements, memory-file updates).
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk &amp;&amp; just check</automated>
  </verify>
  <acceptance_criteria>
    - `just check` exits 0.
    - All ~18 new tests across Plans 01/02/03 are green.
    - SUMMARY records REQ → Plan traceability and the three manual smoke results.
    - SUMMARY includes a continuous_improvement.md §1 reflection block.
    - The session-changed listener at `ReviewPanel.svelte:353-367` is byte-for-byte unchanged across all three plans (D-09).
  </acceptance_criteria>
  <done>Phase 73 ships. `/gsd:verify-work 73` can run against the SUMMARY and the three manual smokes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri event bus → Svelte listener | `session-changed` payload (canonical path string) flows into the listener at `ReviewPanel.svelte:353-367`. Filter on `canonicalPath && payload !== canonicalPath` is the cross-repo isolation control. |
| Plan 01 captured callback → test | `fireSessionChanged(payload)` simulates the cross-tab emit in the test environment only; not a production code path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-09 | Spoofing | session-changed event for an unrelated repo's canonical path | mitigate | Existing listener filter `if (canonicalPath && event.payload !== canonicalPath) return;` at `ReviewPanel.svelte:357`. Plan 03 Task 2 Test 2 proves this filter holds (asserts NO additional safeInvoke calls when a cross-repo payload arrives). |
| T-73-10 | Tampering | Race deleting session file while tab B is mid-reload | accept | Backend serializes via `Mutex<HashMap<...>>` and atomic file ops; tab B's reload sees a deterministic post-deletion status (`state === "none"`) and renders the cold empty state. No new race surface from Plan 03. |
| T-73-11 | Information Disclosure | Summary caption leaks count metadata via DOM | accept | Counts are derived from already-rendered comments/commits; no new IPC; no PII. The caption is in the same DOM tree as the comment list itself. |

**D-04 (summary placement), D-06 (cold vs warm copy), D-09 (multi-tab via existing listener) implemented per CONTEXT.**

**Listener UNTOUCHED invariant:** D-09 demands the session-changed listener at `ReviewPanel.svelte:353-367` is preserved verbatim. Plan 03 acceptance criteria explicitly grep for byte-for-byte preservation.
</threat_model>

<verification>
- Run the new describe blocks: `pnpm vitest run src/components/ReviewPanel.test.ts -t "empty states|summary line|multi-tab coordination"` — seven tests green.
- Run the full file: `pnpm vitest run src/components/ReviewPanel.test.ts` — full suite green.
- Run `just check` — exits 0.
- Three manual smokes from 73-VALIDATION.md §"Manual-Only Verifications" recorded in the SUMMARY.
</verification>

<success_criteria>
- REQ-73-EMPTY: Three distinct empty states render based on `sessionState` and `groups.length` / `hasAnyComment`. Three automated tests prove this.
- REQ-73-SUMMARY: Caption `{N} comments · {M} commits` (U+00B7) appears above the list when a session exists; hidden when cold. Two automated tests prove this.
- REQ-73-MULTITAB: Tab A end → tab B cold state via `session-changed`. Two automated tests prove this; the production-code listener is unchanged.
- REQ-73-NYQUIST: All five lifecycle paths (cold resume / end confirm flow / empty-state transitions / summary line / multi-tab) covered by automated tests across the three plans.
- REQ-73-CHECK: `just check` green.
- D-04, D-06, D-09 implemented.
- No backend code modified across the entire phase. No new packages installed.
</success_criteria>

<output>
Create `.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-03-SUMMARY.md` summarizing:
- Template changes (lines added/modified).
- Test coverage breakdown (5 + 2 = 7 new tests in Plan 03; ~18 total across the phase).
- Listener-byte-for-byte-unchanged confirmation (run `git diff src/components/ReviewPanel.svelte` and quote the listener block to prove no edits).
- Three manual smokes — pass/fail.
- REQ → Plan traceability table.
- continuous_improvement.md §1 reflection block (5 questions).
- Confirmation that Phase 73 is ready for `/gsd:verify-work 73`.
</output>
