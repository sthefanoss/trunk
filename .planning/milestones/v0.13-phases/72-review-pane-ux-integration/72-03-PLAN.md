---
phase: 72-review-pane-ux-integration
plan: 03
type: tdd
wave: 2
depends_on: ["72-01"]
files_modified:
  - src/components/ReviewPanel.svelte
  - src/components/ReviewPanel.test.ts
autonomous: true
requirements: [REQ-72-3a, REQ-72-3b, REQ-72-3c, REQ-72-3d, REQ-72-3e, G-71-A]
tags: [svelte5, clipboard, tdd, refactor, carry-forward]
must_haves:
  truths:
    - "Clicking Copy on ReviewPanel header invokes session.generate(repoPath) then writeText with the returned markdown"
    - "Successful copy shows '✓ Copied' for 1500ms then reverts to 'Copy'"
    - "Re-clicking during the 1500ms window extends the window (no race with stale revert)"
    - "Failure shows error toast with `instanceof Error` narrowing; button does NOT flip to Copied"
    - "Non-Error rejections coerced via String(e)"
    - "ReviewPanel no longer imports ReviewDocPreview, no longer references panelMode"
  artifacts:
    - path: src/components/ReviewPanel.svelte
      provides: "ReviewPanel with single-click Copy action in header"
      contains:
        - "await session.generate(repoPath)"
        - "await writeText"
        - "instanceof Error"
        - "clearTimeout(copyTimer)"
        - "import { Clipboard, MessageSquarePlus } from \"@lucide/svelte\""
        - "import { writeText } from \"@tauri-apps/plugin-clipboard-manager\""
      not_contains:
        - "ReviewDocPreview"
        - "panelMode"
        - "onGenerateClick"
    - path: src/components/ReviewPanel.test.ts
      provides: "Updated tests covering Copy flow (replaces preview-swap tests)"
      contains:
        - "copy click invokes generate and writeText"
        - "remains clickable during window"
        - "shows error toast on failure"
        - "coerces non-Error rejection"
        - "does not flip copied on failure"
      not_contains:
        - "back to comments"
        - "swaps to preview"
  key_links:
    - from: "ReviewPanel.svelte onCopyClick"
      to: "session.generate"
      via: "rune method"
      pattern: "await session\\.generate\\(repoPath\\)"
    - from: "ReviewPanel.svelte onCopyClick"
      to: "@tauri-apps/plugin-clipboard-manager writeText"
      via: clipboard
      pattern: "await writeText\\("
---

<objective>
Refactor ReviewPanel to host the Copy action directly in the header row, consuming the new `generate(repoPath): Promise<string>` from Plan 01 (rune) and writing to the system clipboard via the carry-forward pattern from Phase 71's `ReviewDocPreview`. Atomically rewrite the existing preview-swap tests at `ReviewPanel.test.ts:632-714` in the same edit as the component refactor (RESEARCH Pitfall 3 — cannot delete-then-add, or the suite goes red between commits). Closes G-71-A (Copy off the preview pane).

Purpose: Per CONTEXT.md, the Copy action moves onto the comments view (single click; no preview detour). The existing Phase 71 patterns (1500ms affordance, awaited writeText, instanceof Error narrowing, clearTimeout-before-setTimeout, vi.useFakeTimers) carry forward line-for-line; only the host changes.
Output: ReviewPanel with the renamed/refactored Copy button (replaces Generate at lines 359-368), the preview-swap early-return branch deleted (lines 332-339), the `ReviewDocPreview` import dropped, and the `ReviewPanel.test.ts` `Generate / preview` describe block (lines 632-714) replaced by a new `Copy` describe block (7 tests, scoped fake-timer lifecycle).

This plan depends on Plan 01 because the GREEN Copy handler calls `await session.generate(repoPath)` expecting `Promise<string>`; if Plan 01 hasn't landed, svelte-check rejects the type. Plan 04 will then delete the now-unused `ReviewDocPreview.svelte` and `.test.ts`.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-review-pane-ux-integration/72-CONTEXT.md
@.planning/phases/72-review-pane-ux-integration/72-RESEARCH.md
@.planning/phases/72-review-pane-ux-integration/72-PATTERNS.md
@.planning/phases/72-review-pane-ux-integration/72-UI-SPEC.md
@.planning/phases/72-review-pane-ux-integration/72-01-SUMMARY.md

<interfaces>
<!-- Plan 01 delivered (must be merged before this plan executes) -->
```ts
// review-session.svelte.ts (post-Plan 01)
async generate(repoPath: string): Promise<string>;
```

<!-- Carry-forward source (will be deleted in Plan 04 — copy patterns NOW) -->

ReviewDocPreview.svelte:13 — `import { Clipboard } from "@lucide/svelte";`
ReviewDocPreview.svelte:14 — `import { writeText } from "@tauri-apps/plugin-clipboard-manager";`
ReviewDocPreview.svelte:24-44 — entire Copy handler + state (copy line-for-line):
- `let copied = $state(false);`
- `let copyTimer: ReturnType<typeof setTimeout> | null = null;`
- `async function onCopyClick()` with try/catch wrapping `await writeText(markdown)` (in our case: `await session.generate(repoPath)` THEN `await writeText(md)`)
- `if (copyTimer !== null) clearTimeout(copyTimer);` BEFORE `copied = true;` BEFORE `copyTimer = setTimeout(() => { copied = false; copyTimer = null; }, 1500);`
- catch: `const msg = e instanceof Error ? e.message : String(e); showToast(\`Failed to copy: ${msg}\`, "error");`

ReviewDocPreview.svelte:55-63 — two-state button body ({#if copied} ✓ Copied {:else} Clipboard icon + Copy {/if})
ReviewDocPreview.svelte:118-135 — `.copy-button` CSS (`color: var(--color-text-muted)`, border, hover, transparent bg)

ReviewDocPreview.test.ts (entire file, 168 lines) — 8 tests map 1:1 to new Copy describe block per PATTERNS.md lines 377-388. Drop the "back button still invokes onBack" test (no back button anymore).

<!-- Current ReviewPanel surface being modified -->

ReviewPanel.svelte:8-20 — imports (drop FileText, ReviewDocPreview; add Clipboard, writeText)
ReviewPanel.svelte:272-287 — `onGenerateClick` (REPLACE with `onCopyClick`)
ReviewPanel.svelte:332-339 — preview-pane early-return (DELETE: `{#if session.state.panelMode === "preview" …} <ReviewDocPreview … /> {/if}`)
ReviewPanel.svelte:358-368 — Generate button JSX (REPLACE with Copy button)
ReviewPanel.svelte:350-368 — header row (keep `<span class="preview-spacer" style="flex: 1;"></span>` push-right; do NOT introduce new spacing values per UI-SPEC §Spacing)

<!-- Test file critical zones -->

ReviewPanel.test.ts:92-113 — `installReads` dispatcher already handles `generate_review_doc` (RESEARCH §Wave 0 Gaps)
ReviewPanel.test.ts:115-118 — existing `flush()` uses `setTimeout(r, 0)` — DEADLOCKS under vi.useFakeTimers, MUST scope fake timers to the new Copy describe ONLY (not file-global) and use a local `flushFake` that does `await Promise.resolve(); await tick();` (PATTERNS lines 348-365)
ReviewPanel.test.ts:632-714 — Generate / preview describe (DELETE in same edit as the Copy describe is added)

<!-- Toast import (already in ReviewPanel.svelte) -->
`showToast` from `../lib/toast.svelte.js` — already imported, do not duplicate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED+GREEN atomic — rewrite preview tests as Copy tests AND refactor the component in the same edit</name>
  <files>src/components/ReviewPanel.svelte, src/components/ReviewPanel.test.ts</files>
  <read_first>
    - src/components/ReviewPanel.svelte (entire file, 761 lines — focus on lines 1-20 imports, 272-287 handler, 332-339 preview branch, 350-368 header/button, the existing &lt;style&gt; block for the `.generate-button` class)
    - src/components/ReviewPanel.test.ts (entire file, 716 lines — focus on lines 1-30 mocks, 92-118 installReads + flush helpers, 632-714 Generate / preview describe)
    - src/components/ReviewDocPreview.svelte (entire file, 136 lines — carry-forward source: lines 13-14 imports, 24-44 handler, 55-63 button JSX, 118-135 .copy-button CSS)
    - src/components/ReviewDocPreview.test.ts (entire file, 168 lines — carry-forward source: lines 21-23 clipboard mock, 29-47 fake-timer lifecycle + flushFake, 52-54 getCopyButton selector, 60-167 the 8 test bodies)
    - .planning/phases/72-review-pane-ux-integration/72-PATTERNS.md (lines 217-409 — exhaustive edit map for both files including 8→7 test mapping table)
    - .planning/phases/72-review-pane-ux-integration/72-UI-SPEC.md (lines 178-188 — Copy button interaction contract; lines 132-135 — copy strings)
  </read_first>
  <behavior>
    Component (`ReviewPanel.svelte`):
    - Imports: drop `FileText`; add `Clipboard` to lucide imports; keep `MessageSquarePlus`. Drop `import ReviewDocPreview ...`. Add `import { writeText } from "@tauri-apps/plugin-clipboard-manager";`.
    - State: add `let copied = $state(false);` and `let copyTimer: ReturnType<typeof setTimeout> | null = null;` near other component state.
    - Replace `onGenerateClick` (lines 272-287) with `async function onCopyClick()` that: try { const md = await session.generate(repoPath); await writeText(md); if (copyTimer !== null) clearTimeout(copyTimer); copied = true; copyTimer = setTimeout(() => { copied = false; copyTimer = null; }, 1500); } catch (e) { const msg = e instanceof Error ? e.message : String(e); showToast(\`Failed to copy: ${msg}\`, "error"); }
    - Delete the preview early-return at lines 332-339 (`{#if session.state.panelMode === "preview" && session.state.previewMarkdown !== null} <ReviewDocPreview … /> {/if}`).
    - Replace the Generate button JSX (lines 358-368) with the Copy button two-state JSX: `<button class="copy-button flex items-center" onclick={onCopyClick} disabled={!hasAnyComment} title={hasAnyComment ? "" : "Add at least one comment to generate"}> {#if copied} <span aria-hidden="true">✓</span><span>Copied</span> {:else} <Clipboard size={14} /><span>Copy</span> {/if} </button>`. Keep the preceding `<span class="preview-spacer" style="flex: 1;"></span>`.
    - Replace the existing `.generate-button` CSS class in the component's `<style>` block with `.copy-button` using the verbatim shape from ReviewDocPreview.svelte:118-135 (transparent bg, muted text, border, hover→text+hover-bg). UI-SPEC §Spacing: no new spacing values introduced — these are inherited from the deleted source.

    Tests (`ReviewPanel.test.ts`):
    - Add module mock `vi.mock("@tauri-apps/plugin-clipboard-manager", () => ({ writeText: vi.fn().mockResolvedValue(undefined) }))` at top of file (after the existing `vi.mock("../lib/toast.svelte.js", …)`).
    - Delete the entire `Generate / preview` describe block (lines 632-714, 3 tests).
    - Add a new `describe("Copy", () => { ... })` block at the same location containing 7 tests mapped from `ReviewDocPreview.test.ts` per the table in PATTERNS.md lines 377-388. Scope `vi.useFakeTimers()` / `vi.useRealTimers()` to this describe only via local `beforeEach` / `afterEach`. Define a local `flushFake` helper that does `await Promise.resolve(); await tick();` (NEVER `setTimeout(r, 0)` — deadlocks under fake timers). Define `getCopyButton = () => screen.getByRole("button", { name: /Cop(y|ied)/ })`.
    - Each Copy test uses `installReads({ commits, comments: [lineAnchoredComment("c1", COMMIT_A, "look here")], resolutions: [resolvable("c1")], generateDoc: "the doc" })` to seed `generate_review_doc`, then renders ReviewPanel with the existing prop shape (lines 633-642 template).
    - The 7 tests: (a) "copy click invokes generate and writeText" — asserts `safeInvoke("generate_review_doc", ...)` AND `writeText("the doc")` were called; (b) "shows Copied affordance"; (c) "reverts to Copy after 1500ms"; (d) "remains clickable during window" — 500/1499/1ms timer math verbatim from source lines 95-123; (e) "shows error toast on failure" — mock writeText to reject; (f) "does not flip copied on failure"; (g) "coerces non-Error rejection" — verbatim from source lines 150-159.
    - One additional disabled-state test "Copy button is disabled when no comments" — replaces the deleted `generate button is disabled when no comments` test inside the new describe (button selector becomes `getCopyButton()`).
  </behavior>
  <action>
    Make ALL the changes above in a SINGLE atomic edit (Pitfall 3: cannot split into two commits or the suite is red between them). Order within the edit: (1) component imports + state + handler + JSX + CSS; (2) test mocks + delete preview describe + add Copy describe with scoped fake timers + 7 tests + 1 disabled-state test. Use line-for-line carry-forward from ReviewDocPreview.svelte:24-44 (handler) and ReviewDocPreview.test.ts:60-167 (test bodies) adapted to call `session.generate(repoPath)` before `writeText`. The catch block uses `instanceof Error` narrowing (NEVER `as TrunkError`, NEVER `as Error` — CLAUDE.md + UI-SPEC §Interaction 3 lock this). Run `bun run vitest run src/components/ReviewPanel.test.ts` — all tests (existing + 8 new Copy block) MUST pass GREEN in this single commit.
  </action>
  <verify>
    <automated>bun run vitest run src/components/ReviewPanel.test.ts && bunx svelte-check --tsconfig ./tsconfig.json src/components/ReviewPanel.svelte src/components/ReviewPanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ReviewPanel.svelte` contains the substring `await session.generate(repoPath)`.
    - `src/components/ReviewPanel.svelte` contains the substring `await writeText`.
    - `src/components/ReviewPanel.svelte` contains the substring `instanceof Error`.
    - `src/components/ReviewPanel.svelte` contains the substring `clearTimeout(copyTimer)`.
    - `src/components/ReviewPanel.svelte` contains the substring `import { writeText } from "@tauri-apps/plugin-clipboard-manager"`.
    - `src/components/ReviewPanel.svelte` contains the substring `Clipboard` (lucide icon import).
    - `src/components/ReviewPanel.svelte` does NOT contain the substrings `ReviewDocPreview`, `panelMode`, `onGenerateClick`, `FileText`.
    - `src/components/ReviewPanel.svelte` does NOT contain `(e as TrunkError)` or `as Error` (CLAUDE.md).
    - `src/components/ReviewPanel.test.ts` contains the substring `copy click invokes generate and writeText`.
    - `src/components/ReviewPanel.test.ts` contains the substring `remains clickable during window`.
    - `src/components/ReviewPanel.test.ts` contains the substring `shows error toast on failure`.
    - `src/components/ReviewPanel.test.ts` contains the substring `coerces non-Error rejection`.
    - `src/components/ReviewPanel.test.ts` contains the substring `does not flip copied on failure`.
    - `src/components/ReviewPanel.test.ts` contains the substring `vi.mock("@tauri-apps/plugin-clipboard-manager"`.
    - `src/components/ReviewPanel.test.ts` does NOT contain the substrings `back to comments`, `swaps to preview`.
    - `bun run vitest run src/components/ReviewPanel.test.ts` exits 0 (all tests pass — both existing and new).
    - `bunx svelte-check` reports zero errors for the touched files.
  </acceptance_criteria>
  <done>ReviewPanel hosts the Copy action; preview swap branch + import gone; tests cover the new flow; suite is GREEN in a single commit (no red gap). Plan 04 can now safely delete `ReviewDocPreview.{svelte,test.ts}` because nothing imports them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend ReviewPanel ↔ OS clipboard | The Copy button writes the generated markdown to the OS clipboard via the already-granted `clipboard-manager:allow-write-text` capability (Phase 65); no new capability surface |

## STRIDE Threat Register (per CONTEXT.md `## Threat model`)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-S | Spoofing | UI action | N/A | No new identity surface |
| T-72-T | Tampering | clipboard write | N/A | Same `writeText` plugin used in Phase 71; capability unchanged |
| T-72-R | Repudiation | error log surface | N/A | `Failed to copy: ${msg}` toast text identical to Phase 71 |
| T-72-I | Info disclosure | UI / error toast | accept (LOW) | Carry-forward from Phase 71 — same error surface, same data shape (review markdown the user explicitly clicked Copy for); accepted in CONTEXT.md threat table |
| T-72-D | DoS | runaway timer / IPC | mitigate | `clearTimeout(copyTimer)` BEFORE every new `setTimeout(...)` (Pitfall 2 carry-forward); IPC bounded by session comment count via the existing handler |
| T-72-E | Privilege escalation | capability | N/A | Capability granted Phase 65; no new capability |
| T-72-SC | Supply chain | packages | N/A | Zero new packages |
</threat_model>

<verification>
- `bun run vitest run src/components/ReviewPanel.test.ts` passes 100% (all existing tests + 8 new in the Copy describe).
- `bunx svelte-check` clean for ReviewPanel.svelte and ReviewPanel.test.ts.
- `grep -n 'ReviewDocPreview\|panelMode\|onGenerateClick\|(e as TrunkError)' src/components/ReviewPanel.svelte` returns zero lines.
- `grep -n 'back to comments\|swaps to preview' src/components/ReviewPanel.test.ts` returns zero lines.
</verification>

<success_criteria>
1. Copy button replaces Generate in ReviewPanel header (same docked position, no new spacing).
2. Click invokes `generate` then `writeText` with the returned markdown.
3. `✓ Copied` affordance for 1500ms with re-arm via `clearTimeout`.
4. Failure → `Failed to copy: ${msg}` toast; button stays on `Copy`.
5. `instanceof Error` narrowing; non-Error coerced via `String(e)`.
6. Preview swap branch and `ReviewDocPreview` import removed.
7. Test suite GREEN in single commit (no red gap between deletion and replacement).
</success_criteria>

<output>
Create `.planning/phases/72-review-pane-ux-integration/72-03-SUMMARY.md` when done. Include: exact line ranges deleted (272-287, 332-339, 358-368, 632-714 in tests), exact line ranges added (new handler, new button JSX, new Copy describe), the 7→8 test count delta in the new describe block, and the commit SHA (single atomic commit per Pitfall 3). Note explicitly that `ReviewDocPreview.svelte` and `.test.ts` are now unreferenced and safe for Plan 04 to delete.
</output>
