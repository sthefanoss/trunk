---
phase: 72-review-pane-ux-integration
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/components/Toolbar.svelte
  - src/components/Toolbar.test.ts
  - src/App.svelte
autonomous: true
requirements: [REQ-72-1a, REQ-72-2, G-71-B]
tags: [svelte5, toolbar, tauri-event, tdd, a11y]
must_haves:
  truths:
    - "Toolbar exposes a Review button that emits `review-toggle` on click"
    - "Toolbar Review button shows active styling (background + color + aria-pressed) when reviewActive prop is true"
    - "App.svelte passes reviewActive={reviewPanelOpen} to the Toolbar instance at App.svelte:584"
    - "Default Toolbar Review button (review off) is visually flat among siblings — accent is reclaimed only when active"
  artifacts:
    - path: src/components/Toolbar.svelte
      provides: "Toolbar with Review toggle button"
      contains:
        - "MessagesSquare"
        - "void emit(\"review-toggle\")"
        - "class:toolbar-btn-active={reviewActive}"
        - "aria-pressed={reviewActive}"
        - "reviewActive: boolean"
    - path: src/components/Toolbar.test.ts
      provides: "Toolbar test coverage for Review button"
      contains:
        - "review-toggle"
        - "active state"
        - "emit: vi.fn().mockResolvedValue(undefined)"
    - path: src/App.svelte
      provides: "App passes reviewActive prop to Toolbar"
      contains: "reviewActive={reviewPanelOpen}"
  key_links:
    - from: src/components/Toolbar.svelte
      to: "@tauri-apps/api/event emit"
      via: "review-toggle event bus"
      pattern: "emit\\(\"review-toggle\"\\)"
    - from: src/App.svelte
      to: src/components/Toolbar.svelte
      via: reviewActive prop
      pattern: "reviewActive=\\{reviewPanelOpen\\}"
---

<objective>
Add a Review toggle button to the in-window Toolbar that emits the existing `review-toggle` Tauri event (shared with the menu handler at `src-tauri/src/lib.rs:67-69`). Wire the App-level `reviewPanelOpen` state into the Toolbar as a new `reviewActive` prop so the button can show active styling. Closes G-71-B (smooth entry/exit + no dead button) for the in-window toolbar surface; Cmd+Shift+R accelerator lands in Plan 04.

Purpose: Per CONTEXT.md root-cause analysis, the existing "is review on?" indicator is invisible (only a hidden menu item). Externalizing this state to a Toolbar button with proper active styling is the structural fix.
Output: Toolbar component with a new `MessagesSquare`-icon Review button that flips active when `reviewActive===true`, accompanying test coverage, and the App-level prop pass-through that delivers the flag.

This plan is parallel to Plan 01 (zero file overlap, no behavior dependency). Plan 04 will reference the new `.toolbar-btn-active` class but does not modify this file.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-review-pane-ux-integration/72-CONTEXT.md
@.planning/phases/72-review-pane-ux-integration/72-RESEARCH.md
@.planning/phases/72-review-pane-ux-integration/72-PATTERNS.md
@.planning/phases/72-review-pane-ux-integration/72-UI-SPEC.md

<interfaces>
<!-- Toolbar current Props (Toolbar.svelte:20-26) — extend with reviewActive -->

```ts
interface Props {
  repoPath: string;
  remoteState: RemoteState;
  undoRedo: UndoRedoManager;
  // ADD: reviewActive: boolean;
}
```

<!-- Existing toolbar-group pattern (Toolbar.svelte:264-274) — clone shape -->
```svelte
<div class="toolbar-group">
  <button class="toolbar-btn" onclick={handleBranch}>
    <GitBranch size={14} /> Branch
  </button>
</div>
```

<!-- Existing .toolbar-btn CSS (Toolbar.svelte:209-230) — do NOT modify; add .toolbar-btn-active below it -->

<!-- App.svelte:582-585 — Toolbar mounting site, extend props -->
```svelte
<Toolbar repoPath={activeTab.repoPath} remoteState={activeState.remoteState} undoRedo={activeState.undoRedo} />
<!-- becomes: + reviewActive={reviewPanelOpen} -->
```
`reviewPanelOpen` is declared at `App.svelte:57` as `let reviewPanelOpen = $state(false)`. Confirmed App-owned; the Toolbar is single-instance App-mounted, NOT per-tab (per-tab `reviewSession` is unreachable from here — RESEARCH Pitfall 4).

<!-- Tauri event API (@tauri-apps/api/event.d.ts:145) -->
```ts
export { emit, listen, once, emitTo, TauriEvent };
```

<!-- Rust counterpart (src-tauri/src/lib.rs:67-69) — same event string -->
```rust
"review-toggle" => { let _ = app.emit("review-toggle", ()); }
```

<!-- Lucide MessagesSquare verified at node_modules/@lucide/svelte/dist/icons/index.js:1042 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing tests for Toolbar Review button (emit + active state)</name>
  <files>src/components/Toolbar.test.ts</files>
  <read_first>
    - src/components/Toolbar.test.ts (entire file, 128 lines — existing test patterns)
    - .planning/phases/72-review-pane-ux-integration/72-PATTERNS.md (lines 137-214 — exact test additions)
    - .planning/phases/72-review-pane-ux-integration/72-RESEARCH.md (lines 421-428 — emit mock recipe)
  </read_first>
  <behavior>
    - Test "emits review-toggle on click": render Toolbar with `reviewActive: false`; click the button whose text contains "Review"; `vi.mocked(emit)` was called with `"review-toggle"`.
    - Test "shows active state when reviewActive is true": render Toolbar with `reviewActive: true`; the Review button has class `toolbar-btn-active` AND `aria-pressed="true"`.
    - Existing tests in Toolbar.test.ts must be updated to include `reviewActive: false` in their props object — TypeScript will enforce once the Props interface lands in Task 2; the test author updates them now to keep the suite green after Task 2 GREEN.
    - Update the module mock for `@tauri-apps/api/event` to expose `emit: vi.fn().mockResolvedValue(undefined)` alongside the existing `listen` mock.
  </behavior>
  <action>
    Per PATTERNS.md §`src/components/Toolbar.test.ts` (lines 137-214): (a) modify the module mock at the top of the file to add `emit: vi.fn().mockResolvedValue(undefined)` to the `@tauri-apps/api/event` mock factory; (b) add `reviewActive: false` to every existing `render(Toolbar, { props: { ... } })` call (tests at lines 40-127 — there are multiple); (c) append two new `it` blocks at the bottom of the existing describe: "emits review-toggle on click" (uses `await import("@tauri-apps/api/event")` to retrieve the mocked `emit`, asserts `vi.mocked(emit).toHaveBeenCalledWith("review-toggle")`) and "shows active state when reviewActive is true" (renders with `reviewActive: true`, asserts `btn.toHaveClass("toolbar-btn-active")` AND `btn.toHaveAttribute("aria-pressed", "true")`). Use the exact patterns from PATTERNS.md lines 185-211. Run `bun run vitest run src/components/Toolbar.test.ts` — the two new tests MUST fail (no Review button exists yet).
  </action>
  <verify>
    <automated>bun run vitest run src/components/Toolbar.test.ts 2>&1 | tee /tmp/72-02-red.log | grep -E "FAIL|emits review-toggle|shows active state"</automated>
  </verify>
  <acceptance_criteria>
    - File contains the substring `emits review-toggle on click`.
    - File contains the substring `shows active state when reviewActive is true`.
    - File contains the substring `emit: vi.fn().mockResolvedValue(undefined)`.
    - File contains `vi.mocked(emit).toHaveBeenCalledWith("review-toggle")`.
    - File contains `toHaveClass("toolbar-btn-active")`.
    - File contains `toHaveAttribute("aria-pressed", "true")`.
    - `bun run vitest run src/components/Toolbar.test.ts` exits non-zero on the two new tests (RED).
  </acceptance_criteria>
  <done>Two failing tests added; existing render calls updated with `reviewActive: false`; emit mock in place; RED confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Toolbar Review button + active class + emit + App prop wiring</name>
  <files>src/components/Toolbar.svelte, src/App.svelte</files>
  <read_first>
    - src/components/Toolbar.svelte (entire file, 284 lines — Props interface, .toolbar-btn CSS, group layout)
    - src/App.svelte (lines 50-65 for `reviewPanelOpen` declaration; lines 580-590 for Toolbar mounting site)
    - .planning/phases/72-review-pane-ux-integration/72-PATTERNS.md (lines 25-134 — Toolbar template; lines 619-635 — App prop pass)
    - .planning/phases/72-review-pane-ux-integration/72-UI-SPEC.md (lines 99-119 — active-state contract; lines 200-203 — layout placement)
  </read_first>
  <behavior>
    - Toolbar.svelte Props interface gains `reviewActive: boolean`; destructure receives it.
    - Imports: add `MessagesSquare` to the existing `@lucide/svelte` import block; change `import { listen } from "@tauri-apps/api/event"` to `import { emit, listen } from "@tauri-apps/api/event"`.
    - Add `function handleReviewToggle() { void emit("review-toggle"); }` to the script block.
    - Append a new `<div class="toolbar-group">` as the rightmost group (after the Branch/Stash/Pop group at lines 264-274) containing a single `<button class="toolbar-btn" class:toolbar-btn-active={reviewActive} aria-pressed={reviewActive} onclick={handleReviewToggle}><MessagesSquare size={14} /> Review</button>`.
    - Add `.toolbar-btn-active` CSS (active fill, on-accent text, hover stays filled) per UI-SPEC §Color active-state table — using `var(--color-accent)` and `var(--color-on-accent)`. Do NOT modify the existing `.toolbar-btn` rule.
    - App.svelte:584 `<Toolbar ... />` element gains `reviewActive={reviewPanelOpen}` prop. Do NOT modify the `reviewPanelOpen` declaration or its listener.
  </behavior>
  <action>
    Per PATTERNS.md template at lines 112-130, add to `Toolbar.svelte`: (a) extend Props interface and destructure with `reviewActive: boolean`; (b) add `MessagesSquare` to lucide imports, add `emit` to `@tauri-apps/api/event` imports; (c) add `function handleReviewToggle() { void emit("review-toggle"); }`; (d) append a new `<div class="toolbar-group">` after the Branch/Stash/Pop group (after line 274) with a single Review button using `class:toolbar-btn-active` and `aria-pressed` (NEVER inline `style="background: …"` ternaries — UI-SPEC §Color "Wiring discipline" forbids recreating the deleted blue-button anti-pattern); (e) append a `.toolbar-btn-active` CSS rule after the existing `.toolbar-btn` block (lines 209-230) using only `var(--color-accent)` and `var(--color-on-accent)` tokens. Then in App.svelte, locate the `<Toolbar ... />` element at line 584 and add `reviewActive={reviewPanelOpen}` to its props. Run `bun run vitest run src/components/Toolbar.test.ts` — all tests must pass GREEN.
  </action>
  <verify>
    <automated>bun run vitest run src/components/Toolbar.test.ts && bunx svelte-check --tsconfig ./tsconfig.json src/components/Toolbar.svelte src/App.svelte src/components/Toolbar.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/Toolbar.svelte` contains the substring `reviewActive: boolean`.
    - `src/components/Toolbar.svelte` contains the substring `MessagesSquare`.
    - `src/components/Toolbar.svelte` contains the substring `import { emit, listen } from "@tauri-apps/api/event"`.
    - `src/components/Toolbar.svelte` contains the substring `void emit("review-toggle")`.
    - `src/components/Toolbar.svelte` contains the substring `class:toolbar-btn-active={reviewActive}`.
    - `src/components/Toolbar.svelte` contains the substring `aria-pressed={reviewActive}`.
    - `src/components/Toolbar.svelte` contains the substring `.toolbar-btn-active`.
    - `src/components/Toolbar.svelte` does NOT contain the substring `style="background:` near the new Review button (no inline color ternary).
    - `src/App.svelte` contains the substring `reviewActive={reviewPanelOpen}`.
    - `bun run vitest run src/components/Toolbar.test.ts` exits 0 (all tests GREEN).
    - `bunx svelte-check` reports zero errors for the touched files.
  </acceptance_criteria>
  <done>Toolbar has a Review button that emits `review-toggle` and shows accent fill + on-accent text + aria-pressed when `reviewActive` is true; App.svelte pipes `reviewPanelOpen` into the prop; all Toolbar tests green; no inline-style anti-pattern reintroduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend Toolbar ↔ Tauri event bus | New frontend emitter onto the in-process `review-toggle` channel; consumer is the existing App.svelte listener |

## STRIDE Threat Register (per CONTEXT.md `## Threat model`)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-S | Spoofing | event-bus emit | N/A | Tauri 2 event bus is webview↔core in-process only; not externally reachable; same channel the menu handler already uses |
| T-72-T | Tampering | IPC | N/A | No new IPC handler; no new capability |
| T-72-R | Repudiation | logging | N/A | No new logging surface |
| T-72-I | Info disclosure | UI | N/A | Button label + state styling expose no sensitive data |
| T-72-D | DoS | runaway events | N/A | `void emit(...)` is fire-and-forget local bus, bounded by user click rate |
| T-72-E | Privilege escalation | capability | N/A | No new Tauri capability |
| T-72-SC | Supply chain | packages | N/A | Zero new packages (all imports pre-existing) |
</threat_model>

<verification>
- `bun run vitest run src/components/Toolbar.test.ts` passes all tests (existing + 2 new).
- `bunx svelte-check` clean for all touched files.
- `grep -n 'style="background:' src/components/Toolbar.svelte` returns no matches near the Review button (no inline-color anti-pattern).
- Visual sanity (deferred to Plan 04 manual UAT — covered by REQ-72-1c/1b).
</verification>

<success_criteria>
1. Toolbar renders a Review button that emits `review-toggle` on click.
2. Active styling toggles via `.toolbar-btn-active` class (not inline style) when `reviewActive===true`.
3. ARIA: `aria-pressed` reflects `reviewActive`.
4. App.svelte pipes `reviewPanelOpen` through as the `reviewActive` prop.
5. svelte-check + vitest green.
</success_criteria>

<output>
Create `.planning/phases/72-review-pane-ux-integration/72-02-SUMMARY.md` when done. Include: exact line numbers for the new toolbar-group, the new `.toolbar-btn-active` CSS, the App.svelte prop addition, and both commit SHAs (RED + GREEN). Note explicitly that no inline-style ternary was used (UI-SPEC §Color discipline preserved).
</output>
