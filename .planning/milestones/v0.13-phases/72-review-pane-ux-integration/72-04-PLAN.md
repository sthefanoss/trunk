---
phase: 72-review-pane-ux-integration
plan: 04
type: execute
wave: 3
depends_on: ["72-02", "72-03"]
files_modified:
  - src/components/ReviewDocPreview.svelte
  - src/components/ReviewDocPreview.test.ts
  - src/components/RepoView.svelte
  - src-tauri/src/lib.rs
autonomous: false
requirements: [REQ-72-1b, REQ-72-1c, REQ-72-4a, REQ-72-5a, REQ-72-5b, REQ-72-6, G-71-A, G-71-B]
tags: [cleanup, deletion, rust, tauri-menu, manual-uat]
must_haves:
  truths:
    - "ReviewDocPreview.svelte does not exist"
    - "ReviewDocPreview.test.ts does not exist"
    - "RepoView.svelte no longer contains the blue Review button header strip (lines 813-828 of the pre-72 file)"
    - "src-tauri/src/lib.rs:28 review_item MenuItemBuilder has .accelerator(\"CmdOrCtrl+Shift+R\") chained"
    - "View menu still routes the menu item to review-toggle (regression unbroken)"
    - "Cmd+Shift+R toggles review mode in the running app (manual UAT)"
    - "just check exits 0"
  artifacts:
    - path: src/components/ReviewDocPreview.svelte
      provides: "DELETED"
    - path: src/components/ReviewDocPreview.test.ts
      provides: "DELETED"
    - path: src/components/RepoView.svelte
      provides: "Cleaned RepoView — no blue button strip; inner conditional renders ReviewPanel or DiffPanel directly"
      not_contains:
        - "background: {reviewSession.state.rightPaneMode === 'panel' ?"
        - "onclick={() => reviewSession.showPanel()}"
    - path: src-tauri/src/lib.rs
      provides: "Menu accelerator Cmd+Shift+R bound to review-toggle"
      contains: ".accelerator(\"CmdOrCtrl+Shift+R\")"
  key_links:
    - from: src-tauri/src/lib.rs:28
      to: "review-toggle event"
      via: "Tauri menu accelerator"
      pattern: "\\.accelerator\\(\"CmdOrCtrl\\+Shift\\+R\"\\)"
    - from: src/components/RepoView.svelte (surviving line ~839)
      to: "reviewSession.showPanel()"
      via: "DiffPanel onclose"
      pattern: "reviewSession\\.showPanel\\(\\)"
---

<objective>
Structural cleanup: delete `ReviewDocPreview.svelte` and `ReviewDocPreview.test.ts` (now unreferenced after Plan 03), delete the blue-button header strip at `RepoView.svelte:813-828` (the dead UI from G-71-B), and add the `Cmd+Shift+R` Tauri menu accelerator to `src-tauri/src/lib.rs:28`. Then a blocking manual UAT checkpoint verifies the three behaviors that have no automated test coverage: Cmd+Shift+R toggles review mode (REQ-72-1b), the View → Start/End Code Review menu item still works (REQ-72-1c), and DiffPanel close still returns to ReviewPanel after a jump (REQ-72-5b). Finally `just check` is the phase gate.

Purpose: All three cleanup actions are independent of each other but each too small for its own plan, so they bundle together. Each requires Plan 02 (Toolbar entry surface present so review mode is reachable) and Plan 03 (ReviewPanel Copy refactor + ReviewDocPreview reference removed) to be merged first.
Output: Two files deleted, one file pruned, one Rust line added, `just check` green, manual UAT signed off.

This plan is non-autonomous (`autonomous: false`) because it contains a blocking `checkpoint:human-verify` for the three manual UAT items.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-review-pane-ux-integration/72-CONTEXT.md
@.planning/phases/72-review-pane-ux-integration/72-RESEARCH.md
@.planning/phases/72-review-pane-ux-integration/72-PATTERNS.md
@.planning/phases/72-review-pane-ux-integration/72-UI-SPEC.md
@.planning/phases/72-review-pane-ux-integration/72-02-SUMMARY.md
@.planning/phases/72-review-pane-ux-integration/72-03-SUMMARY.md

<interfaces>
<!-- Wave 0 finding (confirmed by grep): RepoView.test.ts does NOT assert on the blue-button strip.
     REQ-72-5a defaults to manual UAT per VALIDATION.md option. No DOM-absence test needed. -->

<!-- Existing menu accelerator pattern (src-tauri/src/lib.rs:21-23) — exact mirror for our addition -->
```rust
let find = MenuItemBuilder::with_id("find", "Find")
    .accelerator("CmdOrCtrl+F")
    .build(app)?;
```

<!-- Current review_item (src-tauri/src/lib.rs:27-28) — chain .accelerator on it -->
```rust
let review_item =
    MenuItemBuilder::with_id("review-toggle", "Start/End Code Review").build(app)?;
```

<!-- RepoView.svelte:813-828 — exact block to delete (PATTERNS.md lines 418-435).
     Note: this is the OUTER wrapper at 813 (`<div class="flex flex-col" style="height: 100%; min-height: 0;">`)
     plus the inner header div + blue button. The inner conditional at lines 829-845 stays. -->

<!-- Surviving DiffPanel onclose at RepoView.svelte:~839 — DO NOT modify -->
```svelte
onclose={() => { handleDiffClose(); reviewSession.showPanel(); }}
```
This is the back-affordance that survives the blue-button deletion (CONTEXT.md success criterion 5).

<!-- Simplified inner shape after cleanup (PATTERNS.md lines 438-460) -->
```svelte
{#if reviewSession.state.reviewActive}
  <div class="flex flex-col" style="flex: 1; min-height: 0; overflow: hidden;">
    {#if reviewSession.state.rightPaneMode === 'diff' && showDiff}
      <DiffPanel … onclose={() => { handleDiffClose(); reviewSession.showPanel(); }} />
    {:else}
      <ReviewPanel … />
    {/if}
  </div>
{:else if showMergeEditor && selectedFile}
  …
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete ReviewDocPreview component and its test file</name>
  <files>src/components/ReviewDocPreview.svelte, src/components/ReviewDocPreview.test.ts</files>
  <read_first>
    - src/components/ReviewPanel.svelte (confirm Plan 03 merged: grep for `ReviewDocPreview` should return zero matches — this is the precondition that makes deletion safe)
    - .planning/phases/72-review-pane-ux-integration/72-03-SUMMARY.md (confirm Plan 03 completion)
  </read_first>
  <action>
    Run `rg -n "ReviewDocPreview" src/ 2>/dev/null` first as a sanity check — output MUST be empty (or limited to the two files about to be deleted), proving no other component imports it. If anything else still references it, STOP and report the dangling reference (Plan 03 didn't finish its cleanup). Once verified clean, delete both files with `rm src/components/ReviewDocPreview.svelte src/components/ReviewDocPreview.test.ts`. Then run `bun run vitest run` to confirm no test suite was depending on the deleted test file.
  </action>
  <verify>
    <automated>test ! -f src/components/ReviewDocPreview.svelte && test ! -f src/components/ReviewDocPreview.test.ts && (rg -n "ReviewDocPreview" src/ 2>/dev/null | grep -v '^$' | wc -l | grep -qE '^\s*0\s*$') && bun run vitest run</automated>
  </verify>
  <acceptance_criteria>
    - Command `test ! -f src/components/ReviewDocPreview.svelte` exits 0.
    - Command `test ! -f src/components/ReviewDocPreview.test.ts` exits 0.
    - Command `rg -n "ReviewDocPreview" src/` returns no matches.
    - `bun run vitest run` exits 0 (full suite still green after deletion).
  </acceptance_criteria>
  <done>Both files gone; no dangling imports; full test suite still green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete blue-button header strip from RepoView.svelte AND add Cmd+Shift+R accelerator</name>
  <files>src/components/RepoView.svelte, src-tauri/src/lib.rs</files>
  <read_first>
    - src/components/RepoView.svelte (lines 805-850 — the strip being deleted + the inner conditional that survives + the DiffPanel onclose wiring)
    - src-tauri/src/lib.rs (lines 14-72 — menu setup, especially lines 21-28 for the analog `find` accelerator and the current `review_item` builder)
    - .planning/phases/72-review-pane-ux-integration/72-PATTERNS.md (lines 412-462 — RepoView edit; lines 566-593 — Rust accelerator edit)
    - .planning/phases/72-review-pane-ux-integration/72-UI-SPEC.md (lines 168-176 — Interaction 2 contract for Cmd+Shift+R)
  </read_first>
  <action>
    (a) In `src/components/RepoView.svelte`, delete the outer wrapper + header strip + blue button block at the lines currently numbered 813-828 (per CONTEXT.md / PATTERNS.md lines 418-435). The inner conditional (currently at lines 829-845) must survive and be reshaped per PATTERNS.md lines 438-460: it now lives directly inside `{#if reviewSession.state.reviewActive}` wrapped in `<div class="flex flex-col" style="flex: 1; min-height: 0; overflow: hidden;">` (children flow naturally via the parent's existing flex column — no positioning hacks per CLAUDE.md). The DiffPanel `onclose={() => { handleDiffClose(); reviewSession.showPanel(); }}` line stays verbatim (this is the surviving back-affordance per CONTEXT.md success criterion 5). NEVER recreate the inline-style ternary pattern on any element.
    (b) In `src-tauri/src/lib.rs`, modify the `review_item` MenuItemBuilder at line 27-28 to chain `.accelerator("CmdOrCtrl+Shift+R")` between `with_id(...)` and `.build(app)?`, mirroring the `find` accelerator at lines 21-23. No other Rust code changes — the existing `app.on_menu_event` handler at lines 64-70 already routes `review-toggle` → `app.emit("review-toggle", ())`. Run `just check` after both edits — it must exit 0.
  </action>
  <verify>
    <automated>just check</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/RepoView.svelte` does NOT contain the substring `background: {reviewSession.state.rightPaneMode === 'panel' ?`.
    - `src/components/RepoView.svelte` does NOT contain a `<button …>Review</button>` element bound to `reviewSession.showPanel()` (the deleted blue button).
    - `src/components/RepoView.svelte` still contains the substring `onclose={() => { handleDiffClose(); reviewSession.showPanel(); }}` (surviving DiffPanel back-affordance).
    - `src/components/RepoView.svelte` still contains the substring `{#if reviewSession.state.reviewActive}` (review-active branch preserved).
    - `src-tauri/src/lib.rs` contains the substring `.accelerator("CmdOrCtrl+Shift+R")`.
    - `src-tauri/src/lib.rs` line containing `review-toggle` MenuItemBuilder still references `Start/End Code Review` (label unchanged).
    - `just check` exits 0 (fmt, biome, svelte-check, clippy, cargo-test, vitest all green — this is the phase gate REQ-72-6).
  </acceptance_criteria>
  <done>Blue strip gone, accelerator added, full quality gate green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual UAT for the three behaviors without automated coverage</name>
  <what-built>
    - Toolbar Review button (Plan 02) — already automated-tested for emit + active state.
    - Cmd+Shift+R menu accelerator (Task 2 above) — Tauri menu config only; no automated test.
    - View → Start/End Code Review menu item — pre-existing; verify the new accelerator binding did not break the menu item itself.
    - DiffPanel close back to ReviewPanel after a jump — existing wiring (`reviewSession.showPanel()` at line ~839); verify still works after the blue-strip deletion.
  </what-built>
  <how-to-verify>
    Run `just dev` to launch the app on a real repo.

    1. **REQ-72-1c (View menu regression):** Open the macOS View menu. Confirm "Start Code Review" (or "End Code Review" if already active) is present and visible. Confirm the keyboard hint `⌘⇧R` is displayed next to it (this confirms the accelerator binding loaded). Click the menu item → review mode toggles → Toolbar Review button changes to active state (or away from it).

    2. **REQ-72-1b (Cmd+Shift+R accelerator):** With the app focused (any pane), press `Cmd+Shift+R`. Expected: review mode toggles — Toolbar Review button flips active state, ReviewPanel appears in the right pane (or disappears if it was on). Press again → reverses the toggle. Confirm this works from at least two different focus contexts (e.g., commit graph focused, then with the diff panel focused).

    3. **REQ-72-5b (DiffPanel close returns to ReviewPanel):** Enter review mode. Add a line-anchored comment to a file in a commit. From the ReviewPanel, click on the comment to jump to the diff. Confirm the DiffPanel renders. Click the DiffPanel close button. Expected: the right pane returns to showing ReviewPanel (the comments list), NOT a blank state. This verifies the surviving `onclose={() => { handleDiffClose(); reviewSession.showPanel(); }}` wiring still works after the blue-strip deletion.

    4. **Visual sanity (Plan 02 spot-check):** Confirm the new Toolbar Review button is visible on the right edge of the toolbar. Default (review off) state: flat, blends with siblings. Active (review on) state: accent-blue background with white text. ARIA: inspect element → `aria-pressed` attribute toggles between `"true"` and `"false"`.

    5. **NOT TESTED (out of scope per RESEARCH Pitfall 2):** Do NOT verify light-mode rendering. The app is forced dark (`src/app.css:4`); there is no light theme.

    Report any failure with the step number and observed vs. expected behavior. If all five pass, type "approved" to advance.
  </how-to-verify>
  <resume-signal>Type "approved" once all five UAT steps pass, or describe the failure (step number + observed behavior).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS keyboard shortcut ↔ Tauri menu | New `Cmd+Shift+R` accelerator binds the existing `review-toggle` menu item; no new menu item, no new handler |
| File deletions | Removing checked-in source files — verified no remaining imports before deletion |

## STRIDE Threat Register (per CONTEXT.md `## Threat model`)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-S | Spoofing | menu accelerator | N/A | OS-managed accelerator routes to the existing menu handler; no new identity surface |
| T-72-T | Tampering | menu config | N/A | One-line chained method on existing builder; no behavior change to the menu handler at lib.rs:67-69 |
| T-72-R | Repudiation | logging | N/A | No new logging surface |
| T-72-I | Info disclosure | UI | N/A | Deleting UI strictly reduces surface; menu item text unchanged |
| T-72-D | DoS | shortcut chord | N/A | OS-bounded keypress; routes through the same handler as the menu click |
| T-72-E | Privilege escalation | capability | N/A | No new Tauri capability |
| T-72-SC | Supply chain | packages | N/A | Zero new packages (carry-forward from prior plans) |
</threat_model>

<verification>
- `test ! -f src/components/ReviewDocPreview.svelte` exits 0.
- `test ! -f src/components/ReviewDocPreview.test.ts` exits 0.
- `rg -n "ReviewDocPreview" src/` returns no matches.
- `rg -n '\.accelerator\("CmdOrCtrl\+Shift\+R"\)' src-tauri/src/lib.rs` returns one match.
- `rg -n "background: \{reviewSession\.state\.rightPaneMode" src/components/RepoView.svelte` returns no matches.
- `just check` exits 0 (the phase gate REQ-72-6).
- Manual UAT all five steps signed off.
</verification>

<success_criteria>
1. ReviewDocPreview.svelte and .test.ts deleted; no dangling references.
2. RepoView.svelte blue-strip removed; inner conditional preserved with the surviving DiffPanel back-affordance.
3. Cmd+Shift+R bound to the review-toggle menu item.
4. `just check` exits 0.
5. Manual UAT confirms: View-menu regression intact, Cmd+Shift+R toggles, DiffPanel close returns to ReviewPanel.
</success_criteria>

<output>
Create `.planning/phases/72-review-pane-ux-integration/72-04-SUMMARY.md` when done. Include:
- The two deletion commands and their commit SHA.
- The exact line numbers of the deleted blue-strip block in RepoView.svelte (the pre-deletion 813-828 range, post-72 line numbers will shift).
- The one-line Rust diff (chained `.accelerator(...)`).
- `just check` final exit status and any noteworthy CI signals.
- Manual UAT sign-off transcript (all five steps).
- Explicit statement: Phase 72 success criteria 1-6 all met; the milestone is ready for `/gsd:verify-work 72`.
</output>
