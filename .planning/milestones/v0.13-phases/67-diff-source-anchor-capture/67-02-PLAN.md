---
phase: 67-diff-source-anchor-capture
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/commands/review.rs
  - src-tauri/src/lib.rs
autonomous: true
requirements: [ANCH-01]

must_haves:
  truths:
    - "L-08: add_comment_inner is a shared dumb writer — it accepts a fully-formed Anchor carrying source/side, so it persists Source::FullFile unchanged (Rust test locks the Phase-68 contract)"
    - "L-05: add_comment persists immediately (atomic tmp+rename via review_store::save_session) so the anchor survives the watcher's repo-changed re-fetch; save_draft_comment persists the draft on change"
    - "L-06: the persisted Comment stores cached_excerpt verbatim (the excerpt is the canonical body)"
    - "submit clears the single draft_comment slot — add_comment sets draft_comment = None so the composer never reopens with stale text"
    - "save_draft_comment does NOT emit session-changed (drafts are not panel-visible until Phase 69; per-keystroke emits would cause reload storms); add_comment DOES emit"
    - "SC-2: persist anchor -> reload from disk -> all six anchor fields identical (render-time re-resolution is Phase 70, NOT here)"
    - "mutate_session_rmw is generalized from FnOnce(&mut Vec<String>) to FnOnce(&mut ReviewSession) — existing add_review_commit adapts; NO sibling RMW function is cloned"
  artifacts:
    - path: "src-tauri/src/commands/review.rs"
      provides: "add_comment + add_comment_inner, save_draft_comment + save_draft_comment_inner (shared, source/side parameterized), generalized mutate_session_rmw, #[cfg(test)] coverage"
      contains: "add_comment_inner"
    - path: "src-tauri/src/lib.rs"
      provides: "invoke_handler registration of add_comment + save_draft_comment"
      contains: "add_comment"
  key_links:
    - from: "src-tauri/src/commands/review.rs"
      to: "src-tauri/src/git/review_store.rs"
      via: "review_store::save_session (atomic tmp+rename)"
      pattern: "review_store::save_session"
    - from: "src-tauri/src/lib.rs"
      to: "src-tauri/src/commands/review.rs"
      via: "invoke_handler registration"
      pattern: "commands::review::add_comment"
---

<objective>
Add two thin, testable Tauri commands to the existing review command layer: a shared `add_comment` (submit) and `save_draft_comment` (keystroke), each backed by an `_inner` core under the session mutex, persisting through the Phase 65 atomic store. The `add_comment` core receives a fully-formed `Anchor` (source/side already set by the TS adapter), making it a dumb writer that Phase 68 reuses verbatim (L-08).

Purpose: persistence + concurrency safety for capture, reusing the verified Phase 65 store/RMW/emit machinery — no new persistence machinery (RESEARCH §Summary).
Output: `add_comment`/`save_draft_comment` (+ `_inner`) and tests in `review.rs`; registration in `lib.rs`.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-diff-source-anchor-capture/67-PATTERNS.md
@.planning/phases/67-diff-source-anchor-capture/67-RESEARCH.md

<interfaces>
<!-- Frozen Phase 65 schema (src-tauri/src/git/types.rs:295-336) — REUSE, do not redefine. -->
  enum Source { Diff, FullFile }            // PascalCase on wire, NO rename_all
  enum Side { Old, New }                     // PascalCase on wire, NO rename_all
  struct Anchor { commit_oid: String, file_path: String, source: Source, side: Side, start_line: u32, end_line: u32 }
  struct Comment { text: String, anchor: Option<Anchor>, cached_excerpt: Option<String> }
  struct DraftComment { text: String, anchor: Option<Anchor> }   // NOTE: NO cached_excerpt field
  struct ReviewSession { schema_version: u32, commits: Vec<String>, comments: Vec<Comment>, draft_comment: Option<DraftComment> }

<!-- Existing command/RMW/emit pattern to mirror (src-tauri/src/commands/review.rs). -->
  fn mutate_session_rmw<F>(data_dir, canonical, sessions: &Mutex<HashMap<PathBuf, ReviewSession>>, mutate: F) -> Result<(), TrunkError>
    where F: FnOnce(&mut Vec<String>)   // <-- generalize to FnOnce(&mut ReviewSession)
  // thin command: lock state map -> resolve_data_dir -> canonical_repo_path -> *_rmw -> app.emit("session-changed", canonical)
  fn resolve_data_dir(app) -> Result<PathBuf, String>
  fn canonical_repo_path(path, state_map) -> Result<PathBuf, TrunkError>
</interfaces>
</context>

<feature>
  <name>add_comment + save_draft_comment (shared dumb writers) + generalized RMW</name>
  <files>src-tauri/src/commands/review.rs, src-tauri/src/lib.rs</files>
  <read_first>
    - src-tauri/src/commands/review.rs (lines 304-320 mutate_session_rmw; 399-416 add_review_commit thin command + emit; 154-158 resolve_data_dir; 61-69 canonical_repo_path; 623-1018 #[cfg(test)] incl. selection_rmw_serialized:936-1006, rmw_missing_session_is_no_session_error:1008-1017, TestRepo fixtures 645-711). The EXACT analog for the command shape, RMW, emit, and tests.
    - src-tauri/src/git/types.rs (lines 295-336) — frozen Source/Side/Anchor/Comment/DraftComment/ReviewSession. REUSE; do NOT redefine. Note DraftComment has NO cached_excerpt.
    - src-tauri/src/git/review_store.rs — save_session/load_session (atomic tmp+rename, FNV-1a keyed). The persistence path; do NOT hand-roll a write.
    - src-tauri/src/lib.rs (lines 124-131) — invoke_handler review registration block.
    - .planning/phases/67-diff-source-anchor-capture/67-PATTERNS.md §"add_comment", §"save_draft_comment", §"Rust unit tests", §"lib.rs" — request DTO conventions, closure bodies, test assertion shapes.
    - .planning/phases/67-diff-source-anchor-capture/67-RESEARCH.md §"Pitfall 5" (draft has no excerpt) and §"Open Questions" Q3 (draft emit decision).
  </read_first>
  <behavior>
    `add_comment_inner(data_dir, canonical, sessions, req)`:
      Test 1 (SC-1): pushes a `Comment { text, anchor: Some(req.anchor), cached_excerpt: Some(req.cached_excerpt) }` onto `session.comments` and persists; reload via review_store::load_session -> LoadOutcome::Loaded(s) with `s.comments.len() == 1`.
      Test 2: sets `session.draft_comment = None` on submit (clears the single slot) — assert the reloaded session's draft is None even if one was present before.
      Test 3 (no-session): returns TrunkError with code "no_session" when no in-memory session for the canonical path (mirror rmw_missing_session_is_no_session_error).
      Test 4 (L-08): called with an `Anchor { source: Source::FullFile, .. }` persists it unchanged — reload and assert `anchor.source == Source::FullFile`. Locks the Phase-68-shareable contract.
      Test 5 (SC-2): a non-trivial Anchor (all six fields distinct, side=Old) round-trips through save_session->load_session with every field identical.
      Test 6 (concurrency): N concurrent add_comment_inner calls all survive (adapt selection_rmw_serialized) — `s.comments.len() == n` on disk.
      Test 7 (T-67-02 path traversal): an `anchor.file_path = "../../etc/passwd"` round-trips verbatim AND does not influence the on-disk session filename (the filename is the FNV-1a hash of the canonical repo path, never the anchor).
    `save_draft_comment_inner(data_dir, canonical, sessions, req)`:
      Test 8: writes/replaces `session.draft_comment = Some(DraftComment { text, anchor })` (no cached_excerpt — schema asymmetry) and persists; reload asserts the draft text/anchor.
      Test 9: no-session -> "no_session" error.
  </behavior>
  <action>
    RED first: in `review.rs` `#[cfg(test)]`, add Tests 1-9 above using the existing `TempDir` + seeded-session-in-map pattern (selection_rmw_serialized:940-952 — a git repo is not required, just a data dir + a session in the sessions map). Run `just cargo-test review` and confirm failures (functions absent).

    GREEN — implementation:
    1. Generalize `mutate_session_rmw`: change the trait bound from `FnOnce(&mut Vec<String>)` to `FnOnce(&mut ReviewSession)` and call `mutate(session)` instead of `mutate(&mut session.commits)`. Update ALL THREE existing callers' closures (review.rs:330, 342, 354 — verified count) so each takes `&mut ReviewSession` and mutates `session.commits` inside (they currently take `|commits|`). Do NOT write a sibling RMW function (PATTERNS §Shared Patterns).
    2. Add request DTOs with `#[serde(rename_all = "camelCase")]`: `AddCommentRequest { path: String, text: String, anchor: Anchor, cached_excerpt: String }` (wire key cachedExcerpt) and `SaveDraftCommentRequest { path: String, text: String, anchor: Option<Anchor> }`. Persisted Anchor/Comment keep snake_case + PascalCase enums (frozen).
    3. `add_comment_inner`: under the generalized RMW closure, `session.comments.push(Comment { text, anchor: Some(anchor), cached_excerpt: Some(cached_excerpt) })` then `session.draft_comment = None`. `save_draft_comment_inner`: closure sets `session.draft_comment = Some(DraftComment { text, anchor })` — write ONLY text+anchor (DraftComment has no cached_excerpt field — Pitfall 5).
    4. Thin `#[tauri::command] add_comment`: mirror `add_review_commit` (lock state map -> resolve_data_dir -> canonical_repo_path -> add_comment_inner -> `app.emit("session-changed", canonical.to_string_lossy().into_owned())`). Thin `save_draft_comment`: SAME minus the emit — `save_draft_comment` does NOT emit session-changed (RESEARCH Q3 decision: drafts aren't panel-visible in Phase 67/69-stub; per-keystroke emits cause reload storms).
    5. Register both in `lib.rs` invoke_handler after the existing `commands::review::list_session_commits` line: `commands::review::add_comment, commands::review::save_draft_comment,`.

    REFACTOR (if needed): dedupe the two thin commands' canonical-resolution prelude if it reads cleanly; keep tests green.

    Each phase is an atomic commit (test(67-02)/feat(67-02)/refactor(67-02)) per @/Users/joaofnds/code/trunk/.claude/get-shit-done/references/tdd.md.
  </action>
  <verify>
    <automated>just cargo-test review</automated>
  </verify>
  <acceptance_criteria>
    - `just cargo-test review` passes Tests 1-9 (maps to 67-VALIDATION.md SC-1 Rust row, SC-2 Rust round-trip row, SC-3 Rust Source::FullFile row).
    - Test 4 explicitly persists `Source::FullFile` and reloads it unchanged (L-08 contract locked).
    - Test 7 asserts `anchor.file_path = "../../etc/passwd"` round-trips verbatim AND the session file on disk is the FNV-1a hash of the canonical path (path-traversal mitigation T-67-02).
    - Source assertion: `grep -c "fn mutate_session_rmw" src-tauri/src/commands/review.rs` returns 1 (generalized, not cloned).
    - Source assertion: `grep -c "app.emit(\"session-changed\"" src-tauri/src/commands/review.rs` — the count increases by exactly 1 over baseline (add_comment emits; save_draft_comment does not). Confirm save_draft_comment's body has no emit.
    - `grep -c "commands::review::add_comment\|commands::review::save_draft_comment" src-tauri/src/lib.rs` returns 2 (both registered).
    - `just check` (backend portion: clippy + cargo-test) green before commit.
  </acceptance_criteria>
  <done>
    Both commands exist over testable `_inner` cores, persist via the atomic store, RMW is generalized (one function), add_comment clears the draft slot and emits, save_draft_comment does not emit, and the L-08 + path-traversal contracts are locked by Rust tests.
  </done>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend -> Tauri command | User-controlled `text`, `anchor.file_path`, `anchor.commit_oid` cross into the Rust persistence layer and are written to the per-repo JSON store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Tampering | `add_comment`/`save_draft_comment` writing user text to JSON store | mitigate | Carry forward Phase 65 store guarantees: atomic tmp+sync_all+rename (D-10), corrupt-file quarantine (D-15), schema_version refusal (D-16). Text is persisted as serde JSON (escaped); no injection sink in this phase (markdown render escaping is Phase 70). |
| T-67-02 | Tampering | path traversal via `anchor.file_path` | mitigate | The on-disk session filename is the FNV-1a hash of the canonical repo path (review_store.rs, D-11); `anchor.file_path` is metadata only and never constructs a filesystem path. Locked by a Rust test asserting `"../../etc/passwd"` round-trips verbatim and does not affect the disk filename. |
| T-67-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase (RESEARCH §Package Legitimacy Audit — table omitted, none installed). No supply-chain surface introduced. |
</threat_model>

<verification>
- `just cargo-test review` green (SC-1 Rust, SC-2 round-trip, SC-3 FullFile).
- RMW-generalization source assertion (single function).
- Emit-divergence source assertion (add_comment emits, save_draft_comment does not).
- Path-traversal Rust test (T-67-02).
</verification>

<success_criteria>
- SC-1 (Rust): add_comment_inner pushes a Comment with anchor+excerpt, clears draft_comment, persists.
- SC-2 (Rust): store round-trip preserves all six anchor fields identically (no array-index field exists that could shift).
- SC-3 (Rust): add_comment_inner accepts Source::FullFile (proves L-08 sharing).
</success_criteria>

<output>
Create `.planning/phases/67-diff-source-anchor-capture/67-02-SUMMARY.md` when done.
</output>
