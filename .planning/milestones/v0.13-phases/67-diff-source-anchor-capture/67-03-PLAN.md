---
phase: 67-diff-source-anchor-capture
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RepoView.svelte
  - src/components/DiffPanel.svelte
  - src/components/diff/HunkView.svelte
  - src/components/diff/SplitView.svelte
  - src/lib/types.ts
autonomous: true
requirements: [ANCH-01]

must_haves:
  truths:
    - "commitDetail is threaded through to the commit DiffPanel (RepoView.svelte:732 no longer hardcodes null) so the adapter has commit_oid and merge detection"
    - "the diffKind === 'commit' line-selection guard is lifted at all THREE sites (DiffPanel.svelte:307, HunkView.svelte:303, SplitView.svelte:323) as one logical change — selection works in commit diffs while the Context guard stays"
    - "L-07: merge detection is available via commitDetail.parent_oids.length > 1 (consumed by Plan 04's D-04 disable); staging buttons remain ABSENT in commit diffs (no commit branch added to staging toolbars)"
    - "the two camelCase request DTOs (AddCommentRequest, SaveDraftCommentRequest) are declared in types.ts; the existing review schema types (Anchor/Comment/DraftComment/Source/Side) are NOT redeclared"
    - "split view commit selection stays Add-only (right column) by construction (RESEARCH Pitfall 2) — the guard is lifted but the origin === 'Add' constraint remains"
  artifacts:
    - path: "src/components/RepoView.svelte"
      provides: "commitDetail prop threaded to the commit DiffPanel"
      contains: "commitDetail={commitDetail}"
    - path: "src/components/DiffPanel.svelte"
      provides: "handleLineClick commit guard lifted; commitDetail prop available"
      contains: "handleLineClick"
    - path: "src/components/diff/HunkView.svelte"
      provides: "isSelectable allows commit diffs (Context still excluded)"
      contains: "isSelectable"
    - path: "src/components/diff/SplitView.svelte"
      provides: "isSelectable allows commit diffs (right-col Add-only)"
      contains: "isSelectable"
    - path: "src/lib/types.ts"
      provides: "AddCommentRequest + SaveDraftCommentRequest DTOs"
      contains: "AddCommentRequest"
  key_links:
    - from: "src/components/RepoView.svelte"
      to: "src/components/DiffPanel.svelte"
      via: "commitDetail prop pass-through"
      pattern: "commitDetail=\\{commitDetail\\}"
---

<objective>
Close the plumbing gaps that make capture possible: thread the already-loaded `commitDetail` into the commit `DiffPanel` (currently hardcoded `null`), lift the commit-diff line-selection guard at the three sites that disable it, and declare the two request DTOs the composer (Plan 04) will send. No composer/affordance UI yet — that is Plan 04.

Purpose: the adapter needs `commit_oid` + merge info from `commitDetail`; selection must work in commit diffs before a Comment button can use it (RESEARCH §Summary "the hard parts are plumbing gaps").
Output: edited RepoView/DiffPanel/HunkView/SplitView + DTOs in types.ts.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-diff-source-anchor-capture/67-PATTERNS.md
@.planning/phases/67-diff-source-anchor-capture/67-RESEARCH.md

<interfaces>
<!-- Verified guard sites and the threading target (RESEARCH §Pattern 3, §Pitfall 1, all line-checked). -->
  DiffPanel.svelte:307   handleLineClick: `if (diffKind === "commit") return;`  <- LIFT (keep the `origin === "Context"` guard above it)
  HunkView.svelte:303    `{@const isSelectable = diffKind !== 'commit' && line.origin !== 'Context'}`  <- drop the diffKind clause
  SplitView.svelte:323   `{@const isSelectable = diffKind !== 'commit' && line.origin === 'Add'}`  <- drop the diffKind clause (Add-only stays)
  RepoView.svelte:732    `commitDetail={null}` on the commit DiffPanel  <- pass the existing `commitDetail` $state (loaded at RepoView:302-313)
  DiffPanel.svelte:33,51 commitDetail is ALREADY a declared+destructured prop (Props.commitDetail: CommitDetail | null)

<!-- Existing camelCase request convention (src/lib/types.ts:178-182 DiffRequestOptions). Review schema types already exist at types.ts:285-316 — REUSE. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread commitDetail + lift the three commit-diff selection guards</name>
  <files>src/components/RepoView.svelte, src/components/DiffPanel.svelte, src/components/diff/HunkView.svelte, src/components/diff/SplitView.svelte</files>
  <read_first>
    - src/components/RepoView.svelte (lines 285-318 commitDetail load in handleCommitSelect; 730-774 the commit DiffPanel instantiation with commitDetail={null} at 732). The threading target.
    - src/components/DiffPanel.svelte (lines 33, 51 commitDetail prop decl/destructure; 298-339 handleLineClick incl. the 307 commit guard and the 318-328 shift-click contiguous-skip-Context selection model). Lift ONLY the diffKind guard; keep the Context guard.
    - src/components/diff/HunkView.svelte (lines 172-298 toolbar diffKind branches; 301-305 isSelectable guard at 303). Lift the diffKind clause only.
    - src/components/diff/SplitView.svelte (lines 230-340 split-hunk-header toolbar; 321-325 isSelectable at 323). Lift the diffKind clause; keep origin === 'Add'.
    - .planning/phases/67-diff-source-anchor-capture/67-PATTERNS.md §"DiffPanel.svelte", §"HunkView.svelte", §"SplitView.svelte", §"RepoView.svelte" — exact excerpts.
    - .planning/phases/67-diff-source-anchor-capture/67-RESEARCH.md §"Pattern 3" (one logical change, three files) and §"Pitfall 2" (split view stays Add-only).
  </read_first>
  <action>
    In RepoView.svelte:732, replace `commitDetail={null}` with `commitDetail={commitDetail}` (the existing RepoView `$state` loaded in handleCommitSelect at ~302-313). In DiffPanel.svelte handleLineClick (~307), remove the `if (diffKind === "commit") return;` line while keeping the preceding `if (origin === "Context") return;` — the existing selectedHunkKey/selectedLineIndices model below works unchanged for commit diffs. In HunkView.svelte:303, change `isSelectable` to `line.origin !== 'Context'` (drop the diffKind clause). In SplitView.svelte:323, change `isSelectable` to `line.origin === 'Add'` (drop the diffKind clause; right-column Add-only stays per Pitfall 2). Do NOT add any 'commit' branch to the staging toolbars (HunkView:172/254, SplitView:237/271) — staging buttons must stay absent in commit diffs; the Comment affordance is Plan 04.
  </action>
  <verify>
    <automated>just vitest src/components/DiffPanel.test.ts && just svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "commitDetail={null}" src/components/RepoView.svelte` returns 0 (threading done).
    - Source assertion: `grep -c "diffKind === \"commit\") return" src/components/DiffPanel.svelte` returns 0 (guard lifted) while `grep -c "origin === \"Context\") return" src/components/DiffPanel.svelte` returns 1 (Context guard kept).
    - Source assertion: HunkView.svelte:303 isSelectable no longer references diffKind; SplitView.svelte:323 still requires `origin === 'Add'` and no longer references diffKind.
    - Source assertion: `grep -c "diffKind === 'commit'" src/components/diff/HunkView.svelte` reflects no staging-toolbar commit branch added (only Plan 04 adds the Comment branch).
    - `just svelte-check` passes (no type regression from commitDetail threading).
    - Existing DiffPanel.test.ts still green (no behavior regression). [maps to 67-VALIDATION.md SC-1 component guard-lift row, fully exercised in Plan 04]
  </acceptance_criteria>
  <done>
    commitDetail flows to the commit DiffPanel, line selection works in commit diffs at all three render paths (Context still excluded, split-view still Add-only), staging buttons remain absent, and svelte-check is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Declare the two request DTOs in types.ts</name>
  <files>src/lib/types.ts</files>
  <read_first>
    - src/lib/types.ts (lines 178-182 DiffRequestOptions — the existing camelCase request DTO convention; 285-316 the EXISTING review schema types Anchor/Comment/DraftComment/Source/Side — REUSE, do NOT redeclare).
    - .planning/phases/67-diff-source-anchor-capture/67-PATTERNS.md §"src/lib/types.ts" — the correction that the schema types already exist and only the two request DTOs are net-new.
  </read_first>
  <action>
    Add `AddCommentRequest { path: string; text: string; anchor: Anchor; cachedExcerpt: string }` and `SaveDraftCommentRequest { path: string; text: string; anchor: Anchor | null }` to types.ts, reusing the existing `Anchor` type (do NOT redeclare Anchor/Comment/DraftComment/Source/Side). Use camelCase keys (`cachedExcerpt`) mirroring the Rust `#[serde(rename_all = "camelCase")]` request structs from Plan 02.
  </action>
  <verify>
    <automated>just svelte-check</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "interface AddCommentRequest\|interface SaveDraftCommentRequest" src/lib/types.ts` returns 2.
    - Source assertion: `grep -c "cachedExcerpt" src/lib/types.ts` >= 1 (camelCase wire key).
    - Source assertion: no new `interface Anchor`/`type Side =`/`type Source =` added (`grep -c "interface Anchor" src/lib/types.ts` stays 1 — the existing one).
    - `just svelte-check` passes.
  </acceptance_criteria>
  <done>
    Both request DTOs exist with camelCase wire keys, reuse the frozen Anchor type, and svelte-check is clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure UI wiring + type declarations. No new input sink, no IPC call introduced (the composer that calls the commands is Plan 04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03 | Information Disclosure / Tampering | guard-lift enabling selection in commit diffs | accept | Lifting the selection guard exposes no new data — the diff is already rendered; selection is client-only state. Staging actions are NOT enabled (no commit branch added to staging toolbars). No new persistence or privilege crosses a boundary in this plan. |
</threat_model>

<verification>
- `just svelte-check` clean across all four edited components + types.ts.
- `just vitest src/components/DiffPanel.test.ts` green (no regression).
- Guard-lift source assertions (three sites) and commitDetail threading assertion.
</verification>

<success_criteria>
- SC-1 (enabler): line selection functions in commit diffs after the guard lift (verified end-to-end in Plan 04's component tests).
- L-07 enabler: commitDetail.parent_oids is available to Plan 04 for the merge-commit disable.
</success_criteria>

<output>
Create `.planning/phases/67-diff-source-anchor-capture/67-03-SUMMARY.md` when done.
</output>
