---
phase: 68-full-file-source-anchor-capture
plan: 02
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - src/components/diff/FullFileView.svelte
  - src/components/diff/FullFileView.test.ts
  - src/components/diff/DiffViewer.svelte
  - src/components/DiffPanel.svelte
  - src/components/diff/CommentComposer.svelte
autonomous: false

requirements: [ANCH-02]

user_setup: []

must_haves:
  truths:
    - "An unchanged/zero-hunk file exposes no Comment affordance and never throws (V5)"
    - "Click sets a single-line selection; shift-click extends a contiguous span; only new-side lines (new_lineno != null) are valid selection endpoints (V6)"
    - "Submitting the composer calls the shared, already-shipped add_comment writer with a FullFile anchor + plain excerpt (V7, L-02)"
    - "The in-progress draft persists on change via the existing save_draft_comment with 300ms debounce (V8, L-03)"
    - "Merge commits keep the full-file Comment affordance ENABLED — HunkView's isMerge disable is NOT copied here (V10, L-05)"
  artifacts:
    - path: "src/components/diff/FullFileView.svelte"
      provides: "Net-new contiguous selection state, isSelected highlight, and the Comment affordance for the full-file view"
      contains: "new_lineno"
    - path: "src/components/diff/FullFileView.test.ts"
      provides: "V5, V6, V10 component coverage"
      contains: "FullFileView"
    - path: "src/components/DiffPanel.svelte"
      provides: "Full-file composer host: full-file selection ownership, ensureActiveSession reuse, threading commitOid/repoPath, mounting CommentComposer with injected captured result"
      contains: "buildFullFileAnchor"
  key_links:
    - from: "src/components/DiffPanel.svelte"
      to: "src/lib/full-file-anchor.ts"
      via: "import buildFullFileAnchor for the full-file captured result"
      pattern: "buildFullFileAnchor"
    - from: "src/components/diff/FullFileView.svelte"
      to: "DiffPanel composer host"
      via: "oncommentlines-equivalent callback bubbling the full-file selection up"
      pattern: "oncomment"
    - from: "src/components/diff/CommentComposer.svelte"
      to: "add_comment / save_draft_comment"
      via: "safeInvoke with the injected captured FullFile anchor + excerpt"
      pattern: "add_comment"
---

<objective>
Wire the full-file capture path end to end: add net-new contiguous click /
shift-click selection state and a Comment affordance to `FullFileView`, thread
the selection + `commitOid`/`repoPath` through `DiffViewer` to the `DiffPanel`
host, and reuse the existing inline `CommentComposer` (adapted to accept an
injected captured result) to persist a `FullFile`-source comment via the
already-shipped `add_comment` / `save_draft_comment` writers. The comment
survives an app restart.

Purpose: Delivers ANCH-02 — the user can select a line range in the
full-file-at-commit view and attach a comment anchored to absolute blob line
numbers. Consumes the pure adapter from 68-01.
Output: full-file selection + affordance + composer wiring across FullFileView,
DiffViewer, DiffPanel, and CommentComposer, plus FullFileView.test.ts coverage.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-full-file-source-anchor-capture/68-CONTEXT.md
@.planning/phases/68-full-file-source-anchor-capture/68-RESEARCH.md
@.planning/phases/68-full-file-source-anchor-capture/68-VALIDATION.md
@.planning/phases/68-full-file-source-anchor-capture/68-01-SUMMARY.md

# The capture surface (no selection today), the threading layer, the host, the composer:
@src/components/diff/FullFileView.svelte
@src/components/diff/DiffViewer.svelte
@src/components/DiffPanel.svelte
@src/components/diff/CommentComposer.svelte
@src/components/diff/CommentComposer.test.ts

<interfaces>
<!-- Key landmarks the executor needs. Extracted from the codebase. -->

Adapter produced by 68-01 (src/lib/full-file-anchor.ts):
```typescript
export function buildFullFileAnchor(
  commitOid: string,
  file: FileDiff,
  selectedIndices: Set<number>,   // indices into file.hunks.flatMap(h => h.lines)
): { anchor: Anchor; cachedExcerpt: string };
```

DiffPanel host state already present (src/components/DiffPanel.svelte):
```
const commitOid  = $derived(commitDetail?.oid ?? "");            // line 83
const isMerge    = $derived((commitDetail?.parent_oids.length ?? 0) > 1);  // line 84 — DO NOT gate the full-file affordance on this (L-05)
let composerOpen, composerFilePath, composerHunkIdx, composer;   // lines 85-88
async function ensureActiveSession(): Promise<boolean>            // lines 108-140 — REUSE verbatim for full-file
async function handleCommentLines(filePath, hunkIndex)            // lines 142-149 — has `if (isMerge) return;` guard (diff path); full-file analog must NOT have it
// repoPath is a DiffPanel prop (line 39/55); commitOid derives from commitDetail
```

DiffViewer full-file invocation today (src/components/diff/DiffViewer.svelte:117-118):
```
{:else if layoutMode === "inline" && contentMode === "full"}
  <FullFileView {fileDiffs} {showInvisibles} {wordWrap} />   // must be extended to thread selection + comment props
```

CommentComposer current coupling (src/components/diff/CommentComposer.svelte):
```
// props: file, hunkIdx, selectedLineIndices, commitOid, repoPath, onclose
const captured = $derived(buildDiffAnchor(commitOid, file, hunkIdx, selectedLineIndices));  // line 34
// submit -> add_comment { path, text, anchor: captured.anchor, cachedExcerpt: captured.cachedExcerpt }  (lines 73-78)
// 300ms debounce -> save_draft_comment { path, text, anchor: captured.anchor }  (DRAFT_DEBOUNCE_MS=300, lines 28/40-59)
// confirmDiscardIfDirty() instance method (lines 93-100)
```

HunkView affordance (the parallel, with the disable to NOT copy) — src/components/diff/HunkView.svelte:305-325:
```
<button disabled={isMerge} title={isMerge ? "Diff comments aren't available on merge commits" : ""} ...>Comment ({selectedCount})</button>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Adapt CommentComposer to accept an injected captured result</name>
  <files>src/components/diff/CommentComposer.svelte, src/components/diff/CommentComposer.test.ts</files>
  <read_first>
    - src/components/diff/CommentComposer.svelte (current props + `captured = $derived(buildDiffAnchor(...))` at line 34, submit/draft wiring lines 48-100)
    - src/components/diff/CommentComposer.test.ts (mock-safeInvoke pattern to extend)
    - src/lib/full-file-anchor.ts (the injected result shape from 68-01)
  </read_first>
  <behavior>
    - When a `captured` prop ({ anchor, cachedExcerpt }) is provided, the composer
      uses it directly for the preview, save_draft_comment, and add_comment —
      it does NOT call buildDiffAnchor.
    - When `captured` is absent (existing diff path), behavior is unchanged:
      `captured` falls back to `buildDiffAnchor(commitOid, file, hunkIdx, selectedLineIndices)`.
    - submit still disabled on empty text; confirmDiscardIfDirty still works.
  </behavior>
  <action>
    Add an optional prop `captured?: { anchor: Anchor; cachedExcerpt: string }`
    to `CommentComposer`. Change the internal `captured` reference to a derived
    value that returns the injected prop when present, else falls back to
    `buildDiffAnchor(commitOid, file, hunkIdx, selectedLineIndices)`. Rename the
    local `$derived` if needed to avoid shadowing the prop (e.g. internal
    `capturedResult`). Make `file`/`hunkIdx`/`selectedLineIndices` optional/
    non-required when `captured` is supplied (the full-file host passes
    `captured` + `commitOid` + `repoPath` only). Leave the submit/draft/discard
    logic untouched — it already reads from the resolved captured value. This is
    the lower-coupling seam chosen over a `source` mode (RESEARCH §2.2). Extend
    `CommentComposer.test.ts` with a case that renders with an injected
    `captured` FullFile result and asserts submit calls `add_comment` once with
    `anchor.source === "FullFile"` and the injected `cachedExcerpt` (V7), and a
    fake-timer case asserting `save_draft_comment` fires ~300ms after input with
    the injected anchor (V8).
  </action>
  <verify>
    <automated>bun run test</automated>
  </verify>
  <acceptance_criteria>
    - `bun run test` passes including the new injected-`captured` cases.
    - V7: a test asserts `add_comment` is invoked exactly once on submit with `anchor.source === "FullFile"` and the injected `cachedExcerpt` (mock safeInvoke).
    - V8: a fake-timers test asserts `save_draft_comment` is called ~300ms after input with the injected anchor.
    - The existing diff-path tests in CommentComposer.test.ts still pass (no regression on the buildDiffAnchor fallback).
    - `svelte-check` (via `just check`) reports no new type errors for the optional props.
  </acceptance_criteria>
  <done>CommentComposer accepts an injected captured result, uses it for draft + submit, and the diff-path fallback is intact; V7/V8 are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add full-file selection + Comment affordance to FullFileView; thread props through DiffViewer; host in DiffPanel</name>
  <files>src/components/diff/FullFileView.svelte, src/components/diff/FullFileView.test.ts, src/components/diff/DiffViewer.svelte, src/components/DiffPanel.svelte</files>
  <read_first>
    - src/components/diff/FullFileView.svelte (flat `allLines = fd.hunks.flatMap(h => h.lines)` line 49; `lineBackground(origin)` line 16 needs an isSelected arg)
    - src/components/diff/HunkView.svelte:302-349 (the affordance + isSelectable pattern to parallel WITHOUT the isMerge disable)
    - src/components/diff/DiffViewer.svelte:117-118 (FullFileView invocation to extend)
    - src/components/DiffPanel.svelte:80-149 and 519-557 (composer host state, ensureActiveSession, handleCommentLines, DiffViewer invocation, composer mount block)
    - src/lib/full-file-anchor.ts (buildFullFileAnchor signature from 68-01)
    - .planning/phases/68-full-file-source-anchor-capture/68-VALIDATION.md (V5, V6, V10 rows)
  </read_first>
  <behavior>
    - V5: with empty `allLines` (zero-hunk/unchanged file) FullFileView renders no Comment button and throws nothing.
    - V6: a click sets a single-line selection; a subsequent shift-click extends a contiguous span; a line with `new_lineno === null` (Delete) is NOT a valid selection endpoint (clicking it is a no-op for endpoints).
    - V10: when `isMerge === true` the full-file Comment affordance button is present and NOT disabled (the HunkView isMerge disable is not copied) — L-05.
  </behavior>
  <action>
    FullFileView: add net-new contiguous selection state (D-01) — track
    `{ anchorIndex, focusIndex }` (or anchor/focus new_lineno) over the flat
    `allLines`, deriving the contiguous span; `isSelectable = line.new_lineno !== null`
    (D-02 — new-side lines only; Delete lines pass over but are not endpoints).
    A plain click sets a single-line selection; shift-click extends to a
    contiguous span. Extend `lineBackground` to take an `isSelected` arg and use
    a theme `--color-*` selection background var (never an inline color). Render
    a floating "Comment ({count})" affordance when a selection exists and
    `diffKind === 'commit'`, mirroring HunkView but WITHOUT `disabled={isMerge}`
    and without the merge title (L-05). Accept new props
    (commitOid, repoPath, diffKind, isMerge, and a bubbling callback such as
    `oncommentfullfile(filePath, selectedIndices)`); the affordance fires the
    callback with the file path + the flat selected indices.

    DiffViewer: extend the `contentMode === "full"` branch (line 118) to thread
    the new props/callbacks down to FullFileView (commitOid, repoPath, diffKind,
    isMerge, selection callback). Add the corresponding props to DiffViewer's
    Props interface and pass-through.

    DiffPanel: own full-file selection at the host (mirror the diff path's
    parent ownership) — add `fullFileSelectedIndices` state + a
    `handleCommentFullFile(filePath, indices)` analog of `handleCommentLines`
    that calls the SAME `ensureActiveSession()` (reuse verbatim) but has NO
    `if (isMerge) return` guard (L-05). On confirm, compute the captured result
    via `buildFullFileAnchor(commitOid, composerFile, indices)` and mount the
    existing `CommentComposer` passing the injected `captured` result plus
    `commitOid` + `repoPath` (thread both down — RESEARCH Pitfall 4). Reuse
    `closeComposer`/`confirmDiscardIfDirty` and the `session-changed`-driven
    panel reload for silent-success feedback; clear the full-file selection +
    composer on submit. Extend the existing `clearSelection()` (DiffPanel
    lines 212-216, called by handleContentModeChange/handleLayoutModeChange/
    Escape) to ALSO reset the new full-file selection state (anchor/focus
    indices) so it never goes stale across mode/layout toggles. Persist immediately on attach via add_comment and the
    draft on change via save_draft_comment — both already shipped (L-02), draft
    debounce per L-03; the anchor stores absolute blob line numbers only (L-03).

    Create `FullFileView.test.ts` (clone CommentComposer.test.ts's mock-safeInvoke
    setup) covering V5 (empty file → no affordance, no throw), V6 (click +
    shift-click contiguous span; Delete line is not an endpoint), and V10
    (isMerge=true → affordance button present and not disabled).
  </action>
  <verify>
    <automated>bunx vitest run src/components/diff/FullFileView.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bunx vitest run src/components/diff/FullFileView.test.ts` passes V5, V6, V10.
    - V10 assertion is explicit: with `isMerge=true` the Comment affordance button exists and `disabled` is false/absent (asserts the disable was NOT copied — L-05). Fail-closed gate: `test "$(grep -c 'disabled={isMerge}' src/components/diff/FullFileView.svelte)" = "0"` (the HunkView merge-disable must NOT appear).
    - `grep -n "buildFullFileAnchor" src/components/DiffPanel.svelte` shows the host computes the captured result via the 68-01 adapter.
    - D-02 enforced by the V6 component test (clicking a Delete line, new_lineno=null, is not a valid endpoint) plus a fail-closed gate that selection logic never keys off old_lineno: `test "$(grep -c '\.old_lineno' src/components/diff/FullFileView.svelte)" -le "2"` (old_lineno appears only in the two gutter spans, never in selection/endpoint code).
    - The full-file selection highlight uses a `--color-*` theme var (no inline hex/rgb in `lineBackground`).
    - `just check` passes (fmt, biome, svelte-check, clippy, cargo-test, vitest).
    - Commit: `feat(68-02): full-file selection, affordance, and composer wiring`.
  </acceptance_criteria>
  <done>FullFileView has contiguous new-side-only selection + a non-merge-disabled Comment affordance; DiffViewer threads the props; DiffPanel hosts the composer with an injected FullFile captured result; V5/V6/V10 are green and the L-05/D-02 grep gates hold.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verifies the full-file attach flow and restart survival</name>
  <files>(none — verification only)</files>
  <action>
    Checkpoint: pause for the human to verify the full-file attach flow built in
    Task 2. Claude has already automated selection, the affordance, the composer,
    and persistence; this task only confirms visual/functional fidelity and
    restart survival that unit/component tests cannot assert. Present the steps in
    <how-to-verify> and wait for the resume signal.
  </action>
  <what-built>
    Click + shift-click line selection in the full-file-at-commit view, an inline
    Comment affordance + composer that persists a FullFile-source comment via the
    shared add_comment/save_draft_comment writers (V9 round-trip persistence is
    already covered by the existing passing Rust test).
  </what-built>
  <read_first>
    - .planning/phases/68-full-file-source-anchor-capture/68-VALIDATION.md (Manual-Only Verifications table; V9 row)
    - src-tauri/src/commands/review.rs:1255-1277 (existing add_comment_persists_full_file_source_unchanged test)
  </read_first>
  <how-to-verify>
    1. Run `just dev`.
    2. Open a repo, select a commit, switch the diff to full-file content mode (inline layout).
    3. Click a line, then shift-click a line further down — confirm a contiguous highlight using a theme color (not an inline color), and that clicking a red Delete line is not a valid selection endpoint.
    4. Click "Comment (N)", type a comment, confirm the composer shows the line range, and Submit.
    5. Repeat steps 2-4 on a MERGE commit — confirm the Comment affordance is ENABLED (not greyed out) and a comment can be attached (L-05).
    6. Quit and relaunch the app; reopen the same commit and confirm the attached comment still exists (restart survival / ANCH-02 success criterion 2).
    7. Confirm `cargo test --manifest-path src-tauri/Cargo.toml add_comment_persists_full_file_source_unchanged` passes (V9 store round-trip).
  </how-to-verify>
  <acceptance_criteria>
    - Contiguous selection highlights correctly with a theme `--color-*` var; Delete lines are not endpoints (D-02).
    - The affordance is enabled on a merge commit and a comment attaches (L-05).
    - The attached full-file comment survives an app restart (ANCH-02 criterion 2).
    - `cargo test --manifest-path src-tauri/Cargo.toml add_comment_persists_full_file_source_unchanged` passes (V9, L-01).
  </acceptance_criteria>
  <verify>
    <automated>cargo test --manifest-path src-tauri/Cargo.toml add_comment_persists_full_file_source_unchanged</automated>
  </verify>
  <done>Human confirms the contiguous selection highlight (theme color), merge-commit affordance enabled (L-05), and the comment survives an app restart (ANCH-02); the V9 round-trip test passes.</done>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer (FullFileView/DiffPanel) -> Tauri command | User-selected line range + comment text crosses to add_comment/save_draft_comment. These writers are already shipped and hardened (Phase 65 store: atomic write, FNV-1a path-hash, corrupt-file quarantine; Phase 67 L-08 dumb-writer contract). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02 | Tampering | add_comment payload (anchor source/side/coords) | accept | Anchor is built client-side by the pure buildFullFileAnchor (68-01) with constant source="FullFile"/side="New"; the backend persists verbatim (no privilege boundary — local single-user app). Covered by V7 + the existing review.rs round-trip test. |
| T-68-02-I | Information disclosure | cached excerpt content | accept | Excerpt is the user's own repo content already on screen; no new exposure. |

No new IPC command and no package-manager installs in this plan -> T-68-SC (supply-chain) omitted.
</threat_model>

<verification>
- V5/V6/V10: `bunx vitest run src/components/diff/FullFileView.test.ts`.
- V7/V8: `bun run test` (CommentComposer injected-captured cases).
- V9: `cargo test --manifest-path src-tauri/Cargo.toml add_comment_persists_full_file_source_unchanged` (already-shipped round-trip; truth satisfied by existing coverage, L-01/L-03/L-04).
- L-05 / D-02 grep gates on FullFileView.svelte hold.
- Full suite: `just check` is green.
- Manual: human-verify checkpoint (attach + merge-commit + restart survival).
</verification>

<success_criteria>
- A user can click + shift-click a contiguous line range in the full-file-at-commit
  view (new-side lines only) and attach a comment (ANCH-02 criterion 1).
- The comment persists via the shared add_comment writer with a FullFile/New
  anchor storing absolute blob line numbers, the draft persists via
  save_draft_comment (L-02, L-03), the affordance is valid on merge commits
  (L-05), and the comment survives an app restart (ANCH-02 criterion 2).
- V5, V6, V7, V8, V10 are green; V9 confirmed via the existing Rust round-trip.
</success_criteria>

<output>
Create `.planning/phases/68-full-file-source-anchor-capture/68-02-SUMMARY.md` when done.
</output>
