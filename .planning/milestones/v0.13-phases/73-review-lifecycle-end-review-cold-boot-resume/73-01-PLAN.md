---
phase: 73-review-lifecycle-end-review-cold-boot-resume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ReviewPanel.svelte
  - src/components/ReviewPanel.test.ts
autonomous: true
requirements:
  - REQ-73-RESUME
  - REQ-73-NYQUIST
  - REQ-73-CHECK
tags:
  - svelte
  - tauri-ipc
  - review-session
  - lifecycle

must_haves:
  truths:
    - "Cold boot (status.state === 'resume-available') triggers exactly one call to resume_review_session inside reload(), before the parallel list reads."
    - "When status.state === 'active' or 'none', resume_review_session is never called."
    - "A resume_review_session rejection surfaces a showToast(..., 'error') with the extracted message; the list reads still run (and the no_session catch arm renders the cold empty data state)."
    - "canonicalPath is assigned from status.canonical_path BEFORE the resume call, preserving the WR-02 session-changed listener filter invariant."
    - "The test harness can drive resume_review_session resolutions and rejections via installReads, and the captured session-changed listener callback is exposed for Plan 03."
  artifacts:
    - path: "src/components/ReviewPanel.svelte"
      provides: "reload() with cold-boot resume branch + sessionState local rune for Plan 03 to consume"
      contains: "status.state === \"resume-available\""
    - path: "src/components/ReviewPanel.test.ts"
      provides: "installReads dispatcher handling get_review_session_status / resume_review_session / end_review_session; captured listen callback; new describe(\"cold-boot resume\") block"
      contains: "describe(\"cold-boot resume\""
  key_links:
    - from: "src/components/ReviewPanel.svelte:reload()"
      to: "safeInvoke(\"resume_review_session\", { path })"
      via: "branch on status.state === \"resume-available\" (D-01)"
      pattern: "status\\.state === \"resume-available\""
    - from: "src/components/ReviewPanel.svelte:reload() catch arm"
      to: "showToast(errorMessage(e, \"Failed to resume review\"), \"error\")"
      via: "D-07 LoadOutcome surfacing — TrunkError(\"newer_version\") and arbitrary failures both land here"
      pattern: "errorMessage\\(e, \"Failed to resume review\"\\)"
---

<objective>
Wire cold-boot resume into `ReviewPanel.reload()` so opening the panel on a repo with an on-disk session loads its comments without requiring a mutation (closes Bug 3 from 72-VERIFICATION.md). Also extend the test harness with the three new IPC mocks (`get_review_session_status`, `resume_review_session`, `end_review_session`) and a captured `session-changed` listener so Plans 02 and 03 can build on it.

Purpose: closes REQ-73-RESUME (and the test-infrastructure half of REQ-73-NYQUIST) by activating the existing backend lifecycle primitives — no Rust changes; pure frontend wiring at the existing reload chokepoint.

Output:
- `installReads` extended with `status?: SessionStatus`, `resumeRejection?: unknown`, `endRejection?: unknown` and the three new command cases.
- `@tauri-apps/api/event` mock upgraded to capture the listener callback into a test-visible variable.
- `reload()` modified to branch on `status.state === "resume-available"` and call `resume_review_session` exactly once on a cold boot, with `errorMessage`-based toast on rejection.
- `sessionState` local `$state<SessionState>` initialized inside `reload()` from `status.state` (consumed by Plans 02/03 for empty-state gating + End button visibility).
- New `describe("cold-boot resume", ...)` covering five cases: Loaded, Active-skip, None-skip, newer_version rejection, arbitrary IPC failure — each asserts `resume_review_session` is invoked the correct number of times (exactly once for Loaded; zero for skip/rejection-skip cases).
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joaofnds/code/trunk/.planning/PROJECT.md
@/Users/joaofnds/code/trunk/.planning/ROADMAP.md
@/Users/joaofnds/code/trunk/.planning/STATE.md

@/Users/joaofnds/code/trunk/.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-CONTEXT.md
@/Users/joaofnds/code/trunk/.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-RESEARCH.md
@/Users/joaofnds/code/trunk/.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-PATTERNS.md
@/Users/joaofnds/code/trunk/.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-UI-SPEC.md
@/Users/joaofnds/code/trunk/.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-VALIDATION.md

@/Users/joaofnds/code/trunk/src/components/ReviewPanel.svelte
@/Users/joaofnds/code/trunk/src/components/ReviewPanel.test.ts
@/Users/joaofnds/code/trunk/src/lib/types.ts
@/Users/joaofnds/code/trunk/src/lib/invoke.ts

<interfaces>
<!-- TypeScript types the executor must use unchanged. -->
From src/lib/types.ts:336-342:
```
export type SessionState = "active" | "resume-available" | "none";

export interface SessionStatus {
  state: SessionState;
  file_exists: boolean;
  canonical_path: string;
}
```

From src/components/ReviewPanel.svelte:139-156 (existing — DO NOT redefine):
```
function isTrunkError(e: unknown): e is TrunkError { ... }
function errorMessage(e: unknown, fallback: string): string { ... }
```

From src/lib/invoke.ts:
```
export function safeInvoke<T>(cmd: string, args?: Record<string, unknown>): Promise<T>;
export interface TrunkError { code: string; message: string }
```

Decision acceptance — D-02 (Toolbar active-state sync lag): explicitly DEFER. No eager `resume-on-repo-open` path. Toolbar reflects runtime, not disk. No benchmark needed since the eager path is not being taken (RESEARCH §User Constraints).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend installReads dispatcher and capture session-changed listener (Wave 0 test scaffold)</name>
  <files>src/components/ReviewPanel.test.ts</files>
  <read_first>
    - src/components/ReviewPanel.test.ts (lines 1-160 for current `installReads`, lines 22-40 for vi.mock blocks)
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-PATTERNS.md (§"installReads extension" and §"Test mocks (already in place at lines 22-40)")
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-RESEARCH.md (§"Wave 0 Gaps")
    - src/lib/types.ts (SessionStatus + SessionState definitions at lines 336-342)
  </read_first>
  <action>
    Extend the existing `installReads` function in `src/components/ReviewPanel.test.ts` (lines 100-121). Per 73-PATTERNS.md §"installReads extension":

    1. Add four optional fields to the `opts` param: `status?: SessionStatus`, `statusAfterResume?: SessionStatus`, `resumeRejection?: unknown`, `endRejection?: unknown`. Import `SessionStatus` from `../lib/types.js` (already imported file; just add to the named-import group at lines 8-12).
    2. Inside the dispatcher, default `status` to `{ state: "active", file_exists: true, canonical_path: "/repo" }` so the ~50 existing tests stay green without modification (D-01 active-path is the default).
    3. Track whether `resume_review_session` has been called via a closure variable `let resumed = false;` declared inside `installReads` (above the `mockImplementation` call). This is the listener re-trigger mechanism Test 1 in Task 2 depends on: after a successful resume, the next `get_review_session_status` call returns `statusAfterResume ?? status` (post-resume reality), modeling the backend transitioning from "resume-available" → "active".
    4. Add three new switch cases mirroring the analog at 73-PATTERNS.md §"installReads extension":
       - `case "get_review_session_status"` → resolve `resumed ? (opts.statusAfterResume ?? status) : status`
       - `case "resume_review_session"` → if `opts.resumeRejection !== undefined` reject with that value; else set `resumed = true` and resolve `undefined`
       - `case "end_review_session"` → if `opts.endRejection !== undefined` reject with that value, else resolve `undefined`

    5. Upgrade the `@tauri-apps/api/event` mock at lines 32-34 to capture the listener callback into a module-scoped variable. Replace the `vi.fn().mockResolvedValue(() => {})` mock with the captured-callback shape from 73-PATTERNS.md §"Test mocks": a `let sessionChangedHandler: ((event: { payload: string }) => void) | null = null;` declaration at file top-level (above the `vi.mock` call), and a `vi.fn((_event, cb) => { sessionChangedHandler = cb; return Promise.resolve(() => { sessionChangedHandler = null; }); })` implementation. Export `sessionChangedHandler` access through a small helper at file scope (e.g., a `function fireSessionChanged(payload: string)` that calls `sessionChangedHandler?.({ payload })`) so Plans 02/03 can reuse it.

    Do NOT add any new tests in this task — the dispatcher extension is infrastructure-only. The first new test belongs to Task 2. Surgical: do NOT touch any test bodies, mocks, or helpers other than the ones explicitly listed.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk &amp;&amp; pnpm vitest run src/components/ReviewPanel.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All pre-existing tests in `ReviewPanel.test.ts` still pass (~50 tests; no regressions).
    - `grep -n 'get_review_session_status\|resume_review_session\|end_review_session' src/components/ReviewPanel.test.ts` returns at least three switch-case lines.
    - `grep -n 'sessionChangedHandler' src/components/ReviewPanel.test.ts` returns the declaration, the assignment inside the `listen` mock, and at least one export/helper reference.
    - `pnpm svelte-check` passes (no TS errors from the new SessionStatus import).
    - No new test added in this task — the file's test-count delta from `pnpm vitest run src/components/ReviewPanel.test.ts -t "ReviewPanel"` is zero (described tests do not change).
  </acceptance_criteria>
  <done>installReads accepts status/resumeRejection/endRejection; the three new commands are routed via switch; session-changed listener callback is captured into a test-visible handle; existing tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cold-boot resume branch in reload() + sessionState rune (TDD: 5 tests)</name>
  <files>src/components/ReviewPanel.test.ts, src/components/ReviewPanel.svelte</files>
  <read_first>
    - src/components/ReviewPanel.svelte (lines 207-253 for current `reload()`; lines 130-156 for `copied` pattern + `isTrunkError`/`errorMessage`; lines 342-367 for the $effect listener with WR-03 cleanup)
    - src/components/ReviewPanel.test.ts (post-Task-1 — verify installReads/sessionChangedHandler scaffold is in place)
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-RESEARCH.md (§"Pattern 2: Cold-boot resume — exact wiring in reload()" and §"Pitfall 1: reload() recursion via session-changed")
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-PATTERNS.md (§"reload() modification — cold-boot resume branch")
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-UI-SPEC.md (§"Copywriting Contract" row "Toast — resume failed")
    - src/lib/types.ts (SessionState type — kebab-case wire format: `"active" | "resume-available" | "none"`)
  </read_first>
  <behavior>
    Five tests in a new `describe("cold-boot resume", ...)` block at the bottom of `ReviewPanel.test.ts` (peer to `describe("Copy", ...)`). REAL timers — do NOT use `vi.useFakeTimers()` in this describe; the cold-boot reload chain is microtask-driven only.

    Per REQ-73-RESUME from 73-RESEARCH.md §"Phase Requirements → Test Map":

    - Test 1: `installReads({ commits, comments: [lineAnchoredComment("c1", COMMIT_A, "x")], resolutions: [resolvable("c1")], status: { state: "resume-available", file_exists: true, canonical_path: "/repo" } })` → render → flush → `calledCommands().filter(c => c === "resume_review_session").length === 1`. AND that call is recorded BEFORE the three list-read calls in `vi.mocked(safeInvoke).mock.calls` order. AND the comment "x" is in the rendered DOM (proves the reads ran after resume).
    - Test 2: same setup but `status.state === "active"` → render → flush → `calledCommands().filter(c => c === "resume_review_session").length === 0`. The three reads still run (commits/comments arrays render).
    - Test 3: same setup but `status.state === "none"` and `comments: []`, `commits: []` → render → flush → `resume_review_session` not called; reads run; no toast emitted; arrays remain empty (cold render path — exact empty-state copy is Plan 03's concern, but the no-resume-no-toast invariant is THIS test's assertion).
    - Test 4: `installReads({ status: { state: "resume-available", ... }, resumeRejection: { code: "newer_version", message: "Session file is newer than this build supports" } })` → render → flush → `showToast` called once with `"Failed to resume review: Session file is newer than this build supports"` and `"error"` kind. The reads still attempt (they will resolve to defaults since installReads default arrays are `[]`).
    - Test 5: `installReads({ status: { state: "resume-available", ... }, resumeRejection: new Error("boom") })` → render → flush → `showToast` called once with `"Failed to resume review: boom"` and `"error"`. Asserts `errorMessage`'s `Error` branch (not TrunkError) is exercised.

    Each test asserts on the exact `resume_review_session` call count (per advisor: NOT `toHaveBeenCalled()` — explicit `.toBe(1)` or `.toBe(0)`) because the `session-changed` listener may re-trigger `reload()` once after the resume, and the recursion-safety invariant (RESEARCH Pitfall 1: "resume is gated on 'resume-available' state — the second reload sees 'active' and skips") is what keeps the count at exactly 1.

    To exercise the listener re-trigger: Test 1 setup is `installReads({ ..., status: { state: "resume-available", ... }, statusAfterResume: { state: "active", file_exists: true, canonical_path: "/repo" } })`. The dispatcher (per Task 1 step 3) flips `get_review_session_status` to return `statusAfterResume` after the first `resume_review_session` resolves — modeling the backend transitioning "resume-available" → "active" after a successful resume. After the initial render+flush, call `fireSessionChanged("/repo")` and flush again; the second `reload()` reads `status.state === "active"` and SKIPS the resume branch. Final assertion: `calledCommands().filter(c => c === "resume_review_session").length === 1` (exactly one resume call across both reloads). NO ad-hoc `vi.mocked(safeInvoke).mockImplementation(...)` in the test body; NO second `installReads` invocation between flushes. The mechanism is the dispatcher's `resumed` flag.
  </behavior>
  <action>
    Apply RED → GREEN → REFACTOR per task-level TDD (config.json `tdd_mode: true`).

    **RED step:** Add the five tests above to `src/components/ReviewPanel.test.ts` in a new `describe("cold-boot resume", ...)` at the bottom of the file (peer to `describe("Copy")` — NOT nested inside `describe("ReviewPanel")`; check the file's existing structure). Use `flush()` (the file-global one, not `flushFake` — this describe uses REAL timers). Run `pnpm vitest run src/components/ReviewPanel.test.ts -t "cold-boot resume"`; all five MUST fail with messages indicating the resume branch is missing.

    **GREEN step:** Modify `src/components/ReviewPanel.svelte`:
    1. Add `let sessionState = $state<SessionState>("none");` near the existing `copied` declaration at line 132. Import `SessionState` from `../lib/types.js` alongside the existing `SessionStatus` import at lines 7-13.
    2. Inside `reload()` (lines 210-253), inside the first try block (status fetch at lines 219-228), after `canonicalPath = status.canonical_path;` AND inside the SAME try block, add `sessionState = status.state;` (so `sessionState` is set before the resume branch reads it; if the status fetch fails, `sessionState` stays at its previous value — acceptable, since cold-empty arrays will still render via the no_session catch arm).
    3. After the closing brace of that try block (BEFORE the second try block that does Promise.all reads), insert the resume branch per 73-PATTERNS.md §"reload() modification": `if (sessionState === "resume-available") { try { await safeInvoke("resume_review_session", { path: repoPath }); } catch (e) { showToast(errorMessage(e, "Failed to resume review"), "error"); } }`.
    4. Do NOT clear arrays inside the resume catch arm — let the subsequent Promise.all reads run; if the resume failed with newer_version the reads will return no_session and the existing arm at lines 242-247 handles it.

    Run the five new tests; all MUST pass. Run the full file (`pnpm vitest run src/components/ReviewPanel.test.ts`); existing tests MUST still pass.

    **REFACTOR step:** Inspect the new code for duplication, naming, comments. The resume branch is ~6 lines; if it's clear, leave it. If `sessionState = status.state` assignment is awkward inside the existing try block, that's fine — keep co-location with `canonicalPath = status.canonical_path` since both are derived from the same `status` value (Tell Don't Ask). No extraction needed unless the executor finds a clear smell. Re-run tests to confirm no regression.

    Reference 73-PATTERNS.md §"reload() modification — cold-boot resume branch" for the canonical excerpt; do NOT inline it here.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk &amp;&amp; pnpm vitest run src/components/ReviewPanel.test.ts -t "cold-boot resume"</automated>
  </verify>
  <acceptance_criteria>
    - All five new tests in `describe("cold-boot resume", ...)` pass.
    - The full file passes: `pnpm vitest run src/components/ReviewPanel.test.ts` exits 0 (no regressions in the ~50 prior tests).
    - `grep -n 'sessionState' src/components/ReviewPanel.svelte` shows declaration AND assignment in `reload()` AND at least one usage (the resume branch).
    - `grep -n 'resume_review_session' src/components/ReviewPanel.svelte` returns exactly one safeInvoke call (inside the new branch); no other call sites added.
    - `grep -n '"Failed to resume review"' src/components/ReviewPanel.svelte` returns the showToast string from the catch arm.
    - No PascalCase comparison in source: `grep -n '"ResumeAvailable"\|"Active"\|"None"' src/components/ReviewPanel.svelte` returns no hits (advisor reminder: wire format is kebab-case).
    - svelte-check passes for the file.
    - Two commits expected: `test(73-01): add failing tests for cold-boot resume` (RED) and `feat(73-01): wire cold-boot resume in ReviewPanel.reload()` (GREEN); optional `refactor(73-01): …` if anything cleaned up.
  </acceptance_criteria>
  <done>Cold-boot resume fires resume_review_session exactly once when status.state === "resume-available", surfaces errors via showToast with extracted message, leaves the existing no_session-on-reads arm intact, and exposes sessionState as a local rune ready for Plan 02 (End-button visibility) and Plan 03 (empty-state gating).</done>
</task>

<task type="auto">
  <name>Task 3: just check phase gate</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-RESEARCH.md (§"Sampling Rate (Nyquist Coverage)" → "Per wave merge: pnpm vitest run (whole frontend suite)")
  </read_first>
  <action>
    Run `just check` from the repo root. This is the wave-merge gate for Plan 01.

    If anything fails: per `ownership.md`, we own every failure — even pre-existing ones (no "not my problem" claims). Fix in this plan if the fix is small and related; otherwise file a precise TODO with file:line and root-cause language and surface it in the SUMMARY.

    DO NOT skip this step. DO NOT pass via `--no-verify`. DO NOT modify files outside the plan's `files_modified` without first surfacing the failure to the user (advisor + ownership.md).
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk &amp;&amp; just check</automated>
  </verify>
  <acceptance_criteria>
    - `just check` exits 0.
    - All vitest tests pass (existing + 5 new cold-boot tests).
    - cargo test, clippy, fmt, biome, svelte-check all green.
    - If any failure traces to pre-existing breakage, surface it as a finding in the SUMMARY with file:line and a one-line root-cause hypothesis — do NOT defer silently.
  </acceptance_criteria>
  <done>`just check` is green; Plan 01 is mergeable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Svelte UI → Tauri IPC | `repoPath` string flows into `safeInvoke("resume_review_session"/"get_review_session_status", ...)`. Backend already validates via `canonical_repo_path` (review.rs:61-69, rejects unknown repos with TrunkError(`not_open`)). |
| Tauri event bus → Svelte listener | `session-changed` event payload (canonical path string) flows into the panel listener at ReviewPanel.svelte:353-367. Existing canonicalPath filter is preserved untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | `reload()` recursion via `session-changed` re-fire after `resume_review_session` emits | mitigate | The resume branch is gated on `sessionState === "resume-available"`; after a successful resume the backend's status transitions to `active` and the recursive `reload()` skips the resume call. Plan 01 Task 2 Test 1 explicitly asserts `resume_review_session` call count is exactly 1 on cold boot (RESEARCH Pitfall 1). |
| T-73-02 | Tampering | Race deleting session file mid-resume (another tab or external process) | accept | Backend serializes via `Mutex<HashMap<...>>` (review.rs:425-447 `mutate_session_rmw`) and atomic tmp+rename file writes (Phase 65 D-10). No new race surface introduced by Plan 01 — the resume IPC handles all three LoadOutcomes (Loaded / RecoveredCorrupt / RefusedNewer) atomically. |
| T-73-03 | Information Disclosure | Error message in `Failed to resume review: {extracted}` toast | accept | `errorMessage` extracts `e.message` from native `Error` or `TrunkError`; both are produced by our own backend with no PII. `showToast` renders plain text (toast.svelte.ts has no HTML interpolation). |
| T-73-04 | Repudiation / DoS | `resume_review_session` retries on transient failures | accept | Phase 73 does NOT add retry loops; a single failed resume surfaces a toast and the user can re-open the panel. Persistent failures (RefusedNewer) are user-actionable. |

**No new IPC handlers introduced. No new persisted state. No new packages.** ASVS L1 verdict: threat surface identical to Phase 72 + Phase 65 baseline; accept LOW.

**D-02 acceptance recorded:** Toolbar reflects runtime (not disk) lag is explicitly DEFERRED. No eager `resume-on-repo-open` path is added in this plan — the user's `[[measure-before-assuming]]` constraint is honored by NOT taking the eager path without a benchmark (RESEARCH §User Constraints).
</threat_model>

<verification>
- Run the new describe block first: `pnpm vitest run src/components/ReviewPanel.test.ts -t "cold-boot resume"` — five tests, all green.
- Run the full file: `pnpm vitest run src/components/ReviewPanel.test.ts` — no regressions in the ~50 prior tests.
- Run the whole gate: `just check` — exits 0.
- Manual smoke (optional, recorded in SUMMARY): `pnpm tauri dev`, open a repo that already has an on-disk session, observe comments appear in the ReviewPanel on first open without clicking Add Note. This is the Bug 3 closure proof and is also listed in 73-VALIDATION.md §"Manual-Only Verifications" as a required manual step before `/gsd:verify-work`.
</verification>

<success_criteria>
- REQ-73-RESUME: Cold-boot resume calls `resume_review_session` exactly once when status is `resume-available`; never when `active` or `none`; rejection surfaces a toast with extracted message. Five automated tests prove this.
- REQ-73-NYQUIST (partial): The cold-boot lifecycle path has automated test coverage at the Nyquist sampling rate defined in 73-RESEARCH.md §"Sampling Rate (Nyquist Coverage)".
- REQ-73-CHECK: `just check` is green.
- `sessionState` local rune is exposed for Plans 02/03 consumption.
- D-01 (resume trigger location), D-02 (toolbar lag accepted), D-07 (LoadOutcome handling) all implemented per 73-CONTEXT.md decisions.
- No backend code modified. No new packages installed.
</success_criteria>

<output>
Create `.planning/phases/73-review-lifecycle-end-review-cold-boot-resume/73-01-SUMMARY.md` summarizing:
- New tests added (file + test names).
- Code change locations (file:line for the resume branch and sessionState declaration).
- Any `just check` issues found and how resolved.
- Confirmation that Plans 02 and 03 can consume `sessionState` and `sessionChangedHandler` without additional scaffolding.
</output>
