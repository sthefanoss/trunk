---
phase: 62-ui-refactor-component-structure
plan: 02
type: execute
wave: 2
depends_on: [62-01]
files_modified:
  - src/components/DiffPanel.test.ts
autonomous: false
requirements: [VIEW-01, DISP-01]

must_haves:
  truths:
    - "Tests verify that the segmented control renders three view mode buttons"
    - "Tests verify that clicking a non-hunk mode shows placeholder text"
    - "Tests verify that line numbers render in the gutter"
    - "Tests verify that Add lines show only new line number and Delete lines show only old"
    - "All existing 14 tests still pass alongside new tests"
  artifacts:
    - path: "src/components/DiffPanel.test.ts"
      provides: "New test cases for VIEW-01 and DISP-01 requirements"
      min_lines: 370
  key_links:
    - from: "src/components/DiffPanel.test.ts"
      to: "src/components/DiffPanel.svelte"
      via: "render(DiffPanel, { props }) and DOM assertions"
      pattern: "render.*DiffPanel"
---

<objective>
Add new unit tests for VIEW-01 (view mode toggle) and DISP-01 (line number gutter) to the existing DiffPanel test file, then verify the complete UI visually.

Purpose: Ensure the new view mode toggle and line number gutter features have automated test coverage, and visually confirm the refactored UI looks correct.
Output: Extended DiffPanel.test.ts with ~6-8 new test cases, visual confirmation of the UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/62-ui-refactor-component-structure/62-CONTEXT.md
@.planning/phases/62-ui-refactor-component-structure/62-01-SUMMARY.md
@src/components/DiffPanel.svelte
@src/components/DiffPanel.test.ts
@src/lib/types.ts

<interfaces>
<!-- Test data pattern from existing DiffPanel.test.ts -->

```typescript
const testDiff: FileDiff = {
  path: "src/main.ts",
  status: "Modified",
  is_binary: false,
  hunks: [{
    header: "@@ -1,3 +1,4 @@",
    old_start: 1, old_lines: 3, new_start: 1, new_lines: 4,
    lines: [
      { origin: "Context", content: "import { foo } from 'bar';", old_lineno: 1, new_lineno: 1, spans: [] },
      { origin: "Delete", content: "const x = 1;", old_lineno: 2, new_lineno: null, spans: [] },
      { origin: "Add", content: "const x = 2;", old_lineno: null, new_lineno: 2, spans: [] },
      { origin: "Add", content: "const y = 3;", old_lineno: null, new_lineno: 3, spans: [] },
      { origin: "Context", content: "export { x };", old_lineno: 3, new_lineno: 4, spans: [] },
    ],
  }],
};
```

ViewMode type: `"hunk" | "full" | "split"`
Segmented control buttons text: "Hunk", "Full", "Split"
Stub text: "Full file view — coming soon", "Split view — coming soon"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test cases for VIEW-01 (view mode toggle) and DISP-01 (line number gutter)</name>
  <files>src/components/DiffPanel.test.ts</files>
  <read_first>
    src/components/DiffPanel.test.ts
    src/components/DiffPanel.svelte
    src/components/diff/DiffToolbar.svelte
    src/components/diff/HunkView.svelte
    src/components/diff/DiffViewer.svelte
    src/lib/types.ts
  </read_first>
  <action>
Add new test cases to the existing `describe("DiffPanel")` block in `src/components/DiffPanel.test.ts`. The existing tests and test data (testDiff, binaryDiff, testDiffWithMergedSpans) must remain unchanged.

Also add a mock for the store module to prevent LazyStore from being called in tests:
```typescript
vi.mock("../lib/store.js", () => ({
  getDiffViewMode: vi.fn().mockResolvedValue("hunk"),
  setDiffViewMode: vi.fn().mockResolvedValue(undefined),
  // Keep other store functions if imported elsewhere
}));
```

**New tests for VIEW-01 (view mode toggle):**

1. **"renders view mode segmented control with three options"**:
   - Render DiffPanel with `fileDiffs: [testDiff]`, `commitDetail: null`, `onclose: vi.fn()`.
   - Assert `screen.getByText("Hunk")` is in the document.
   - Assert `screen.getByText("Full")` is in the document.
   - Assert `screen.getByText("Split")` is in the document.

2. **"shows hunk view by default"**:
   - Render DiffPanel with testDiff.
   - Assert hunk header is visible: `screen.getByText("@@ -1,3 +1,4 @@")`.
   - Assert "Full file view" placeholder is NOT visible: `screen.queryByText(/Full file view/)` is null.

3. **"shows full file stub when Full mode selected"**:
   - Render DiffPanel with testDiff.
   - Click the "Full" button: `await fireEvent.click(screen.getByText("Full"))`.
   - Assert stub text is visible: `screen.getByText(/Full file view/)`.
   - Assert hunk header is NOT visible: `screen.queryByText("@@ -1,3 +1,4 @@")` is null.

4. **"shows split view stub when Split mode selected"**:
   - Render DiffPanel with testDiff.
   - Click the "Split" button: `await fireEvent.click(screen.getByText("Split"))`.
   - Assert stub text is visible: `screen.getByText(/Split view/)`.

**New tests for DISP-01 (line number gutter):**

5. **"renders line numbers in gutter for context lines"**:
   - Render DiffPanel with testDiff.
   - The first context line has `old_lineno: 1, new_lineno: 1`.
   - Assert the container text content contains "1" (line numbers).
   - Use `container.querySelectorAll` to find gutter elements (e.g., elements with class containing "gutter"). Verify at least 2 gutter elements exist per line (old + new).

6. **"shows only new line number for Add lines"**:
   - Render DiffPanel with testDiff.
   - Add lines have `old_lineno: null, new_lineno: 2` and `old_lineno: null, new_lineno: 3`.
   - Find all diff-line-add elements: `container.querySelectorAll(".diff-line-add")`.
   - For each add line, verify the old gutter is empty (textContent is '') and the new gutter has a number.

7. **"shows only old line number for Delete lines"**:
   - Render DiffPanel with testDiff.
   - Delete lines have `old_lineno: 2, new_lineno: null`.
   - Find all diff-line-delete elements: `container.querySelectorAll(".diff-line-delete")`.
   - For each delete line, verify the new gutter is empty and the old gutter has a number.

Note on gutter element selection: The gutter spans will have a CSS class (e.g., "gutter-old", "gutter-new" or similar). Check the actual class names used in HunkView.svelte and query by those classes.

Run `bun run test -- --run DiffPanel` after adding tests to confirm all pass (existing 14 + new 6-7).
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && bun run test -- --run DiffPanel 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/DiffPanel.test.ts contains test "renders view mode segmented control"
    - src/components/DiffPanel.test.ts contains `screen.getByText("Hunk")`
    - src/components/DiffPanel.test.ts contains `screen.getByText("Full")`
    - src/components/DiffPanel.test.ts contains `screen.getByText("Split")`
    - src/components/DiffPanel.test.ts contains test for "full file stub" or "Full mode"
    - src/components/DiffPanel.test.ts contains test for "split view stub" or "Split mode"
    - src/components/DiffPanel.test.ts contains test for "line numbers" or "gutter"
    - src/components/DiffPanel.test.ts contains `vi.mock("../lib/store.js"`
    - `bun run test -- --run DiffPanel` shows 20+ tests passing (14 existing + 6+ new)
    - `bun run test -- --run DiffPanel` exits 0
  </acceptance_criteria>
  <done>DiffPanel.test.ts has new test cases for VIEW-01 (segmented control rendering, mode switching, stub display) and DISP-01 (line numbers in gutter for context/add/delete lines). All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of refactored diff panel</name>
  <files>none</files>
  <action>
    No automated action needed. This is a visual verification checkpoint where the user manually verifies the refactored UI in the running application by following the steps below.
  </action>
  <what-built>
    DiffPanel decomposed into DiffToolbar + DiffViewer + HunkView + stub components. View mode segmented control added. Line number gutter added. All existing behavior preserved.
  </what-built>
  <how-to-verify>
    1. Run `bun run dev` to start the app
    2. Open a repository with changes (or view a commit diff)
    3. Verify the toolbar shows: [Hunk | Full | Split] segmented control on the left, filename in center, close button on right
    4. Verify "Hunk" is highlighted as the active mode
    5. Click "Full" -- should show "Full file view -- coming soon" centered placeholder
    6. Click "Split" -- should show "Split view -- coming soon" centered placeholder
    7. Click "Hunk" -- should return to normal diff view
    8. Verify line numbers appear in the gutter (two columns: old line number, new line number)
    9. Verify context lines show both old and new line numbers
    10. Verify Add lines show only new line number (old gutter empty)
    11. Verify Delete lines show only old line number (new gutter empty)
    12. If viewing unstaged changes: verify Stage File, Stage Hunk, Discard Hunk buttons still work
    13. Verify line selection (click to select, shift-click for range) still works
    14. Verify keyboard navigation ([ and ] to navigate hunks, Escape to clear selection)
    15. Verify word-level diff highlights still render (colored backgrounds on changed words)
    16. Verify syntax highlighting still renders (colored keywords, strings, etc.)
  </how-to-verify>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && bun run test && bun run check</automated>
  </verify>
  <done>User visually confirmed the refactored diff panel UI renders correctly with view mode toggle, line numbers, and all existing functionality preserved.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `bun run test -- --run DiffPanel` -- all tests pass (14 existing + 6+ new)
2. `bun run test` -- full test suite passes
3. `bun run check` -- svelte-check passes
4. Visual confirmation that the UI looks correct
</verification>

<success_criteria>
- VIEW-01 has automated test coverage (segmented control, mode switching, stubs)
- DISP-01 has automated test coverage (line numbers for context/add/delete)
- Full test suite passes with no regressions
- Visual confirmation from user that the refactored UI works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/62-ui-refactor-component-structure/62-02-SUMMARY.md`
</output>
