---
phase: quick
plan: 260320-wz2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WelcomeScreen.svelte
  - src/lib/store.ts
autonomous: false
requirements: [QUICK-simplify-splash]

must_haves:
  truths:
    - "Each recent project displays as a single-line item, not a two-line card"
    - "The repo name (last path segment) is visually distinct (bold/bright) within the full path"
    - "Up to 10 recent projects are stored and displayed"
    - "Remove button still works on hover"
  artifacts:
    - path: "src/components/WelcomeScreen.svelte"
      provides: "Single-line recent project list items"
      contains: "recentRepos"
    - path: "src/lib/store.ts"
      provides: "MAX_RECENT = 10"
      contains: "MAX_RECENT = 10"
  key_links:
    - from: "src/components/WelcomeScreen.svelte"
      to: "src/lib/store.ts"
      via: "getRecentRepos import"
      pattern: "getRecentRepos"
---

<objective>
Simplify the WelcomeScreen recent projects from two-line bordered cards to compact single-line items showing the full path with the last segment (repo name) visually emphasized. Increase storage from 5 to 10 recent projects.

Purpose: Reduce vertical space per project item so more repos are visible without scrolling, and present a cleaner, more information-dense list.
Output: Updated WelcomeScreen.svelte with single-line items, updated store.ts with MAX_RECENT = 10.
</objective>

<execution_context>
@/Users/joaofnds/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/WelcomeScreen.svelte
@src/lib/store.ts
@src/app.css

<interfaces>
From src/lib/store.ts:
```typescript
export interface RecentRepo { name: string; path: string; }
export async function getRecentRepos(): Promise<RecentRepo[]>;
export async function addRecentRepo(repo: RecentRepo): Promise<void>;
export async function removeRecentRepo(path: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Increase MAX_RECENT to 10 and simplify project list items to single lines</name>
  <files>src/lib/store.ts, src/components/WelcomeScreen.svelte</files>
  <action>
In src/lib/store.ts:
- Change `MAX_RECENT = 5` to `MAX_RECENT = 10` on line 8.

In src/components/WelcomeScreen.svelte, replace the current two-line card layout (lines 85-112, the `{#each}` block contents) with a compact single-line design:

1. Replace the current `<div class="group flex items-center ... rounded-md px-3 py-2" style="background: var(--color-surface); border: ...">` card wrapper with a simpler row: `<div class="group flex items-center gap-2 rounded px-3 py-1.5 cursor-pointer hover:bg-white/5" ...>`. No background, no border — just a subtle hover highlight.

2. Replace the inner `<div class="flex flex-col gap-0.5 ...">` two-line layout with a single `<span>` that shows the full path. Split the path to render the leading directories in muted color and the last segment (repo name) in bold with the primary text color. Use a structure like:
   ```svelte
   <span class="text-sm truncate min-w-0 flex-1">
     <span style="color: var(--color-text-muted);">{repo.path.substring(0, repo.path.lastIndexOf('/'))}/</span><span class="font-semibold" style="color: var(--color-text);">{repo.path.split('/').at(-1)}</span>
   </span>
   ```

3. Keep the existing remove button (the x button) exactly as-is with its `opacity-0 group-hover:opacity-100` behavior.

4. Keep all existing event handlers (onclick, onkeydown, role, tabindex, a11y suppression comments) unchanged.

IMPORTANT: Do NOT inline any color hex values. Use existing CSS custom properties: `var(--color-text)`, `var(--color-text-muted)`, etc.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && npx svelte-check --threshold error 2>&1 | tail -20</automated>
  </verify>
  <done>
    - MAX_RECENT is 10 in store.ts
    - Each recent repo renders as a single line showing the full path with the last segment bold
    - No two-line card layout remains
    - Remove button still appears on hover
    - No inline color values introduced
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify simplified project list appearance</name>
  <files>src/components/WelcomeScreen.svelte</files>
  <action>
User visually verifies the simplified welcome screen project list.
  </action>
  <verify>User confirms the layout looks correct</verify>
  <done>User approves the visual result</done>
  <what-built>Simplified recent project list items from two-line bordered cards to compact single-line path display with bold repo name, and increased max from 5 to 10.</what-built>
  <how-to-verify>
    1. Run `cargo tauri dev` and observe the welcome screen
    2. Verify each recent project appears as a single line (path with bold last segment)
    3. Verify hovering shows the remove button
    4. Verify clicking a project still opens it
    5. Verify the list can accommodate up to 10 items (add repos if needed to test)
    6. Confirm the overall look is clean and compact
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx svelte-check --threshold error` passes with no errors
- Visual inspection confirms single-line layout
</verification>

<success_criteria>
- Recent projects display as single-line items with bold repo name at end of path
- Up to 10 projects stored and displayed
- No visual regressions (hover states, remove button, click-to-open all work)
</success_criteria>

<output>
After completion, create `.planning/quick/260320-wz2-simplify-splash-screen-project-cards-to-/260320-wz2-SUMMARY.md`
</output>
