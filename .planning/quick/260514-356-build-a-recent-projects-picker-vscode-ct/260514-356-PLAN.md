---
quick_id: 260514-356
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/commands/fs.rs
  - src-tauri/src/commands/mod.rs
  - src-tauri/src/lib.rs
  - src/lib/recent-filter.ts
  - src/lib/recent-filter.test.ts
  - src/components/RecentReposPicker.svelte
  - src/App.svelte
autonomous: true
requirements:
  - QR-01  # Cmd/Ctrl+R opens picker, preventDefault reload
  - QR-02  # Picker opens repo in CURRENT tab via openRepoInTab
  - QR-03  # Missing paths auto-pruned each time picker opens
  - QR-04  # Rows = name + tildified path via displayPath()
  - QR-05  # Case-insensitive substring filter on name + path
  - QR-06  # Keyboard nav: arrows, Enter, Esc, Backspace-empty
  - QR-07  # Empty/no-match states; re-trigger while open is no-op

must_haves:
  truths:
    - "Pressing Cmd/Ctrl+R from anywhere opens the picker; the webview never reloads"
    - "Typing filters the visible rows by case-insensitive substring against name and path"
    - "ArrowUp/ArrowDown move the highlight; Enter opens the highlighted repo in the current tab"
    - "Esc closes the picker; Backspace on an empty query closes the picker"
    - "Opening the picker prunes recents whose paths no longer exist (or are not git repos) before render"
    - "Selecting a repo that matches the active tab's path is a no-op (closes picker, no reload)"
    - "Picking moves the repo to top of recents (addRecentRepo) and opens via App.svelte:openRepoInTab"
    - "Empty recents shows 'No recent repositories' with a button that triggers the native open dialog"
    - "No-match state shows 'No matches'"
    - "Cmd/Ctrl+R while picker already visible is a no-op (no toggle, no focus steal)"
    - "When focus is in an <input> or [contenteditable], Cmd/Ctrl+R is ignored"
  artifacts:
    - path: "src-tauri/src/commands/fs.rs"
      provides: "validate_recent_path Tauri command — returns bool"
      contains: "pub async fn validate_recent_path"
    - path: "src-tauri/src/commands/mod.rs"
      provides: "fs module registration"
      contains: "pub mod fs;"
    - path: "src-tauri/src/lib.rs"
      provides: "validate_recent_path wired into generate_handler!"
      contains: "commands::fs::validate_recent_path"
    - path: "src/lib/recent-filter.ts"
      provides: "filterRecents(repos, query) pure function — case-insensitive substring match against name and path"
      exports: ["filterRecents"]
    - path: "src/lib/recent-filter.test.ts"
      provides: "Vitest coverage of filterRecents"
    - path: "src/components/RecentReposPicker.svelte"
      provides: "Overlay picker UI — input, filtered list, keyboard nav, empty/no-match states"
    - path: "src/App.svelte"
      provides: "Cmd/Ctrl+R global binding, picker mount, onpick handler that calls openRepoInTab"
  key_links:
    - from: "src/App.svelte keydown handler"
      to: "picker open state"
      via: "Cmd/Ctrl+R sets pickerOpen=true; preventDefault prevents webview reload"
      pattern: "(metaKey|ctrlKey).*key === \"r\""
    - from: "src/components/RecentReposPicker.svelte onMount"
      to: "validate_recent_path Tauri command"
      via: "Promise.all over recents, drops invalid entries, persists via removeRecentRepo"
      pattern: "safeInvoke.*validate_recent_path"
    - from: "RecentReposPicker.svelte Enter handler"
      to: "App.svelte:openRepoInTab"
      via: "onpick callback prop — App handles open_repo invoke + addRecentRepo + tab routing"
      pattern: "onpick\\(.*path.*name"
    - from: "src/components/RecentReposPicker.svelte filtered list"
      to: "src/lib/recent-filter.ts:filterRecents"
      via: "$derived(filterRecents(recents, query))"
      pattern: "filterRecents\\("
---

<objective>
Add a VSCode-style command-palette overlay (`Cmd/Ctrl+R`) that lets the user search and re-open any previously-opened repo in the current tab. Reuses existing recents persistence (`store.ts`), tildified path rendering (`displayPath`), and tab-open machinery (`App.svelte:openRepoInTab`). One small new Tauri command for path validation.

Purpose: Quick repo-switching without leaving the active tab or navigating WelcomeScreen.
Output: New backend `validate_recent_path` command, pure filter utility with tests, picker component, and `App.svelte` wiring.
</objective>

<execution_context>
@/Users/joaofnds/code/trunk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joaofnds/code/trunk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joaofnds/code/trunk/CLAUDE.md
@/Users/joaofnds/code/trunk/.planning/quick/260514-356-build-a-recent-projects-picker-vscode-ct/260514-356-CONTEXT.md
@/Users/joaofnds/code/trunk/src/lib/store.ts
@/Users/joaofnds/code/trunk/src/lib/path.ts
@/Users/joaofnds/code/trunk/src/lib/invoke.ts
@/Users/joaofnds/code/trunk/src/App.svelte
@/Users/joaofnds/code/trunk/src/components/WelcomeScreen.svelte
@/Users/joaofnds/code/trunk/src-tauri/src/commands/mod.rs
@/Users/joaofnds/code/trunk/src-tauri/src/commands/repo.rs
@/Users/joaofnds/code/trunk/src-tauri/src/lib.rs

<interfaces>
<!-- Key contracts the executor needs. Use these directly — no codebase exploration required. -->

From `src/lib/store.ts`:
- `interface RecentRepo { name: string; path: string }`
- `async function getRecentRepos(): Promise<RecentRepo[]>`
- `async function addRecentRepo(repo: RecentRepo): Promise<void>` (moves to top, capped at 10)
- `async function removeRecentRepo(path: string): Promise<void>`

From `src/lib/path.ts`:
- `async function displayPath(path: string): Promise<string>` (replaces `$HOME` with `~`)

From `src/lib/invoke.ts`:
- `async function safeInvoke<T>(cmd: string, args?: Record<string, unknown>): Promise<T>`
- `interface TrunkError { code: string; message: string }`

From `src/App.svelte`:
- `function openRepoInTab(tabId: string, path: string, name: string): void` — handles duplicate detection and tab routing. Does NOT call `open_repo`; caller must invoke it first (see `WelcomeScreen.svelte:openPath` for the canonical pattern: `safeInvoke("open_repo", { path })`, then `addRecentRepo`, then `onopen(path, name)`).
- Existing keydown handler lives in the `$effect` near line 393. Add the Cmd/Ctrl+R branch inside the same `if (e.metaKey || e.ctrlKey)` block, BEFORE the zoom/pane branches.

From `src-tauri/src/commands/repo.rs` and `src-tauri/src/git/repository.rs`:
- `repository::validate_and_open(path: &Path) -> Result<(), TrunkError>` — the existing "is this a git repo?" gate used by `open_repo`. Reuse it inside `validate_recent_path` instead of writing new logic.

Tauri command registration: `src-tauri/src/lib.rs` line ~68 inside `tauri::generate_handler![…]`. Add `commands::fs::validate_recent_path,` in the list.

The existing keydown `$effect` (App.svelte ~line 393) handles `metaKey || ctrlKey` shortcuts. The Cmd/Ctrl+R branch must:
1. Run only when `e.key === "r" || e.key === "R"` and `e.metaKey || e.ctrlKey`.
2. Skip if `document.activeElement` is `HTMLInputElement`, `HTMLTextAreaElement`, or `[contenteditable]`.
3. Skip if picker is already open.
4. Call `e.preventDefault()` (critical: stops webview reload).
5. Set picker open state to true.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validate_recent_path Tauri command</name>
  <files>src-tauri/src/commands/fs.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <action>
  Create `src-tauri/src/commands/fs.rs` with one async Tauri command `validate_recent_path(path: String) -> Result<bool, String>` that returns `Ok(true)` iff the path exists AND is a valid git repo, `Ok(false)` otherwise. Implementation: run inside `tauri::async_runtime::spawn_blocking` (mirror `repo::open_repo` style). Inside the blocking closure, construct `PathBuf::from(&path)`, then attempt `crate::git::repository::validate_and_open(&path_buf)`. Map `Ok(_)` → `true`; map any `Err(_)` (missing path, not a repo, permission error) → `false`. Never bubble up an error — this command is best-effort validation, callers only care about the bool. Use `.map_err` on the `spawn_blocking` join error path to return a serialized `TrunkError` like `repo::open_repo` does, but the inner closure must always return `Ok(bool)`. Register the module: add `pub mod fs;` to `src-tauri/src/commands/mod.rs` (alphabetically — between `diff` and `history`). Wire the command into `tauri::generate_handler![…]` in `src-tauri/src/lib.rs` (add `commands::fs::validate_recent_path,` to the list; placement near the `repo::*` entries is fine).
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | tail -20</automated>
  </verify>
  <done>`cargo check` passes. `grep -n "validate_recent_path" src-tauri/src/lib.rs` shows it inside the generate_handler list. `grep -n "pub mod fs" src-tauri/src/commands/mod.rs` returns a match.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter util, picker component, and App.svelte wiring</name>
  <files>src/lib/recent-filter.ts, src/lib/recent-filter.test.ts, src/components/RecentReposPicker.svelte, src/App.svelte</files>
  <behavior>
    For `filterRecents(repos: RecentRepo[], query: string): RecentRepo[]`:
    - Test 1: empty query returns the input array unchanged (same order, same length)
    - Test 2: case-insensitive substring match against `name` (e.g. query "TRU" matches `{name: "trunk", path: "/x/trunk"}`)
    - Test 3: case-insensitive substring match against `path` (e.g. query "users" matches `{name: "x", path: "/Users/joao/x"}`)
    - Test 4: whitespace-only query is treated as empty (returns input unchanged)
    - Test 5: no matches returns empty array
    - Test 6: preserves original ordering of input (it filters, does not sort)
    - Test 7: matches on either name OR path (union), not requires both
  </behavior>
  <action>
  Step A — Create `src/lib/recent-filter.ts`. Export a single pure function `filterRecents(repos: RecentRepo[], query: string): RecentRepo[]`. Trim the query; if empty, return `repos` unchanged. Otherwise lowercase the trimmed query once, then `repos.filter(r => r.name.toLowerCase().includes(q) || r.path.toLowerCase().includes(q))`. Import `RecentRepo` from `./store.js`. No fuzzy matching — substring only (per CONTEXT.md "Claude's Discretion").

  Step B — Write `src/lib/recent-filter.test.ts` covering the seven behaviors above. Follow the existing test style in `src/lib/invoke.test.ts` (vitest `describe`/`it`/`expect`). No DOM, no Tauri mocks needed — pure function.

  Step C — Create `src/components/RecentReposPicker.svelte`. Props (Svelte 5 `$props()` rune):
  - `open: boolean` — visibility flag (parent controls).
  - `onpick: (path: string, name: string) => void` — called when user picks a row.
  - `onclose: () => void` — called on Esc, Backspace-empty, backdrop click.

  Internal state (`$state` runes):
  - `query: string = ""`
  - `recents: RecentRepo[] = []` — loaded and pruned each time `open` flips true.
  - `resolvedPaths: Record<string, string> = {}` — cache of `displayPath()` results, populated in an `$effect` over `recents` (mirror the WelcomeScreen pattern).
  - `highlightedIdx: number = 0` — index into the filtered list.
  - `loading: boolean = false` — true during the prune/load phase.

  Derived (`$derived`):
  - `filtered = filterRecents(recents, query)`

  Behavior:
  - `$effect` watching `open`: when it transitions to `true`, run an async load:
    1. Set `loading = true`, clear `query`, reset `highlightedIdx = 0`.
    2. Fetch `getRecentRepos()`.
    3. Run `Promise.all` mapping each repo to `safeInvoke<boolean>("validate_recent_path", { path: repo.path })`. Treat any rejected promise as `false` (use `.catch(() => false)` per entry) — never let prune errors crash the picker.
    4. Partition: kept entries → in-memory list; dropped entries → `await Promise.all(dropped.map(r => removeRecentRepo(r.path)))`.
    5. Assign `recents = kept`, set `loading = false`. Focus the input element (use `bind:this` on the input and call `.focus()` after the assignment via `tick()` from `svelte`).
  - Keyboard handling on the input (`onkeydown`):
    - `ArrowDown`: `highlightedIdx = Math.min(filtered.length - 1, highlightedIdx + 1)`; `preventDefault()`.
    - `ArrowUp`: `highlightedIdx = Math.max(0, highlightedIdx - 1)`; `preventDefault()`.
    - `Enter`: if `filtered[highlightedIdx]` exists, call `onpick(filtered[highlightedIdx].path, filtered[highlightedIdx].name)`; `preventDefault()`.
    - `Escape`: call `onclose()`; `preventDefault()`.
    - `Backspace`: only if `query.length === 0`, call `onclose()` (do NOT `preventDefault` for normal backspace — the browser must keep deleting characters when query is non-empty).
  - When `query` changes, clamp `highlightedIdx` so it stays in `[0, filtered.length - 1]` via a `$effect`.
  - Mouse `onmouseenter` on each row sets `highlightedIdx` to that row's index.
  - Click on a row calls `onpick(repo.path, repo.name)`.

  Render (only when `open === true`):
  - Outer fixed-position backdrop covering the viewport: `position: fixed; inset: 0; background: rgba(0,0,0,0.5); z-index: 50;` (semi-transparent black backdrop is acceptable here — it is an opacity overlay, not a theme color; do NOT use `var(--color-bg)` for the backdrop). Click on backdrop calls `onclose()`. Stop propagation on the modal panel itself.
  - Centered modal panel: `width: 480px; max-width: 90vw; margin-top: 80px;` positioned via flex justify-center on the backdrop. Panel uses theme tokens only: `background: var(--color-surface); border: 1px solid var(--color-border); border-radius: 6px; color: var(--color-text);`. No inline colors.
  - Top: a text `<input>` bound to `query` with placeholder "Search recent repositories", `bind:this={inputEl}`. Border-bottom uses `var(--color-border)`.
  - Body content:
    - If `loading`: render nothing in the body (panel + input still shown).
    - Else if `recents.length === 0`: empty-state message "No recent repositories" with a button "Open Repository". Button uses the same styling pattern as WelcomeScreen.openRepository — `background: var(--color-accent);`. On click: import `open` from `@tauri-apps/plugin-dialog`, prompt for directory, and if a path is returned, call `onpick(path, derivedName)` where `derivedName = selected.split('/').at(-1) || selected`.
    - Else if `filtered.length === 0`: text "No matches" using `var(--color-text-muted)`.
    - Else: a `<ul>` of rows. Each row is two-line: top line bold repo name (`var(--color-text)`), second line tildified path (`var(--color-text-muted)`) from `resolvedPaths[repo.path] ?? repo.path`. Highlighted row uses `background: var(--color-hover);` (or whatever existing hover token the codebase uses — grep `--color-hover` first; if absent, use `background: rgba(255,255,255,0.06)` as a neutral hover; do not invent a new theme token).
  - Keep the file under ~180 lines. Reuse existing styling patterns from `WelcomeScreen.svelte` for visual consistency.

  Step D — Wire into `src/App.svelte`:
  1. Add an import for the picker: `import RecentReposPicker from "./components/RecentReposPicker.svelte";`
  2. Add `let pickerOpen = $state(false);` near the other top-level state.
  3. In the existing keydown `$effect` (the one near line 393), inside the `if (e.metaKey || e.ctrlKey)` block, BEFORE the zoom/pane branches, add an `r`/`R` branch:
     - Skip if `document.activeElement` is `HTMLInputElement`, `HTMLTextAreaElement`, or has `getAttribute("contenteditable") === "true"`.
     - Skip if `pickerOpen === true` (per CONTEXT.md: second Cmd/Ctrl+R while open is a no-op).
     - Call `e.preventDefault()` and set `pickerOpen = true; return;`
  4. Add an async handler `async function handlePickerPick(path: string, name: string)`:
     - If `activeTab?.repoPath === path`, just set `pickerOpen = false; return;` (no-op on same repo, per CONTEXT.md).
     - Try `await safeInvoke("open_repo", { path })`. On failure: `removeRecentRepo(path)` (the validate step missed it), set `pickerOpen = false`, return. Do not surface an error toast — quiet by design.
     - On success: `await addRecentRepo({ name, path })`; call `openRepoInTab(activeTabId, path, name)`; set `pickerOpen = false`.
  5. Mount the picker just above `<Toast />` at the bottom of the template:
     ```
     <RecentReposPicker open={pickerOpen} onpick={handlePickerPick} onclose={() => (pickerOpen = false)} />
     ```

  Do NOT modify `WelcomeScreen.svelte`. Do NOT add fields to `RecentRepo`.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && pnpm vitest run src/lib/recent-filter.test.ts 2>&1 | tail -20</automated>
    <automated>cd /Users/joaofnds/code/trunk && pnpm svelte-check 2>&1 | tail -20</automated>
    <automated>cd /Users/joaofnds/code/trunk && pnpm biome check src/lib/recent-filter.ts src/lib/recent-filter.test.ts src/components/RecentReposPicker.svelte src/App.svelte 2>&1 | tail -20</automated>
  </verify>
  <done>
  - All seven filterRecents tests pass.
  - `svelte-check` reports zero errors.
  - `biome check` clean on the changed files.
  - Manual smoke (run by user during verification, not in CI): Cmd/Ctrl+R opens the picker; typing filters; Up/Down moves highlight; Enter switches the current tab to that repo; Esc closes; backdrop click closes; deleting a recent repo's directory then opening the picker silently drops it.
  </done>
</task>

</tasks>

<verification>
- `cargo check` (src-tauri) succeeds.
- `pnpm vitest run` passes (filter unit tests).
- `pnpm svelte-check` reports zero errors.
- `pnpm biome check` clean on changed files.
- `just check` succeeds end-to-end.
- Manual: Cmd/Ctrl+R opens the picker from inside an open repo tab AND from a Welcome-only tab; webview does not reload; deleting a recent repo's directory then opening the picker silently prunes it.
</verification>

<success_criteria>
- All four locked CONTEXT.md decisions implemented exactly:
  1. Cmd/Ctrl+R bound globally with `preventDefault()` so webview reload is suppressed.
  2. Picker opens repo in CURRENT tab via App.svelte's existing `openRepoInTab` (no duplicate open path).
  3. Missing/non-repo paths are pruned every time the picker opens; pruning is silent.
  4. Rows render two-line: name (bold) + `displayPath()` tildified path.
- `WelcomeScreen.svelte` untouched.
- `RecentRepo` shape unchanged.
- No inline hex/rgb theme colors in `RecentReposPicker.svelte` (backdrop's `rgba(0,0,0,0.5)` is an opacity overlay, not a theme token, and is acceptable; grep confirms zero hex literals).
- Filter logic covered by vitest.
</success_criteria>

<output>
After completion, create `.planning/quick/260514-356-build-a-recent-projects-picker-vscode-ct/260514-356-SUMMARY.md` documenting:
- Files touched (final list).
- Tauri command added (`validate_recent_path`).
- Decisions made under "Claude's Discretion" (e.g. fuzzy vs substring, hover-row styling token chosen).
- Any deviation from CONTEXT.md (should be none — flag explicitly if any).
- Manual verification steps performed.
</output>
