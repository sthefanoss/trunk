---
phase: quick-260405-ik1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/commands/staging.rs
  - src-tauri/src/lib.rs
  - src-tauri/tests/common/drivers/staging.rs
  - src-tauri/tests/test_staging.rs
  - src/components/StagingPanel.svelte
autonomous: true
requirements: [fix-tree-view-folder-staging]

must_haves:
  truths:
    - "Clicking stage on a folder stages ALL files in that folder, not just one"
    - "Clicking unstage on a folder unstages ALL files in that folder, not just one"
    - "Bulk stage/unstage handles mix of new, modified, and deleted files"
  artifacts:
    - path: "src-tauri/src/commands/staging.rs"
      provides: "stage_files_inner and unstage_files_inner bulk functions"
      contains: "pub fn stage_files_inner"
    - path: "src-tauri/src/commands/staging.rs"
      provides: "stage_files and unstage_files Tauri commands"
      contains: "pub async fn stage_files"
    - path: "src/components/StagingPanel.svelte"
      provides: "stageDirectory and unstageDirectory call bulk commands"
      contains: "stage_files"
  key_links:
    - from: "src/components/StagingPanel.svelte"
      to: "src-tauri/src/commands/staging.rs"
      via: "invoke('stage_files') and invoke('unstage_files')"
      pattern: 'invoke\("stage_files"'
---

<objective>
Fix race condition in tree view folder staging where concurrent individual `stage_file` IPC calls cause last-writer-wins on the git index, resulting in only one file being staged instead of all.

Purpose: Folder staging/unstaging is broken -- only one file ends up staged/unstaged due to concurrent index writes.
Output: Bulk `stage_files` and `unstage_files` Tauri commands that process all paths in a single index read/write cycle, with frontend updated to use them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src-tauri/src/commands/staging.rs
@src-tauri/src/lib.rs
@src/components/StagingPanel.svelte
@src-tauri/tests/common/drivers/staging.rs
@src-tauri/tests/test_staging.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bulk stage_files and unstage_files Rust commands with tests</name>
  <files>src-tauri/src/commands/staging.rs, src-tauri/src/lib.rs, src-tauri/tests/common/drivers/staging.rs, src-tauri/tests/test_staging.rs</files>
  <behavior>
    - Test: stage_files with 3 modified files in a directory stages all 3 (all appear in status.staged, none in status.unstaged)
    - Test: stage_files with mix of new + modified files stages all correctly
    - Test: stage_files with a deleted file stages the deletion
    - Test: stage_files with empty vec is a no-op (no error)
    - Test: unstage_files with 3 staged files unstages all 3 (none in status.staged)
    - Test: unstage_files on unborn HEAD removes all specified paths from index
    - Test: unstage_files with empty vec is a no-op (no error)
  </behavior>
  <action>
    1. In `src-tauri/src/commands/staging.rs`, add `stage_files_inner`:
       - Signature: `pub fn stage_files_inner(path: &str, file_paths: &[String], state_map: &HashMap<String, PathBuf>) -> Result<(), TrunkError>`
       - Open repo once via `open_repo_from_state`
       - Get index once via `repo.index()?`
       - Loop through all `file_paths`: if abs_path exists, `index.add_path()`; else `index.remove_path()` (handles deletions)
       - Single `index.write()` at the end
       - Follow `stage_file_inner` (line 117) pattern for the add/remove logic, `stage_all_inner` (line 288) for the single-write pattern

    2. Add `unstage_files_inner`:
       - Signature: `pub fn unstage_files_inner(path: &str, file_paths: &[String], state_map: &HashMap<String, PathBuf>) -> Result<(), TrunkError>`
       - Handle unborn HEAD case: get index, `index.remove_path()` for each file, single `index.write()`
       - Normal case: use `repo.reset_default(Some(head_commit.as_object()), file_paths.iter().map(String::as_str))` -- this already accepts an iterator so all paths reset in one call
       - Follow `unstage_file_inner` (line 184) pattern

    3. Add `#[tauri::command]` wrappers:
       - `pub async fn stage_files(path: String, file_paths: Vec<String>, state: State<'_, RepoState>) -> Result<(), String>` -- same spawn_blocking + error mapping pattern as `stage_file`
       - `pub async fn unstage_files(path: String, file_paths: Vec<String>, state: State<'_, RepoState>) -> Result<(), String>` -- same pattern

    4. Register in `src-tauri/src/lib.rs`: add `commands::staging::stage_files` and `commands::staging::unstage_files` to the `invoke_handler` list (after `stage_file` and `unstage_file`)

    5. Add test driver methods in `src-tauri/tests/common/drivers/staging.rs`:
       - `pub fn stage_files(&self, file_paths: &[&str]) -> Result<(), TrunkError>` -- converts to Vec<String> and calls `stage_files_inner`
       - `pub fn unstage_files(&self, file_paths: &[&str]) -> Result<(), TrunkError>` -- converts to Vec<String> and calls `unstage_files_inner`

    6. Add tests in `src-tauri/tests/test_staging.rs` per the behavior block above.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && cargo test -p trunk --test test_staging -- stage_files unstage_files 2>&1</automated>
  </verify>
  <done>All bulk staging tests pass. stage_files_inner stages N files atomically in one index write. unstage_files_inner unstages N files atomically. Both handle edge cases (empty list, deleted files, unborn HEAD).</done>
</task>

<task type="auto">
  <name>Task 2: Update frontend to call bulk commands for directory staging</name>
  <files>src/components/StagingPanel.svelte</files>
  <action>
    In `src/components/StagingPanel.svelte`:

    1. Update `stageDirectory()` (line 177): Replace the `Promise.all` with individual `stage_file` calls with a single `safeInvoke("stage_files", { path: repoPath, filePaths: pathsToStage })`. The `filePaths` param name matches the Rust `file_paths` field (Tauri uses camelCase for serde deserialization by default with `rename_all = "camelCase"` -- but check existing commands: they use `file_path` snake_case in Rust and `filePath` camelCase in TS. The new command takes `file_paths: Vec<String>` in Rust, which Tauri's serde will expect as `filePaths` from JS).

    2. Update `unstageDirectory()` (line 196): Same change -- replace `Promise.all` with individual `unstage_file` calls with a single `safeInvoke("unstage_files", { path: repoPath, filePaths: pathsToUnstage })`.

    Note: The loading state logic (adding/removing from loadingFiles set) stays the same -- it already correctly adds all paths before the call and removes after.
  </action>
  <verify>
    <automated>cd /Users/joaofnds/code/trunk && just check 2>&1 | tail -30</automated>
  </verify>
  <done>stageDirectory() calls stage_files with all paths in one IPC call. unstageDirectory() calls unstage_files with all paths in one IPC call. No more Promise.all with individual calls. All checks pass (fmt, biome, svelte-check, clippy, cargo-test, vitest).</done>
</task>

</tasks>

<verification>
1. `cargo test -p trunk --test test_staging` -- all staging tests pass including new bulk tests
2. `just check` -- all 6 checks pass (fmt, biome, svelte-check, clippy, cargo-test, vitest)
3. Manual: Open a repo with multiple unstaged files in a folder, click stage on the folder -- all files stage
4. Manual: Click unstage on the folder -- all files unstage
</verification>

<success_criteria>
- Bulk stage_files command stages N files in one atomic index write (no race condition)
- Bulk unstage_files command unstages N files in one atomic reset_default call
- Frontend stageDirectory/unstageDirectory use bulk commands
- All existing and new tests pass
- `just check` passes clean
</success_criteria>

<output>
After completion, create `.planning/quick/260405-ik1-fix-tree-view-folder-staging-to-stage-al/260405-ik1-SUMMARY.md`
</output>
